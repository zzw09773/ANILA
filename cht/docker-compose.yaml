services:
    api:
        build: .
        volumes:
            - .:/workspace:ro
        working_dir: /workspace
        command: python app.py
        ports:
            # The synthetic signer is a localhost-only development device.
            # Never expose its fixed test PIN and signing API to the LAN.
            - "127.0.0.1:16888:16888"

