fastapi==0.136.1
# 與 CSP／flux-agent 同鎖。2026-08-05 三個服務一起前進到 1.3.1：0.49.3 帶著
# CVE-2026-48710（`request.url` 由呼叫端 `Host` 標頭字串串接而成，路徑邊界可被移動）。
# 本服務未直接 import starlette，升級純粹是不要把已知有洞的版本凍進氣隙 wheelhouse。
starlette==1.3.1
uvicorn[standard]==0.34.0
pydantic==2.9.2
# Inference deps (NOT pinned exact because we layer on top of a CUDA base image)
torch==2.6.0
# Pin diffusers >= 0.36 (FLUX.2-dev requires Flux2Pipeline added in 0.36.0.dev0)
diffusers>=0.36.0,<0.40.0
# Pin transformers <5 — transformers 5.x's moe.py uses torch.library.custom_op
# signatures that torch 2.4.0 cannot infer (ValueError on import). 4.50.x works.
transformers>=4.50.0,<5.0
accelerate>=1.1.0,<2.0
sentencepiece
protobuf
safetensors
Pillow
