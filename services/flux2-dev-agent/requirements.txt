fastapi==0.136.1
# 與 CSP 同鎖。2026-08-05 三個服務一起前進到 1.3.1：0.49.3 帶著 CVE-2026-48710
#（`request.url` 由呼叫端 `Host` 標頭字串串接而成，路徑邊界可被移動）。
# 本服務未直接 import starlette，升級純粹是不要把已知有洞的版本凍進氣隙 wheelhouse。
# ⚠ 本檔 httpx 仍鎖 0.27.2：starlette 1.x 的 TestClient 優先找 httpx2，找不到會
# 退回 httpx 並發 StarletteDeprecationWarning —— 是警告不是錯誤，測試照跑。
starlette==1.3.1
uvicorn[standard]==0.34.0
# 刻意留 httpx 0.27.2：升到 0.28 需連帶升 respx≥0.22，且本服務無 httpx advisory。
httpx==0.27.2
pydantic==2.9.2
# 0.0.12 有多條 form-parsing CVE；與 CSP 同鎖 0.0.32。
python-multipart==0.0.32
