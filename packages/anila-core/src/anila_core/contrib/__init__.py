"""Optional drop-in helpers (not part of the runtime import surface).

``anila_verify`` is intentionally a single-file module third-party agents
can copy verbatim; prefer importing it by file path in air-gapped deploys.
"""
