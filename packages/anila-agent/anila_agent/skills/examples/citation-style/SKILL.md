---
name: citation-style
description: 統一回答中的來源引用格式
when_to_use: 當回答需要引用檢索到的文件片段時
trusted: true
---

# 引用格式

- 每個事實主張後緊接來源：`【來源：<chunk id 或檔名>】`。
- 多個來源時逐一列出：`【來源：a, b】`。
- 無法從檢索內容支撐的內容，明確標示「（無來源）」或改為查無。
- 引用 id 一律使用 search_documents 回傳的 `id` 或 `metadata.filename`。
