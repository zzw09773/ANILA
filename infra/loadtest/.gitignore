# k6 summary exports are run artefacts, not source. The numbers that matter
# are transcribed into docs/planning/load-baseline.md.
results/

# ZIP fixtures are generated by make-zip-fixtures.sh — binaries do not belong
# in the repo.
fixtures/
