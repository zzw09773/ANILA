# UI 全面掃蕩 — 發現清單（2026-08-12 第一輪）

> 擁有者指示：「請對面使用瀏覽器，測試每一顆按鈕都是好的，每一個功能都是正常的。」
> ⚠ 這份原本在 `/tmp` scratchpad，**隨機器當機蒸發**，此為重建版並改放 repo。
> 工具：自寫 playwright 腳本 ＋ 系統 google-chrome ＋ `ignoreHTTPSErrors`（自簽憑證）。
> **全程沒有改任何 harness 設定，也沒有把 http→https 的 301 關掉。**

## 待修（四條）

### 🔴 F-1 「註冊」表單送出後顯示英文的 `Not Found` — **2026-08-15 重新開啟**

> 🔴 **我曾把這條判為已關，那是錯的。UI-1 藏的是入口，不是缺陷本身。**
> `LoginView.vue:248` 的註冊 modal **仍然存在**，條件是 `v-if="showAlternativeLogin"`
> ——而它**在破窗參數 `?show_alternatives=1` 下為真**。
> `handleRegister()` 的錯誤處理是 `detail || '註冊失敗'`，後端 404 的 body 是
> `{"detail":"Not Found"}` → **原封不動顯示英文**。
> ⚠ **而破窗正是卡登壞掉時管理員唯一的入口**——平台出事、人最緊張的那一刻。
> 📌 教訓：**證明「入口消失」不等於證明「行為改變」。**（詳見
> `~/anila-deliverables/audit-findings-closed-20260815.md`）

**重現**：登入頁 →「其他登入方式」→「註冊」→ 填帳號/EMAIL/密碼 →「送出」
**使用者看到**：`! Not Found`（英文，未翻譯）
**根因（2026-08-13 更正，我原本判錯）**：
❌ 我原本寫「前端打的端點**根本不存在**」——**那是錯的**。
✅ `POST /api/auth/register` **確實存在**於路由表；那個 404 是**刻意的政策回應**：
`services/csp/app/api/auth/_common.py:29-41` 的 docstring 逐字寫著——
> 回 404 而非 403/410，讓非卡片 endpoint 在內網部署「**看起來不存在**」——
> 外部探測無法區分「該功能本來就沒做」還是「被政策關掉」。

**所以缺陷不在後端，純粹在 UI**：畫面**提供了一個表單**，去打一條這個部署模式
**刻意讓它看起來不存在**的路徑，然後把框架的原始 404 文字（`Not Found`，未翻譯）給使用者看。

🔴 **修這條時最容易犯的錯**：把後端改成回 403 或友善訊息。
**那會破壞上面那個刻意的不可分辨性**——後端一個位元組都不該動。

> 🔴 **2026-08-15 更正：上面引用的那個「不可分辨性」，實測不成立。**
> 我親手發請求量過（不是推論）：
> - `GET` 不存在的路徑 → **`200 text/html`**（SPA 殼 1534B）
> - `POST` 不存在的路徑 → **`405`** `{"detail":"Method Not Allowed"}`
> - `POST /api/auth/register` **空 body** → 🔴 **`422`，附完整欄位清單**（洩漏的是**形狀**，比存在更多）
> - `POST` 同一路徑**填好欄位** → `404`（那顆刻意的）
>
> **所以那段 docstring 宣稱了系統沒有的性質。**
> ⚠ **危害不是洩漏，是它會變成錯誤的約束**——我今天兩次拿它當理由說「後端不准動」。
> **一段錯的註解會變成一個錯的決定。**
>
> **「後端不准動」這個結論仍然成立，但理由要換**：
> 不是因為不可分辨性（它不成立），而是因為
> **F-1 是 UI 缺陷，改後端對它沒有幫助，且會引入沒有好處的行為變更**。
>
> 📌 **判定：不修行為**（統一 404／壓掉 422 細節要加中介層，
> 那是「把系統越搞越嚴」那條路，也是上一輪 25 天工作被放棄的直接原因）。
> **只修那段 docstring**，讓它說它真正成立的話。

**驗證**：curl（經 nginx）與容器內直打（繞過 nginx）都是 404；`users` 表**零新增**。

⚠ **為什麼排第一**：這是**三千人看到的第一個畫面**。那顆按鈕開出一個做得很完整的表單
（含即時的「8 字元以上·大寫·小寫·符號」強度指示），填完得到一句英文 `Not Found`。
使用者不會知道那是「這個部署模式不開放自助註冊」。

### 🟡 F-2 card-only 下整塊「其他登入方式」對一般使用者都是死路

帳密登入在 `ANILA_AUTH_MODE=card-only` 時**只保留給 owner**
（`services/csp/app/api/auth/password.py:88-93`）。一般同仁填了帳密按登入，**必定失敗**。
👉 建議 card-only 時整塊隱藏，或改成一句「本平台僅接受憑證卡登入」。owner 破窗留著。

### 🔴 F-3 首次上線硬阻擋：沒有單位，任何人都完不成註冊 — **2026-08-15 改判「未修」**

> **這是文件緩解，不是平台修正。** 初裝章 §2.1 寫了、演練也實際執行過，
> 但**平台行為一個位元組都沒變**，正當性完全依賴「操作者會照文件做」。
> 🔴 **而且守衛在這裡做不到**：全新安裝必然是零單位，
> 擋了就是死鎖（不啟動 → 無法建單位 → 永遠不啟動）。
> 👉 所以正解不是守衛，是**owner 首次登入後的初裝待辦提示**
> （**提示不是限制**，不牴觸「不要把系統越搞越嚴」）。不擋 tag，建議排。

卡登驗章通過後，畫面要求選單位，但「**目前沒有可選的單位，需要管理員先建立。**」
DB 實測 `departments` **0 筆** → **提示是誠實的**（不是假控制項 ✅）。
⚠ **但三千人插卡都會卡在這一步**，而第 8 段演練是 `down -v`，單位會歸零 → **演練必撞**。
👉 「建立單位」必須列進部署文件的初裝必要步驟（與 admin 密碼最後一哩、JWT keypair 並列）。
📌 平台已知這個家族的風險：`startup_security.py:68-72` 擋了
`CARD_INITIAL_OWNERS` placeholder 導致「沒人能 approve → bricked」，**但「零單位」沒有對應守衛**。

### 🟡 F-4 打錯網址 → 完全空白的頁面（治理中心沒有 catch-all 路由）

實測不存在的路徑 → `innerText` 長度 **0**，沒有「找不到頁面」、沒有回首頁的路。
⚠ **我自己就被這個騙過**：第一輪我掃了三個**我編的路徑**，全部「零錯誤、零按鈕」，
差點報成「頁面壞掉」。**空白頁不產生錯誤，所以它能通過任何以「有沒有錯誤」為判準的檢查。**

### 🟡 F-5 「重新載入 agent」按了沒有任何回饋

實測：**確實發出** `/v1/agents` 請求，但畫面完全沒變化——沒有轉圈、沒有「已更新」。
使用者無法分辨「按了有用」與「按了沒用」。⚠ `docs/FAKE-CONTROLS.md` 那一族的輕症版本。

## ✅ 通過（實測）

- **未登入存取控制**：13 條路徑全部導向 `/login`。例外只有 `/health`（標準健康端點）
  與 `/anilalm/`（刻意的 release gate，**有正常中文說明頁**）。
- **治理中心 20 條真實路由**：`/`、`alerts`、`api-keys`、`audit-logs`、`banners`、
  `classification-inventory`、`departments`、`developer/agents`、`developer/guide`、`feedback`、
  `knowledge-collections`、`message-actions`、`models`、`platform-links`、`platform-settings`、
  `service-access`、`service-clients`、`trusted-hosts`、`usage`、`users`
  —— 每頁都有內容、有按鈕、**零 HTTP 錯誤、零 JS 例外、零 console error**。
- **使用者介面 `/anila/` 主工具列**逐顆實測：設定／新功能／Tweaks／Agents／專案入口 ✅；
  切換深色 ✅（**改看 `data-theme` 與背景色才驗得出，文字比對會誤判**）；
  附加檔案 ✅（`filechooser` 事件真的觸發）；比較模式 ⊘ 停用並說明原因；
  **任務中心不是死鈕**——它是「目前所在」項目。
- **已加星**：❓ 無法判定，目前一則對話都沒有，篩選器無從觀察。

## 🟢 一個值得抄的正例：語音輸入的錯誤訊息

> 「找不到可用的麥克風。**這不是權限問題**——請確認麥克風已接上，
> 並在系統的『聲音 → 輸入』裡看得到它、而且不是靜音。」

**先排除使用者最可能的誤判，再給可執行步驟。** F-1 那句 `Not Found` 可以照這個改。

## 尚未涵蓋（誠實說明）

- 需要**有資料**才能測的：對話中的每個動作（重新產生、編輯重送、分支切換、引用抽屜、
  加星、資料夾）、知識庫上傳與檢索、記憶頁刪除、比較模式（需 2 個 agent）。
- 治理中心 20 頁**內部**約 180 顆按鈕，只驗了「頁面載入無錯」，**尚未逐顆點**。

## 測試環境紀律（供重跑）

- 卡登在本機需 `ANILA_AUTH_MODE` **不是** `card-only`（三條件之一，`card_auth.py:80-96`）。
  當時經擁有者授權暫改，**用 shell 環境變數傳給 compose，`.env` 未被寫入**（sha256 前後一致），
  測完復原並**實測守衛重新擋下**。
- mock 讀卡機：`cd cht && CHT_DEV_CA_HOST_DIR=../secrets/dev-card-ca docker compose -p cht-mock up -d --build`，PIN `123456`。
- ⚠ 測試留下 `9999999`（role=owner）在 DB 裡，是 mock 假卡 bootstrap 出來的；第 8 段 `down -v` 會清掉。

---

## 🔴 F-6（2026-08-13，審 UI-2 時撞到）：突變檢查器死在第 4 條，其餘 44 條從來沒跑過

**`apps/anila-shell/scripts/mutation-check.mjs` 是這個專案宣告的品質閘門**
（`CLAUDE.md`：「宣稱『測試過了』之前先跑突變檢查」）。

**實測**：
- 突變 `update-msg-wipes-list` 會讓測試行程收到 SIGTERM；
  檢查器把它當成**使用者要停**（`:699-702` 的註解寫明這個判斷），於是**乾淨地提早離開**。
- 那條突變排在 **第 4 條 / 全部 48 條** → **後面 44 條從來沒有被執行過**。
- 離開碼 **130**（SIGINT 慣例，看起來像「使用者按了 Ctrl-C」）；正常跑完是 2。
- 畫面訊息是「測試行程被 SIGTERM 中止，還原被突變的檔案後離開」
  ——**讀起來像使用者中斷，不像失敗**。

**確認是既有問題，與當時審的 UI-2 無關**：在乾淨基底 `66906308`（無 UI-2 改動）
單跑那一條，**同樣死法**。UI-2 的作者不需要為此負責。

⚠ **為什麼這條要緊**：這是「**恆真綠燈**」的最高階版本——
不是某條測試沒咬住，而是**整個工具在第 4 步就停了，而停下來的樣子很像正常結束**。
任何人跑它、看到檔案被還原、沒有紅字，都會以為跑完了。

**要修的兩件（分開）**：
1. **檢查器**：子行程被 SIGTERM ≠ 使用者中斷。要能分辨，並在非使用者中斷時
   **以失敗離開碼結束、且明說「還有 N 條沒跑」**。
2. **那條突變本身**：`update-msg-wipes-list` 讓 `updateMsg` 回空，疑似造成 render 迴圈
   ／記憶體暴走而被外部殺掉。要嘛修那條突變的形狀，要嘛替它加隔離。

👉 在修好之前，**任何「突變檢查通過」的宣稱只涵蓋前 3 條**。

---

## 🔴 F-7（2026-08-14，第 8 段演練實測）：錯誤訊息把原始 JSON 吐在對話框裡

**環境**：第 8 段從零演練的棧（08-13 出貨包），owner 破窗登入，聊天送出一則問題。

**重現**：
1. 從零安裝完成、**尚未指定主路由模型**（這是初裝的正常中間狀態，見發現 #13）
2. 在 `/anila/app` 送出任何一個問題

**畫面上出現的**（不是 console，是使用者看得到的對話區）：

```
{"detail":"ANILA Router 無可用主路由模型。請管理員前往 CSP Models 頁面指定一個
LLM 為「主路由」。（細節：CSP 404: {\"detail\":\"尚未指定 ANILA 主路由模型\"}）"}
```

下面才接一句給人看的「產生過程發生錯誤，這則回答沒有留下任何內容。」

**三個問題（依嚴重度）**：
1. 🔴 **原始 JSON 直接呈現給使用者**——三千人會看到 `{"detail":...}` 這種東西。
2. 🔴 **巢狀跳脫沒有解開**（`{\"detail\":\"…\"}`），連工程師讀起來都費力。
3. 🟡 **洩漏內部結構**：`CSP 404` 把服務名與內部狀態碼攤出來。
   全域規矩是**錯誤訊息不洩內部資訊**。

⚠ **注意這裡有個好東西不要一起改掉**：**訊息的內容是對的**——
它明確說了「請管理員前往 CSP Models 頁面指定一個 LLM 為主路由」，
**是一句可以照著做的話**。要修的是**呈現**（解析出 `detail`、當成一般文字顯示），
**不是**把它換成「發生錯誤，請洽管理員」那種沒有資訊的句子。

### 📌 順帶（同一次實測，小但三千人會看到）

日期顯示成 **`14/08/2026, 18:44:22`**（DD/MM/YYYY）。
台灣慣用 **`2026/08/14`**。⚠ 這個格式對台灣讀者**不只是不習慣，是會誤讀**——
`08/14` 與 `14/08` 在月份 ≤ 12 的日子會完全反過來。
（治理中心 `/departments` 的「建立時間」欄實測；其他頁面尚未逐一掃。）

---

## 🟡 F-8（2026-08-15，跨家重驗 sol 找到）：下游服務的錯誤字串被原樣轉送給使用者

**F-7 修好的是「平台自己產生錯誤」那條路。這一條是「下游產生錯誤、平台照抄」。**

**路徑**：CSP 的 proxy 對已註冊模型／agent 的 SSE block **除 `anila.meta` 外原樣轉送**
（`services/csp/app/services/proxy/service.py:917-945, 998-1050`，文件與實作都明講 verbatim）。
下游若送 `event: anila.error`，其字串 `message` **未經正規化**就被 shell throw 成 Error
（`sse.js:315-320, 393-403`）→ 寫進 `msg.error`（`app.jsx:1524-1531`）→ 顯示在聊天泡泡
（`chat.jsx:879-895`）。sol 用純記憶體 stub 送 raw JSON 與 raw HTML 各一，**兩者都原樣進到畫面**。

### 🔴 為什麼這是「既有不變式沒套滿」，不是新政策

**我們對自己的錯誤是有守衛的**：`service.py:1293` 發 `anila.error` 時，
訊息取自 `stream_failure_user_message()`（`:155-157`）——**固定中文字串，從不內插上游例外**，
而且 `test_proxy_stream_error.py:180` 有測試守著。

> **所以：對「自己的錯誤」有正規化，對「別人的錯誤」沒有。**
> 而 2026-08-15 的 🅱 包（`1c24550a`）剛為 HTTP 路徑立下的不變式正是
> **「不得把下游字串當自己的字串顯示，原文進 log」**。**同一條規矩，另一個傳輸層。**

**驗收工具**：`~/anila-deliverables/verification-probes-20260815/`
（`probe-r2.mjs` 要加「下游送 `event: anila.error`」那一種形狀；**同時驗原文有沒有進 log**）。

**修法**：沿用 🅱 的形狀——轉送的 `anila.error` 其 `message` 不得原樣進使用者可見文字；
**原文進 log（移不是刪）**。

### ⚠ 可達性是一個**會過期**的判斷

`anila.error` 是 **CSP 內部事件名**：全樹搜過，開發者指南、`docs/`、
治理中心開發指南頁**零命中**，只出現在 CSP 自己的測試與產生器。
所以今天下游要踩到它，得**自己猜中我們的內部名**——**可達性低**。

🔴 **但有兩個升溫因素**：
1. **本 repo 是 PUBLIC** → agent 開發者讀得到 `format_anila_stream_error`，
   **然後合理地以為那是回報錯誤的契約**——它**看起來像契約，只是我們沒把它寫成契約**。
2. **Q49 已裁定同事的 agent 全部走平台** → **下游作者只會變多**。

👉 **不擋上線 tag，但不得標成「低優先」**（低優先會沉底）。
**標成「上線後每多一個外部 agent 就重評一次」。**

📌 **它為什麼躲過 F-7 的四形狀探針**：那四種形狀**全是平台自己產生的錯誤**。
**測了「我們怎麼講自己的錯」，沒測「別人怎麼講它的錯，而我們照抄」。**

---

## 📌 F-2 的處置（2026-08-15）：不重開，併入 F-1 修復包

sol 確認 `card-only` ＋ `?show_alternatives=1` 下，一般同仁**看得到也送得出**帳密表單。
🔴 **新事實**：`loginSurface.js:5-13, 36-37` 的顯示判斷**只看 mode 與 query，沒有 role 判斷**
——而且**結構上不可能有**（登入頁上還沒有人通過認證，沒有身分可判）。

> **所以「破窗是給既有 owner 用的」是我們的意圖，不是碼的性質。**
> 安全性仍成立（後端對五類非 owner 情形一律回 404），**但描述要改**。

**不另立案的理由**：可達性低（該參數只寫在管理員手冊），
且**修法與 F-1 完全同一塊畫面**——分開會讓同一個介面被兩包各改一次。
