# 交接 — 2026-08-06（接手先讀這份）

> 上一份是 `docs/HANDOFF-2026-08-05.md`。
> **一句話狀態**：**第 3 段做完、第 4 段做一半**。六包全部合進主線、全綠。
> **主線領先遠端 68 個 commit，沒有推。映像還是舊的。**

---

## ⓪ 三件先知道

**一、順序照 `PLAN.md` 開頭那九段**，而且那張表現在有**狀態欄**。
第 6 段已經正式移到第 5 段之前（先定版本、再凍 wheelhouse）。
**下一件是第 4 段剩的那半：csp 映像沒有 `USER`。**

**二、`PLAN.md` 新增了一個區塊：〈第 8 段之前必須關掉的〉。**
六件，全部是第 3、4 段做的時候**量出來的**，不是猜的。
**沒關掉就重建映像，等於把它們封進交付包。**

**三、交付包在 repo 外**：`~/桌面/ANILA-DELIVERY/`。裡面的映像建於 08-03，落後主線非常多。

---

## 一、今天做完的（六包，全部經雙輪以上驗收）

| 包 | 修了什麼 | 輪數 |
|---|---|---|
| **健康總覽假訊號** | 路由是假綠燈（404 被當健康）、匯入工作者是永久假紅燈（拖紅整個總覽）、nginx 探測是**當天自己打壞的** | 實作 4、驗收 3 |
| **PII 誠實化** | 設定頁說「CSP 會遮罩」是假的；「阻擋」其實擋不住四條路 | 實作 5、驗收 3 |
| **RAG 靜默退化** | 檢索失敗時**對模型說謊**（宣稱搜尋跑了、沒命中），模型照著假前提推理 | 實作 6、驗收 4 |
| **索引錯配** | 指定的 embedding 模型與既有資料對不上時，搜尋**永遠回空、零筆日誌** | 實作 5、驗收 4 |
| **CSRF＋starlette 1.3.1** | Host 標頭可以把請求推過 CSRF 檢查（實測翻掉一個 `is_approved`） | 實作 4、雙票＋3 次驗收 |
| **拆掉遮罩** | 擁有者裁定移除；`warn`／`block` 留著，預設 `warn` | 實作 3、驗收 1 |
| **憑證偵測＋精度修正** | 抓 API 金鑰／權杖／私鑰／密碼（**只接警告**）；四條舊規則加檢查碼、Luhn、分隔符去換行 | 實作 3、驗收 2 |

最終：**csp 2150 過、anila-shell 621 過＋突變 48/48、anila-studio 610 過、匯入工作者 158 過、anilalm 25 過、治理中心映像建得起來。**

### 憑證那包的數字（值得單獨看）

| | 改前 | 改後 |
|---|---|---|
| 院內樣本誤報 | **10 個命中** | **0 個** |
| 真陽性 | 7 | **7 全保留** |
| 憑證樣本 | 0 | **全命中，0 個可阻擋** |

被修掉的誤報包含 `react-dom@18.3.1`（被當成 Email）與整欄預算數字（被當成信用卡）——
**在一個工程單位裡，這兩種每天都在貼。**

---

## 二、今天最該記住的一件事

**六包裡沒有一包第一次驗收就過。** 而且被打回的理由高度集中：

| 包 | 它自己長出來的東西 |
|---|---|
| PII | 關住了門，**門外沒有出口**（提示指向一個當下不存在的控制項） |
| RAG | 守衛的註解宣稱「不可能」，驗收讓它發生了**三次** |
| 索引錯配 | 印了**兩條走不通**的自救方法（重新上傳會被唯一鍵擋掉、重新索引的功能不存在） |
| 健康總覽 | 把間隔改成 31 秒，**反而會把正在工作的服務畫成紅色** |
| 拆遮罩 | 文案斷言「**有**個資」，而它自己量到六成是誤報 |
| CSRF | 說明宣稱「永遠不會放行」，實測**會放行** |
| 憑證偵測 | **註解裡寫下規則，下一行沒有遵守**（「短前綴要嘛綁死長度、要嘛不收」，而 `sk-` 正是三字元短前綴配寬鬆下限） |

**七包全中，而且全部是驗收抓的。** 全部來自驗收單裡的同一句話：

> **Look for the shape this package exists to eliminate, in the package's own work.**

📌 **這句話寫進每一張驗收單。** 它的命中率比逐條檢查驗收條件還高。

📌 **為什麼會這樣（我的解釋，不是實測）**：實作者寫註解的時候，是在**描述他打算做的事**，
而不是他做完的事。那兩件事之間的落差，就是這一整天所有缺陷的所在。
所以**註解與斷言是最該被當成待驗宣稱的東西**，不是最可信的東西。

---

## 三、擁有者的五個裁決（`docs/OWNER-QUESTIONS.md` 現在零待答）

| # | 裁決 | 要記住的前提 |
|---|---|---|
| **Q32** | **遮罩整個拆掉** | ⚠ 未來重開要先問兩件：①偵測器精度是硬前提 ②**院內有沒有「識別碼不得進模型」的硬性規範**——如果有，正解是**伺服器端稽核**（留證據、不改資料），不是客戶端改寫 |
| **Q33** | 升密對衍生記憶**先不做** | UI 不准暗示已回收 |
| **Q34** | 空白表單**不做檔名索引** | 擁有者補的關鍵事實：那些表單多半是**附在規範最後的填寫範例**，整份文件有文字、索引得進去 |
| **Q35** | 自動標題**不接快模型** | Open WebUI 0.11.0 的 `TASK_MODEL` 正是這顆按鈕，而且他們正在分裂成第三顆設定——**不要抄** |
| **Q36** | 憑證偵測**加，只接警告** | 不得接到 `block`；**不做泛用高亂度偵測**（會對任何 base64／十六進位響，也就是對貼程式碼的人一直響） |

**擁有者對這個功能的模型，原話：**
> 「其實我認為應該是偵測 > 警示使用者不要輕易交出，就像我貼 apikey 給你一樣。」

所以文案現在是「**疑似**有 1 個身分證（只比對格式，可能認錯）。送出後，模型和這份對話紀錄都會留著它，收不回來。」——**說了抓到什麼、說了代價，然後停住**。沒有「請先移除」、沒有「建議」。

---

## 四、每一包留下什麼要長期照顧的

`PLAN.md` 現在把這個列為**收貨條件**（擁有者 08-05：「要新增也可以，但重點是系統不要越來越複雜」）。

**健康總覽** — 四個「假設住在兩個地方，只有一個被測試看著」：arq 的健康鍵字串（釘的是我們的漂移，不是 arq 的）；TTL 與阻塞表是**同一個保證**，而**沒有東西守著那張表**（它是人工稽核，會腐）；「容器停掉立刻轉紅」**完全建立在 docker DNS 不解析未執行容器**這個環境事實上（換 k8s 就靜默降級）；`PDF_OCR_FALLBACK` 一個旗標把餘裕從約 40 倍砍到約 2 倍。

**CSRF** — 🔴 **最重要的一條**：出貨的版本上 `request.url.path` **就是** `scope["path"]`，所以這裡每一條靠 Host 的測試在語意上都是空的。**撐住六個安全判斷可觀測性的，是一個刻意做作的公開屬性替換。** 哪天有人覺得那些夾具很假而刪掉，六條線安靜地回到沒有測試，而**只有把 starlette 降版才會發現**。（`routed_path()` 的 docstring 就是講這件事的，它現在是承重的。）

**RAG** — 文案守衛只擋**原樣貼回**，不擋說謊；三個已知繞法有名字、有測試釘住。anilalm **沒有 CI**。設定檔拆成兩份，vitest 是**取代**不是合併 vite config。

**索引錯配** — PG-only 的測試套件**沒有連線字串就整批跳過**，所以那段 SQL 在預設執行裡是隱形的；大小寫比對是**資料缺陷的權宜之計**，而缺陷還在（#56 九條清單）。

**PII／拆遮罩** — 🔴 **隱私分頁那兩顆按鈕不可以改成條件渲染**。那是全平台唯一能改這個長期設定的入口；改成有條件顯示，等於把使用者鎖在門外（08-05 實際發生過一次）。`ui_settings` 是**整包取代**，寫第三個 key 會在下次存檔被洗掉（今日無實害，只有兩個 key）。

**憑證偵測** — 🔴 **`BLOCKING_FAMILIES` 是那個檔案裡最扛事的一行。**
所有規則都會衰退，而且是兩個方向各自衰退：

- **負空間判斷**（案號不會剛好帶合法檢查碼、預算欄是 tab 貼的不是空格、沒有人在 `password:` 後面寫八個無意義英數）——
  **每一條今天成立，都只是因為現在的表單剛好長那樣。表單一改就不成立，而且不會大聲壞掉**，
  它會一次一個樣本地重新喊狼，直到那條橫幅變成壁紙。
- **廠商前綴**（`sk-`／`ghp_`／`AKIA`）沒有穩定性保證 →
  **這份清單會安靜地對新的金鑰格式失明，而且看起來跟今天一樣健康。**

**兩種衰退之所以可以承受，唯一的理由是「憑證不能擋人」**——代價是一條白跳的橫幅，
不是一則送不出去的訊息。**所以未來任何把 `BLOCKING_FAMILIES` 放寬的改動，
要用改送出路徑同等的規格去審。**
`credentialDetect.test.jsx` 的語料是這些假設**唯一被寫成可檢查事實**的地方——
誰改了表單、文件編號規則或廠商金鑰格式，**動正規式之前先把樣本加進去**。

⚠ 已知殘留（實測、不打算修）：空格分隔的四欄數字若**開頭是 2–6 而且剛好通過 Luhn**，
約十分之一仍會誤報；tab 與換行兩種形式已關。純字母、七字以內的密碼偵測不到。

---

## 五、環境事實與踩過的坑（今天新增）

- **匯入的阻塞點有八個，跨兩個檔案，三個在逐份文件的迴圈裡**。`reresolve_collection_relations` 可以佔住 arq 的 poll loop **幾十分鐘**。那張表是**人工稽核、會腐**，而且第八個位置是複審才發現第一次漏掉的。
- **實測阻塞時間**：400 頁 33–89 秒、1000 頁 81–103 秒（離散來自主機負載）。TTL 是 3601 秒。
- **`asyncio.to_thread` 取消不掉**，而 arq 逾時會重試 → 同一份文件可能在兩條執行緒裡同時解析。（所以 offload 最後**沒有採用**。）
- **突變檢查不能跟任何會動到同一棵樹的東西並行跑**（今天踩過，競態留下未還原的突變，紅燈看起來像真迴歸）。
- ⚠ **有 agent 在容器裡跑 `env | grep -i vision`，把一把活的 API key 印進自己的紀錄檔**。沒進 repo、值沒有被複製。**內網不要用這個 grep 形狀，也不要寫進 runbook。**

---

## 六、接手的下一步

1. **第 4 段剩的那半：csp 映像沒有 `USER`。** 偵察已完成——⚠ **有兩個 Dockerfile，而帶 `USER csp` 的那個是死的**（compose 建的是 `infra/docker/csp.Dockerfile`，實測 `uid=0`）。修的時候會踩到兩件：`/app/logs` 的 RotatingFileHandler 是 root 所有（降權即開不起來）、`share/uploads/ingestion` 掛載內容已經是 `root:root`（只加 `USER` 會得到**容器全綠、上傳靜默 500**），**要有一次性 `chown` 的部署步驟**。
2. **`PLAN.md` 的〈第 8 段之前必須關掉的〉六件。**
3. 然後照九段往下：檢視套件版本 → wheelhouse → 重掃 → 重建打包 → 使用者手冊。

### 派工要寫死的（沿用 08-05，今天再驗證一次全部有效）

1. **破壞性指令用絕對路徑**（cwd 每次工具呼叫之間會重設）。
2. **`pkill` 樣式要精確**。
3. **禁用 `--no-verify`。**
4. **worktree 先建再派**，存在的時候先看裡面有什麼。
5. **不要把「打算做的事」記成「已經做的事」。**
6. 🆕 **agent 對「它沒改的那部分程式碼」的斷言，不可以直接記成事實。** 今天犯兩次：一次把「409 會弄壞兩個內部呼叫端」記進待辦（AST 掃過發現五個呼叫端全都有處理），一次把作者拒絕段邊界比對的理由轉述給驗收並背書（實測 14 個候選裡 8 個會被擋掉，包括作者自己舉的例子）。**收貨要把報告切成「我改的東西」（有驗證）與「我看到的別的東西」（線索）。**
7. 🆕 **驗收單一定要有一個「不該被擋」的對照案例。** 今天 PII 閘門的 `detectPII` 忘了 import，於是**每一次送出都靜默失敗**——所有「該被擋」的測試照樣綠，**只有那條對照轉紅**。

---

## 七、狀態快照

```
主線 restart/from-redesign  = 七包全合，領先遠端 70 commit，未推
csp             2150 passed · 24 skipped · 0 failed
anila-shell      621 passed · 突變 48/48（既有套件 11/48）· build 0
anila-studio     610 passed（3 紅為 base 既有）
ingestion-worker 158 passed
anilalm           25 passed（今天才第一次有測試執行器）
治理中心 UI       89 tests / 87 pass（2 紅同 base）+ 映像建置成功
裁決冊            零待答
```

⚠ **映像沒有重建。** 這六包動到 **csp、anila-ui、anilalm、asr-gateway、anila-core、ingestion-worker、治理中心**。
**合併完不等於部署完**——尤其 **CSRF 那個洞在跑著的容器裡還是開的**，直到 csp／flux2-dev／flux2-dev-agent 被重建為止。
