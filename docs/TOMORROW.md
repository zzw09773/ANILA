# 給你的清單（2026-08-06 收工）

> 技術細節在 `docs/HANDOFF-2026-08-06.md`。這份只講你需要知道和需要決定的。

---

## 一、今天做完的：第 3 段全部，第 4 段一半

**六包，全部經過獨立驗收，全部合進主線。**

| | 一句話 |
|---|---|
| 健康總覽 | 路由是**假綠燈**、匯入工作者是**永久假紅燈**（整個總覽被它拖紅）、nginx 那格是**當天自己打壞的** |
| PII 誠實化 | 設定頁說「CSP 會遮罩」是假的；「阻擋」其實**擋不住四條路** |
| RAG 靜默退化 | 檢索失敗時**對模型說謊**，然後模型照著假前提回答 |
| 索引錯配 | 換 embedding 模型之後，搜尋**永遠回空、一行日誌都沒有** |
| CSRF | 帶一個特製的 Host 標頭就能繞過檢查——**實測翻掉了一個帳號的核准狀態** |
| 拆掉遮罩 | 你裁的 |

最終：**csp 2150 過、anila-shell 557 過、studio 610 過、worker 158 過、anilalm 25 過**，治理中心映像建得起來。

**主線領先遠端 68 個 commit，我沒有推。**

---

## 二、今天最值得你知道的一件事

**六包裡，沒有一包第一次驗收就過。**

而且理由幾乎都一樣：**修「假訊號」的包，自己長出了假訊號。**

- PII 那包**關住了門，門外沒有出口**
- 索引那包印了**兩條走不通**的自救方法
- 健康那包修完會**把正在工作的服務畫成紅色**
- CSRF 那包的說明宣稱「永遠不會放行」，**實測會放行**

**六包全中，而且全部是驗收抓的。** 我把那句話寫進 `CLAUDE.md` 了，它的命中率比逐條檢查驗收條件還高。

---

## 三、你今天的五個裁決都結案了

裁決冊**零待答**。Q32（拆遮罩）、Q33（升密記憶先不做）、Q34（空白表單不做檔名）、Q35（標題不接快模型）、Q36（憑證偵測只接警告）。

**Q32 未來要重開的話，先問一件事**：院內有沒有「識別碼不得進模型」的**硬性規範**？
如果有，正解不是我們原本要做的那個——**是伺服器端稽核（留證據、不改資料）**，不是在瀏覽器改寫使用者打的字。這句話值得帶去跟主管談。

---

## 四、你那個 API key 的例子，現在有答案了

你說「就像我貼 apikey 給你一樣」。實測結果：**這個偵測器對那把金鑰一個字都不會說。**

四種憑證形式（API key、bearer token、私鑰、密碼）**全部靜默**。而它會誤判的東西是：採購案號、預算欄、年份列表、表格裡的直欄數字、公文裡的 email。

**十條合理的院內文本，十條全部誤中。**

所以今天在做兩件事：把那四條規則的精度修對（檢查碼、Luhn、分隔符），以及照你的裁決加憑證偵測、**只接警告**。

---

## 五、我需要你做的：目前沒有

裁決冊清空了。下一件是**第 4 段剩的那半**（csp 映像用 root 跑），我自己來。

---

## 六、上線前還沒關的（我加進 `PLAN.md` 了）

這六件都是這兩段**量出來**的，不在原本的九段裡。**沒關掉就重建映像，等於把它們封進交付包。**

| | 事情 |
|---|---|
| **匯入** | 大文件的 embedding 請求**不分批**——整份塞一個請求，而後端共用約 35 秒預算 → **任何大文件都會整份失敗** |
| **匯入** | **DOCX 的表格全部被搬到文件最後**——「如下表：」底下沒有表，兩張不相干的表黏在一起。**每一份有表格的規範都中，現在就在發生** |
| **匯入** | 掃描 PDF 的 **OCR 永遠不觸發**（圖片佔位符自己撐過門檻）。現在旗標關著所以沒事，**但誰打開都會以為生效了** |
| **資料** | embedding 模型名稱的**大小寫碰撞**——每建一個新知識庫就再犯一次 |
| **設定** | `ALLOWED_HOSTS` 沒設，等於少一層防護 |
| **部署** | 匯入工作者要 **recreate 不是 restart** 才吃得到新的健康偵測 |

---

## 七、只有你做得到的四件（沒變）

| | |
|---|---|
| **FQDN 向網管申請** | PLAN 明寫是前置，網管有等待期 |
| **`.15` 的備份排程手動裝** | 程式和演練都好了，部署路徑裡沒有裝 cron 的步驟 |
| **卡登完整走查** | 建帳號→選部門→等核准→核准→登入，**從來沒真的走過** |
| **SMTP 位址** | P3.2 只差寄送 |

---

## 八、一件安全上的小事

有一個工作在容器裡跑了 `env | grep -i vision`，**把一把還活著的 API key 印進它自己的紀錄檔**。沒有進 repo、沒有進 commit、值沒有被複製到別的地方，而且那是本機這套的值。

本機還沒對外，風險很低，**要不要輪換你決定**。內網的值一定不一樣，而且**絕對不要用同樣的方式去看**。

---

## 九、最後一句

⚠ **映像沒有重建。** 六包動到七個服務的原始碼。
**合併完不等於部署完**——特別是 **CSRF 那個洞，在跑著的容器裡還是開的**，要等重建映像才會關上。

---

## 十、收工後補的（08-06 晚）：第 4 段整段關板，加一件要你裁的

**第八包做完了**：csp、匯入工作者、pptx-renderer 三個原本用 root 跑的映像，現在都是釘死的非 root 使用者，而且四條「降權後會靜默壞掉」的路（日誌、上傳、附件、簽章私鑰）每條都先證明會壞、再證明修好。部署腳本會自動做一次性的擁有權對齊，重跑無害。三輪實作、三輪驗收，那句咒語又中三次——**八包八中**。

**要你裁的（Q37）**：查這一包的時候發現，**08-03 那份交付包裡的 csp 映像，烤著一把測試用的 RSA 私鑰和大約 25MB 的本機開發日誌**。原因是舊的 `.dockerignore` 只擋最外層的 `secrets/`，這次已經整類修掉。那把鑰匙不是正式環境在用的（正式的那把是掛載進去的，不在映像裡），日誌是本機這套的。但**那份包是準備手提進內網的**——我的建議：第 8 段重建出新包之後，舊的 tar **銷毀**，不要讓有鑰匙的版本進氣隙。細節在 `docs/OWNER-QUESTIONS.md` Q37，我先照最保守假設做（舊包原封不動放著、什麼都不刪），等你拍板。
