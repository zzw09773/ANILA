# 交班 —— 2026-07-31 白天

> **兩分鐘讀完。** 早上那份是 `HANDOFF-2026-07-31.md`(凌晨那一輪)。
> 等你裁決的事在 `docs/OWNER-QUESTIONS.md`,**16 題,11 題已答**。

---

## 一句話

**你交辦的 11 件,10 件做完並部署;順路挖到五件比原題目更嚴重的,也全部修掉了。**
alembic 到 `r1_0027`、15 個容器全綠、五個入口都通、41 個 commit 已推。

**測試從「26 個紅燈大家繞過去」變成 1366 passed / 0 failed。**

---

## 你交辦的 11 件

| 你說的 | 真正的原因 |
|---|---|
| 新增部門沒有三層 | 後端一直支援任意層數。**新增部門的表單根本沒有「上層單位」欄位**,所以你建的每個部門都是根節點 |
| 使用者回饋要能匯出 | 做好了,匯出你畫面上篩選的那批,不含對話正文 |
| 第一次點建議按鈕沒送出 | **它有送出。是畫面自己把它擦掉了** —— 建立對話後去跟後端要訊息,拿到空清單就整個覆蓋 |
| 第一句話跑出分支 | 上一條害的(你點兩次,資料庫真的有兩則)。外加一個獨立錯誤:「刪除此訊息分支」的顯示條件寫成「第一則以外的每一則」 |
| 移除產出中心 | 它跟「我的知識庫」是逐字元相同的連結 |
| 沒有麥克風 | **麥克風早就寫好了,兩邊都有。** 語音服務沒開;開了之後認證又壞 —— gateway 檢查的是這個平台從來沒有簽發過的欄位 |
| Q5 稽核帳 | 照你的裁決做了日級鏈＋錨進匯出。維運者每月要做的事:**零** |
| Q7 派工夠不夠好 | 見下 |
| Q8 anila-core | 兩條都合了。core-opt 先修掉兩個壞預設,再過一輪跨家審查 |
| Q9 同事名單 | 開了,連「接受之後真的移交對話」也做了(那個 API 之前是空殼) |
| Q11 備份路徑 | 改成家目錄,實測免 sudo、1.3 MB、不到一秒 |

**Q7 的答案:它不是好或不好,它從來沒真正跑過。** `agents` 表 0 筆,router 每則訊息花十秒推導出唯一可能的結論。原因見下一段。

---

## 五件你沒問、但更重要的

1. **改一次密碼,那個帳號就再也不能用 Studio 與語音。** 撤銷權杖的比較方向寫反了,新發的權杖剛好符合「已撤銷」。csp 自己的登入還是好的,所以症狀是「登入了但半個平台不認我」。admin 帳號昨天起就是這個狀態。

2. **文件教開發者掛 agent 的那個指令,一定會失敗。** CLI 送模型**名稱**、後端要模型 **id**,兩邊從來沒對過。這很可能就是「零個 agent」的原因 —— 而零個 agent 又讓 router 每則訊息白付一次判斷。修好之後這個平台掛上了**史上第一個 agent**,router 也做出了第一次派工決策。

3. **還原備份會弄壞平台,而且是「看起來沒事」的那種。** 擁有權被壓平 → 開機 `/health` 回 200、容器顯示健康、**沒有人登得進來**。已用真的 dump／真的還原演練修好並驗過。

4. **入口層 502 的真正病因還在。** 前一次的修正治的是另一個病因。今天改完之後當場證明:所有後端被重建、**nginx 完全沒動**,五個入口照樣全通。

5. **測試套件本身不可信。** 「26 個紅燈」是錯的數字(同一份程式碼,換個目錄就變 14 或 27)。更嚴重的是:**卡片登入是全院唯一入口,而它整條流程沒有任何綠燈守著**,還有一支測試是「空綠」——它斷言 401 之後又是 401,pending 那段程式碼一行都沒執行。現在建帳號、email 衝突、pending、選部門、核准解鎖這五個行為,每一個被弄壞都會有測試變紅。

---

## 等你裁決(五題,都不擋開發)

| | 事情 | 我的建議 |
|---|---|---|
| **Q12** | GPU 沒接進 Docker,語音只能跑 CPU 小模型(中文會把「語音」聽成「與因」) | 本機就這樣,品質等上 `.15` 用 GPU 再看 |
| **Q13** | 登入頁想顯示公告,但公告 API 要登入 —— 而登入頁的人沒有權杖 | 另開一支只回「登入頁可見」公告的端點 |
| **Q14** | 同一個記憶在兩個對話各學過一次,升密舊的那個刪不到 | 上線後再說,管理頁的真刪已經可用 |
| **Q15** | 本機卡登入是關的 | 找一天開起來把整條流程真的走一次 |
| **Q16** | 下載的範本叫 `anila-core-template`,裡面裝的是 `anila-agent` | 改名字(每個獨立來源都說內容是 anila-agent) |

---

## 還沒做

- **P2.1** agent 身分改短效簽章 JWT(大,且是計費正確的前提)
- **P2.6** 源碼掃描
- **P3.2 寄送**(等 SMTP)、**平台無回應**需要外部看門狗
- **origin 邊界撿回** —— 會改所有知識庫 API 的網址,**需要你在旁邊看畫面**
- **1.6 前綴快取**、**agent latch 活體格**、**P4.6 活體抓取** —— 都要進內網
- **P5.1 給三千人的操作手冊**、P5.5 部署到 `.15`

---

## 明天第一件事

**打開來用眼睛看一遍。** 特別是這三個,只有你判斷得了:

1. **新分頁開 ANILA,點一次建議按鈕** —— 那條只在單元層驗過,真正的確認要你的卡。
2. **治理中心建一個三層部門**,把人綁在「所」那一層看看順不順。
3. **麥克風**。它現在會出現而且能用,但 `small` 模型的中文會有同音錯字。

---

## 我今天犯的錯

1. **交班文件裡有兩條不實** —— 說 P4 全關、說 502 修好了。兩條都是**看到有一包叫那個名字就推論它做了名字說的事**,沒回去看它實際改了什麼。判準已改寫:宣稱關閉要指得出**行為改變**的證據。
2. **Q7 又錯一次** —— 說「直答不能串流」。我引的是非串流路徑的註解,而前端走的是另一條。同一題連錯兩次,兩次都是**讀了註解就當成系統行為**。
3. **派工寫錯規則** —— 記憶收斂我寫成 per-application,你的原話是「同一個對話框」。執行的 agent 照你的原話做,沒有照我的。
4. **merge 又在 worktree 裡跑了一次**,得到假的「已經是最新的」。這條我自己記過。
