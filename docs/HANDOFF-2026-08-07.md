# 交接 — 2026-08-07（接手先讀這份）

> 上一份是 `docs/HANDOFF-2026-08-06.md`（那份的 §八～§十仍然有效，本份不重複）。
> **一句話狀態**：〈第 8 段前必關〉**工程面全部關完**；新功能
> （ANILA 直接檢索院內規章）**十個任務全部做完、全部審查判乾淨**
> （第十個經一輪修訂後判乾淨；最終全分支審查＝fable＋kimi-k3 **雙票**，
> 唯二 Important 已於單一修訂波關閉並經 scoped 複審）。
> 分支 `wt/institutional-kb` **已於 08-08 合併回 `restart/from-redesign`**（b5266fda）；
> **映像尚未重建**（拋棄式 kbcheck-\*:wt 驗過建得起來）。
> **主線未推。**

---

## ⓪ 先知道三件

**一、這份交接是因為 session 要換，不是因為出事。** 舊 session 的對話紀錄裡有一個
**孤兒 `server_tool_use` 區塊**（第 2023 行，`advisor`，`srvtoolu_011DNRb…`，沒有配對結果），
起因是一次被中斷的 advisor 呼叫。每次請求重播紀錄都會碰到它，於是
`Unsupported content type: server_tool_use` 一直冒出來。**工作完全沒有損失**——
全部在 git 與帳本裡。⚠ **不要試圖手動編輯那個 jsonl**。

**二、功能開發用的是 SDD 流程（subagent-driven development），進度在帳本裡，不在對話裡。**
帳本：`wt-institutional-kb/.superpowers/sdd/2026-08-07-anila-institutional-kb/progress.md`
它記著每個任務的 commit 範圍、審查結果、**以及所有裁決與延後項目**。
接手時**先讀帳本再讀這份**——帳本是恢復點，對話不是。

**三、⚠ 這個分支有兩支 migration（`r1_0033`、`r1_0034`），而 `origin/main` 上也有 r1_0033。**
兩條線在 r1_0030 之後已各走各的（r1_0030–0032 早就重疊）。**本分支照本地線編號，不要為了避開 main 跳號。**
下一個編號是 **`r1_0035`**。

---

## 一、今天上午做完並已合併主線的（承接 08-06 那批）

〈第 8 段之前必須關掉的〉八件**工程面全部關完**，那張表現在只剩擁有者已裁決的兩列。
細節在 `PLAN.md` 與 `docs/HANDOFF-2026-08-06.md` §十，這裡只留接手必知：

- **引用頁碼修好了**，但 ⚠ **已匯入的文件頁碼仍是錯的**，而且 chunk 邊界與向量也錯
  → 只能重新匯入，不能改號。擁有者已裁決：**內網還沒匯入文件，所以不用補救**。
- **Q37 已執行**：08-03 交付包的 csp tar（內含測試私鑰）已刪，同目錄留 `00-這包不能用了.md`。
  ⚠ `02-weights/`（7.7 GB）**沒有動**。
- **Q38 已裁決：兩者都豁免**（codeserver 的 httpolyglot 測試鑰是公開 npm 套件的夾具；
  asr 的 `bootstrap.log` 是 Canonical 2024-10-16 建 base 的紀錄，逐行查過零敏感）。
- ⚠ **在這台機器上建交付映像是擲骰子**：本機 Symantec DCS 會在 RUN 執行當下把自己的目錄
  注入容器可寫層，**約六分之一的建置會產出 `docker save` 不出去的映像**。修法已進去，
  匯出前也會先驗 `docker save`。**第 8 段重建時匯出失敗，先重建一次再說。**

### 上午另外做的一次本機部署（已完成）

15 容器全綠、五個入口通、非 root 生效（csp/worker/asr 是 uid 10001、pptx 是 1000）、
JWKS 200（證明擁有權對齊有效）、`r1_0032` 對本機真實 DB 跑過（四張表 0 列改寫、無殘留未修列）。

⚠ **`deploy-prod.sh` 在這條分支上跑不起來**——它第一關檢查 git 分支必須是三條舊 prod 分支之一，
而那個模型早就進 attic。上午是**手動照它的順序**做的：
`fix-runtime-ownership.sh` → `docker compose -p anila-restart -f compose.yaml -f infra/compose/asr-cpu.yml --profile asr build` → `up -d` → nginx 重載。
**這件事要修**（分支閘門過時），否則那支腳本裡的擁有權對齊步驟等於沒人跑得動。

---

## 二、正在做的功能：ANILA 直接檢索院內規章

### 為什麼做

`SYSTEM-MAP.md` §3 的主線圖早就寫著 Router 判斷「不需要 agent」時要「用 general 知識庫
＋模型直接回答」——**這條路從來沒實作**。擁有者的原話：ANILALM 是使用者放自己的文檔，
ANILA 是智慧代理平台，**院內規章不該每一份都養一個 agent**。

### 權威文件（都已 commit）

| | 路徑 |
|---|---|
| 設計 | `docs/superpowers/specs/2026-08-07-anila-institutional-kb-design.md` |
| 實作計畫 | `docs/superpowers/plans/2026-08-07-anila-institutional-kb.md` |
| 進度帳本 | `wt-institutional-kb/.superpowers/sdd/2026-08-07-anila-institutional-kb/progress.md` |

### 擁有者的裁決（全部已定，設計文件 §2 有完整清單）

多個庫可標記（**規格變更**：SYSTEM-MAP §5 原寫「一個」）／只有**被選中的無機密**
（無機密是必要條件不是觸發條件）／~~Router 判斷再搜~~ **改為：檢索照門檻走，掛在答案通道**
（Q39，見下方那一節）／搜不到要說但不阻止回答／
引用**不顯示頁碼**（規章的定位點是第幾條）／已標記的庫必須同一顆嵌入模型／
Router 沒搜時不顯示任何標記／門檻用**設定**不用常數／原文泡泡＋信心分數／
密等閘門用**文件欄位**，OCR 直覺改做上傳時警示（另一件事）。

### 進度：10/10 完成，全部判乾淨（Tasks 1–9 逐一審查；Task 10 一輪修訂後判乾淨；
### 最終全分支審查 fable＋kimi 雙票 With fixes → 修訂波已關閉並複審通過）

| Task | 狀態 | commit 範圍 |
|---|---|---|
| 1 資料模型＋`r1_0033`（欄位＋CHECK） | ✅ | `23100b21..a1cfc5da` |
| 2 標記端點＋四道前置檢查 | ✅ | `a1cfc5da..e4e05a8e` |
| 3 檢索服務 `institutional_kb.py` | ✅ | `e4e05a8e..5724d50e` |
| 4 門檻設定＋校準（⚠ **只出後端端點，沒有畫面**，見 §七第 8 點）＋`r1_0034` | ✅ | `0268c5e3..6666fbc8` |
| 5 Router 直答訊號 | ✅ | `016d8dd0..212d95b1` |
| 6 CSP 注入與五狀態 payload | ✅ | `212d95b1..31e804e8` |
| 7 治理中心標記 toggle | ✅ | `31e804e8..3eb62811` |
| 8 前端五狀態徽章與原文泡泡 | ✅ | `3eb62811..f9c4f088` |
| 9 「改用院內規章重查」 | ✅ | `f9c4f088..e9afff43` |
| 10 文件與待辦收尾 | ✅ | 本次 commit |
| 最終全分支審查（最強模型） | ⬜ | |

⚠ **表裡的範圍照帳本原樣抄**（`progress.md`）。Task 4 的起點 `0268c5e3` 不接在 Task 3 的終點上，
帳本就是這樣記的，**不要「修正」它**；另有三段範圍內含與該包檔案不相交的控制端文件 commit
（Task 5 的 `4f306754`、Task 7 的 `2cf40135`、Task 8 的 `4daba708`），帳本都標明了。

⚠ **Tasks 6–9 的計畫段落原本是壓縮的**（只有標題與步驟名）——已照 Tasks 4、5 的做法在派工前逐一展開並 commit。

### ✅ Task 6 之前那件必須裁的事：已裁（Q39），且原因值得留著

**Router 的 LLM 呼叫發生在路由決策「之前」。** `router_server.py:1170` 呼叫 LLM →
`:1195` 取出文字 → `:1196` **才從那段文字裡解析出要不要派工**。
**只有一次呼叫，而那次呼叫的產出就是答案。**

所以 CSP 看到那通呼叫時，**router 自己都還不知道要不要派 agent**——
原設計「CSP 收到『直答』訊號才加規章檢索」**在時序上做不到**。
Task 5 出貨的那個 header 標的是 **router 的答案通道**，不是一個判定結果
（語意以 `task-5-report.md` §1 為準，**不是計畫的 Interfaces 那行**）。

**擁有者 08-07 裁決 = (a)，記為 `docs/OWNER-QUESTIONS.md` Q39：**

| | 做法 | 代價 | 裁決 |
|---|---|---|---|
| **(a)** | 照樣在那通呼叫注入 ＝ **答案通道上每則都檢索** | 連「你好」都付一次 embedding＋向量搜尋 | ✅ **採用**；改由 **Task 4 的分數門檻**擋離題回合 |
| **(b)** | router 判定直答後**再發一次「有依據」的呼叫** | 每個直答多一趟完整 LLM 往返，第一次的產出被丟掉 | ❌ |
| **(c)** | 檢索移進 router（＝設計的做法 B） | router 需要一把跨庫讀取憑證——那正是當初選 A 的理由 | ❌（08-07 已否決過） |

**設計裁決 3「Router 判斷再搜」因此改寫成「檢索照門檻走，掛在答案通道」**
（設計文件 §2 有原句與修訂並列）。接受的代價：**最後決定派工的回合白做一次檢索**（數十毫秒）。

**另一件 Task 9 之前要裁的也裁了 —— Q40：`forced` **會**同時抑制派工。**
按下「改用院內規章重查」保證那一輪由直答路徑回答並帶規章檢索，**永遠不會被交給 agent**。
Task 9 實作時發現要守的不是三道門是**四道**（串流機器有 mid-stream、end-of-stream 最終解析，
再加一條**繞過 `_parse_dispatch` 的無查詢救援路徑**）。

---

## 三、接手怎麼繼續（照這個順序）

1. **讀帳本**（不是讀對話）：`progress.md` 的最後幾行就是斷點。
   有 `Task <N>: complete` 的別重派；最後一行是 fix round 的表示卡在迴圈中。
2. ~~確認 Task 5 的 agent 還在不在~~ **已無斷點**：十個任務都 `complete`，
   `git status` 應該是乾淨的。**現在的斷點是最終全分支審查**（第 4 點）。
3. **流程**（十個任務已經跑完，這條留給下一個功能）：
   每個任務 = 派 coder → 產生審查包（`scripts/review-package PLAN BASE HEAD`）→
   派 verifier（規格＋品質**兩個判定都要**）→ 有發現進修訂輪（最多五輪）→ 寫帳本 → 下一個。
   腳本在 `~/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/subagent-driven-development/scripts/`。
4. **十個任務跑完後**：最終全分支審查（**最強模型**），然後 `superpowers:finishing-a-development-branch`。

### 派工前必做的一件事（這個計畫吃過三次虧）

**驗一遍計畫裡引用的東西是否真的存在。** 已經抓到三次：
補救訊息指向一個**這個平台沒有的功能**（重新嵌入）、函式簽章少了計量必要的參數、
程式碼草稿用了兩個沒定義的變數。派工單要明寫：
**「把計畫的程式碼當意圖草稿，不是抄寫來源；跟真實程式碼衝突時以真實程式碼為準。」**

### 常設要求（earned，別省）

- **任何碰到密等的測試必須跑過整個 `ClassificationLevel` 列舉**，不准只斷言「機密」。
  這個缺陷類**出現過兩次**（Task 1、Task 3）：只造一級的夾具會讓其他級數無人看守而測試全綠。
- **驗收要自己想一個實作者沒做過的突變。** 四個任務裡，實作者自列的突變**全部被殺**，
  而審查另外想的**活了四個**——而且活下來的都打在功能最核心的防線上。
- **驗收單必帶**：「Look for the shape this package exists to eliminate, in the package's own work.」

---

## 四、這個功能的硬約束（違反會靜默出事）

| | 約束 | 為什麼 |
|---|---|---|
| 1 | **逐庫檢索，絕不跨庫單一查詢** | `document_chunks` 的 RLS 靠 `SET LOCAL anila.collection_id` 放行，綁在 store 建構子裡。跨庫查詢會讓 DB **靜默**濾到只剩一庫，「搜了沒命中」就變成不報錯的謊 |
| 2 | **文件密等過濾只放在新模組** | agent 與 ANILALM 檢索機密內容是**合法的**；加進共用元件（`pgvector_store.py`）就是靜默弄壞別人 |
| 3 | **不准改 `_require_collection_access`** | 19 個呼叫點跨 9 個檔共用；放寬它等於同時放寬灌文件與刪除 |
| 4 | **不准改 `services/csp/app/services/institutional_kb.py`** | Task 3 已關板，blob 釘死 `02b0765825252618febe2b597f85b6a69ca46adb` |
| 5 | **絕不對活體 DB 跑 alembic** | 用丟棄式容器，按名字刪 |

---

## 五、已裁決的爭議（別重吵）

- **雙重宣告 CHECK**（ORM `__table_args__` ＋ migration，同名）**是這個 repo 的慣例**——
  6 例雙重 vs 3 例只寫 migration，而 `r1_0029` 的 origin check 自己的 docstring 寫著
  「是產品貨架分區，不是授權邊界」。Task 1 的審查曾主張相反並被判定錯誤。
- **`except IntegrityError` 裡的 `db.rollback()`** 是合理的防禦行、公開介面測不到，**保留**。
- **密等比對用嚴格相等**（fail-closed）是對的：漏放會外洩機密內容，過嚴只是少一筆命中。
  ⚠ 但那個排除是**靜默的**——現在可接受（檢索是盡力而為的脈絡），
  **哪天它變成對使用者的存取控制宣稱，這個靜默就是缺陷**。
- **門檻上界 1.0 兩端都接受**：0.0（全收）與 1.0（只收完全吻合）是同一個校準動作的兩端。

---

## 六、延後的小發現（最終審查要分診）

帳本裡有完整清單，這裡只列會咬人的：

- **門檻的四捨五入盲點**：測試裡流經 PUT 的值（0.0/0.5/1.0/0.99/0.55/0.3）**剛好都是常見失真變換的不動點**，
  所以「存進去的＝生效的」只在兩位小數穩定的值上成立。加一個 `0.375` 這種內插值就補起來。
  ⚠ **那個加固過的參數才是該被複製到 41 個設定的版本。**
- `is_kb_threshold_calibrated` 現在是**孤兒**（零呼叫點），覆蓋率從間接被釘變成零。
- `platform_setting_set` 這個稽核事件**沒有任何測試斷言**。
- `platform_settings` **沒有 CHECK/RLS**：繞過 API 寫壞值會退回預設＋記警告，
  **不適合當安全開關**。要放安全類設定進去之前必須重新設計。

---

## 七、這個功能之外、還開著的事

1. **`deploy-prod.sh` 的分支閘門過時**（見 §一），擁有權對齊步驟等於沒人跑得動。
2. **`prompt_gen_service` 401**：它只讀全域 gateway 金鑰、不看模型自己的憑證
   （`prompt_gen_service.py:115`），而正式代理路徑是「先找模型自己的、再退回全域」。
   **少了前半段，所以怎麼設定都繞不過去。**
3. **系統內部元件不該逐顆模型授權**（擁有者已裁決要做）：換嵌入模型就 403，
   而畫面上**沒有任何地方**能幫既有 key 加授權（後端 `PUT /api/keys/{id}` 支援，前端沒接）。
4. **計量圖兩條線同色**（`#2b4c7e` vs `#2b6cb0`，同色相只差亮度）。
5. **知識庫卡片「位元組 0」**顯示不實（有 3 份文件 330 區塊）。
6. ~~**設定總覽頁**（擁有者要的大工程）~~ **✅ 2026-08-09 已出貨**（Q41 當日裁決把它
   提前到打包前，且**範圍直接做到可編輯**，所以原句「建議先做唯讀總覽」沒有照辦）。
   原句還有一個數字要留著看：「光 csp 就 41 個環境變數」——**41 是 compose 鍵數，
   csp 實讀 95 顆**（登錄表 96 筆＝95 顆環境變數＋1 顆本來就在 DB 的別名），
   其中 **57 顆從未出現在 compose**、今天的管理員不知道它們存在。三分類則守住了，
   只是實作成五類（C／B-可編輯／B-鎖定／SEC／A）四區（見設計文件）。操作面看
   [`runbooks/settings-page.md`](./runbooks/settings-page.md)，
   長期照顧看本節末的〈設定頁留下什麼要長期照顧〉。
7. ~~**SYSTEM-MAP §5 要改**~~ **✅ Task 10 已改**：「general 知識庫**一個**」→ 可標記多個，
   §5 表格下方補上標記規則（admin、限無機密、個人庫不可、同一顆嵌入模型）。
8. 🆕 **設計 §7 的門檻校準畫面沒被任何 task 承載**——Task 4 只出後端（6 個檔全是 csp 側，
   治理中心零改動），所以**今天只能走 API 改門檻與跑 preview**。
   **上線前要嘛補畫面、要嘛把「怎麼用 API 校準」寫進運維手冊**。
   ⚠ 這一條要在內網量測清單被人拿去執行**之前**決定——那份清單不可以把操作者送到一個不存在的畫面。
   （這也是「設定頁第一塊磚」那個論述的缺口：後端模式證明了，前端樣板還沒有。）
9. 🆕 **多輪合成出口丟棄自己的 meta**（最終審查 kimi I-2＋M-a；**這是同一件事第三次掉下去，
   所以這次有名字**）。
   多輪迴圈的合成呼叫**是被標記的答案通道**（`router_server.py:1599-1605` 傳了
   答案通道 marker），CSP 真的檢索、真的把段落注入，模型可能照 `[N]` 引用——
   但 `_multi_turn_dispatch`（`:2273`）的回傳值不含那通呼叫的 meta，兩個出口
   （非串流 `:1624-1626`、串流 `:2233-2235`）都合併 `None`。結果是
   **有依據的答案無從查證**：畫面上沒有出處抽屜，讀者看到的引用編號指向空氣。
   更精確地說，`kb_state` 在 router 邊界是**欄位缺席**而不是明帶的 `not_searched`
   ——因為 router 自己的骨架 `_default_anila_meta`（`:576-591`）根本沒宣告這個欄位，
   所以「狀態一律明帶」這條硬規則事實上只守到 CSP 邊界（所有合併 `None` 的出口都一樣：
   `llm_error :1312`、route-miss 兩條、多輪合成兩條）。
   **方向是保守的**（該亮沒亮，不是不該亮卻亮），且緩解事實明確：
   `anila_multi_turn` 由 client 指定，**目前零 production 發送者**——所以列 follow-up 不列 must-fix。
   **修法**：`_multi_turn_dispatch` 回傳合成呼叫的 meta，兩個出口合併它（比照 Task 9 對 `:2098` 的修法），
   加一支釘；順手在 router 骨架也明帶 `kb_state`（預設 `not_searched`），把缺席補成明帶。
   ⚠ **它掉下去的三次**：Task 5 記為「Task 6 必須吸收」→ Task 6 是 CSP 側沒接 →
   Task 9 只釘了直答兩個出口。**兩個最終審查各自獨立指出同一處**。下一包收走它時先看這一段。
10. 🆕 **anila-core router 家族那 3 個既有紅燈，名字第一次寫下來**
   （帳本從 Task 5 到 Task 9 記了四次「same 3 pre-existing by name」，**卻從來沒寫出名字**）：
   `tests/test_router_resume_proxy.py::test_dispatch_pins_owning_agent`（順序相依）、
   `tests/test_router_runtime_contract.py::test_router_non_stream_includes_anila_meta`、
   `tests/test_router_runtime_contract.py::test_router_stream_emits_trace_and_meta_events`。
   ⚠ **但 `docs/HANDOFF-2026-08-06.md` 寫的是「anila-core 4 個既有紅」**，
   而本包從頭到尾量到的是穩定的 3——**3 與 4 的差異沒有人對過帳**。
   這條 follow-up 的**第一個動作就是對清 3 vs 4**（是 08-06 數錯、是不同的測試集合、
   還是有一支在這期間被修好或被跳過），對清之前不要拿任何一個數字當基準宣告「無回歸」。
   ⚠ 復現的完整形（兩個坑都踩過才湊齊）：`cd packages/anila-core &&`
   **`PYTHONPATH=src /home/c1147259/.cache/anila-core-venv/bin/python -m pytest …`**——
   缺 `PYTHONPATH=src` 是收集期 22 ERROR（0.3 秒全滅）；用系統 anaconda 是假紅 124 條（缺 `aiosqlite`）。

### 🆕 設定頁留下什麼要長期照顧（2026-08-09 收尾；軸線：維護成本）

上面第 6 點做完了，但它在系統裡留下八件**會持續要人照顧的東西**。
這一節不是缺陷清單，是**「以後每次動到設定，就會被這幾件事咬」**。

1. **登錄表是唯一宣告點：`services/csp/app/services/settings_registry.py`。**
   新增一顆設定＝**改一處**（key、型別、值域函式、預設、對應的 env 名、說明文字、類別）。
   **類別決定畫面**：顯示什麼、能不能編輯、鎖起來的理由要說什麼，全部從那一筆長出來。
   ⚠ 反過來說：**在別的地方多抄一份名單，就是多一個會漂開的真相**。
   端點自己不再抄一份可編輯類別，前端也不抄——這是刻意的。
2. **雙層優先序是 `DB > env > 程式預設`，而畫面的「來源」欄說的就是哪一層贏了。**
   值沒生效的時候**先看來源欄，不要先翻 compose**：來源說 `env` 就表示 DB 那層沒東西在贏，
   說 `db-boot` 就表示現在跑的是這次開機從 DB 載進來的值。除錯順序寫在
   [`runbooks/settings-page.md`](./runbooks/settings-page.md)。
3. **B-可編輯靠的是「開機覆蓋」，所以改了要重啟。**
   存進 DB 的值在**下一次開機**（DB 可達之後、開始服務之前）就地蓋掉 `Settings` 單例。
   ⚠ 覆蓋載入失敗時服務仍以 env 值開機，畫面最上面會出現
   **「這次開機沒有載入設定覆蓋」**——那一輪所有「重啟後生效」的承諾都落空，
   **絕不會被畫成已生效**。看到那條紅字先解決它，再談任何一顆設定。
4. 🆕 **env 那一層不套值域（Task 5 審查發現，具名 follow-up）。**
   compose 填一個超出值域的值，畫面會**照實顯示那個 env 值**（不夾限、不退回預設），
   而消費端若自己另有夾限或自己直讀 env，就會出現**「畫面 vs 實跑」分歧**。
   今天已知兩顆，而且**兩顆的成因不同**：
   - `queue.token_revocation_redis_timeout`：**消費端直接讀 env 並 `float()`**
     （`token_revocation_publisher.py` import 期就算成模組常數），填 999 就真的跑 999，
     而畫面說預設值。→ **Task 3 那種「改成走登錄表」的重接線候選**。
   - `alerts.check_interval`：從 compose 填 5 → 畫面顯示 5／來源 env，而迴圈實際跑 15
     （消費端 `alert_detectors.py:577` 的 `max(15, …)`）。**成因是 env 層跳過了
     `domain_fn`**（登錄表的下界已經在 2026-08-09 拉齊成 15，從畫面存是進不去 5 的）。
   👉 收這條 follow-up 的人：**兩件事都要做**（直讀 env 的消費端改走登錄表 ＋ env 層套上
   `domain_fn`），只做一件會留下另一半的分歧。
5. 🆕 **`ANILA_TRUSTED_HOSTS` 與 DB 表 `trusted_hosts` 是兩個來源，還沒收斂**
   （Task 1 起遞延，設計文件 §8 明列不做）。兩邊做的是同一件事，**改哪一邊會贏**
   要看讀取點——動到 SSRF 白名單之前先把這條收掉。
6. 🆕 **A 類（祕密）的 `updated_at`／`updated_by` 後端仍然會回。**
   值本身遮蔽得乾淨（`effective`／`stored`／`default` 一律 `None`），**metadata 沒有**。
   那不是值外洩；但「某顆祕密有一列被人動過、什麼時候、被誰」如果也算資訊，
   把它一起遮掉是一行的事。**留給擁有者裁。**
7. 🆕 **鎖定區 51 列（B-鎖定 26 ＋ SEC 25）沒有分頁也沒有搜尋。**
   這是**可用性**問題，不是誠實性問題（每一列都是對的，只是要捲很久）。
   Task 6 已裁定本輪不做；**擁有者若嫌難用，補搜尋框是獨立的小包。**
8. 🆕 **`infra/compose/dev.yml:121-123`（csp 的 `FLUX_*` 三顆）與 `:242`
   （router 的 `MODEL`）還留著已經退役的旋鈕。** Task 7 只清了
   `infra/compose/platform.yml` 與 `.env.example`（**那是它的 SCOPE**），
   dev.yml 是明白揭露的範圍外殘留。**待掃**：它們在 csp／router 都已經零讀取點，
   留著只會讓下一個人以為那顆還活著。
   ⚠ 別順手把 `FLUX_BACKEND_URL` 從 `.env.example:311` 或 `platform.yml:456`
   （anila-studio 那個服務區塊）一起刪——**那一顆對 anila-studio 是活的 toggle**
   （recon 的「死」是 csp 範圍內的說法，Task 7 已更正；csp 區塊 `platform.yml:215`
   留著歸檔註記）。

9. 🔴 **PUT 的「確認真的存進去了」沒有辨識「這一筆交易是我的」**（擁有者 2026-08-09
   裁決：帶著這條合併，列為具名 follow-up）。
   `services/csp/app/api/platform_settings.py:301-315,368,417-431` 的查證是
   「重讀那一列的最終值 ＋ 稽核筆數比寫入前多一」。兩個管理員**同時**寫**同一顆**設定時：
   A 的 commit 失敗、B（同 key 同值）在 A 查證前成功 → A 會把 B 的列與那多出來的一筆
   認成自己的，回 2xx；反向也可能把已經成功的 A 因為 B 多一筆而誤報 500，
   回應裡的 `updated_by` 也可能標成當下這個人。
   **不是資料損壞**：兩筆寫入都過值域驗證、最後一筆胜出，而且頁面自己會重讀真相
   （管理員重新整理就看得到實際狀態）。**觸發條件是兩人在同一個寫入瞬間改同一顆**，
   而這台是一人運維——這是擁有者裁定可以帶著上線的理由。
   **修法（已知，不要用推論）**：讓查證帶著**這一筆交易自己的身分**——
   把 `set_setting` flush 出來的那一列（或稽核列）的主鍵抓在手上，回頭用主鍵確認，
   **不要用「最終值＋筆數差」去推**。前兩輪的教訓：那兩次都是「用推論代替事實」，
   一次回 200 說存好了其實沒存、一次回 500 說沒存其實存了。
   ⚠ 這一條的三次審查紀錄（含兩次鏡像缺陷）在
   `wt-settings-page/.superpowers/sdd/2026-08-08-settings-page/progress.md` 末段，
   動手前先讀那一段，別重蹈。

---

## 八、本包留下什麼要長期照顧（軸線：維護成本）

功能做完了，但它在系統裡留下八件**會持續要人照顧的東西**。這一節不是缺陷清單
（缺陷在 §六），是**「以後每次動到什麼，就會被這幾件事咬」**。

1. **門檻是唯一「換嵌入模型就必須重新校準」的數字。**
   Task 4 出貨的是**後端**：值存 `platform_settings`（migration `r1_0034`）、每次請求真的查 DB
   （刻意不做快取，否則設定就是假控制項）、三個 **admin 限定**端點——
   `GET`／`PUT /api/institutional-kb/threshold`（後者改完**下一個請求就生效**、留稽核）、
   `POST /api/institutional-kb/preview`（拿一句問句跑**正式那條檢索路徑**，回內容與實際分數）。
   ⚠ **沒有畫面**：設計 §7 描述的校準視圖**沒有被任何 task 承載**（見 §七第 8 點）。
   ⚠ preview 會套用目前門檻，所以校準是 **`PUT` 成 0 → preview 看分數分布 → `PUT` 成中間值**。
   ⚠ **換嵌入模型＝已知的門檻失效事件**——不是「建議重看」，是**那個數字當場失去意義**，
   因為分數是模型算的。內網要做的第一次校準已寫進 `PLAN.md` 的〈部署／內網才能做的量測〉。

2. **標記集的「同一顆嵌入模型」限制只擋在標記那一刻。**
   標記時若該庫的模型與已標記集不同就直接拒絕，訊息給的做法是
   **「開一個用已標記集那顆模型的新庫、重新上傳」**——因為**這個平台沒有重新嵌入的功能**
   （`PATCH embedding_model` 會回 200 但被靜默忽略，不要把它當出路）。
   ⚠ 所以**平台換嵌入模型時，那道檢查完全不會被觸發**：已標記的庫裡放的還是舊模型算的向量。
   **換模型＝要重新檢視每一個已標記的庫**，否則同一個標記集裡混著兩顆模型的分數——
   排序看起來合理但是錯的，**而且不報錯**。
   （另有一個已入帳的小洞：兩筆「第一次標記」若同時進來、模型不同，兩筆都會過——
   跨列沒有資料庫約束擋著。）

3. **`kb_state` 的五個值是前後端契約，改一個值＝跨三個區域的 breaking change。**
   產出端是 **csp 的對話代理**；消費端是 **anila-shell 的兩個映射縫**（訊息渲染那一路、
   對話重載那一路——兩邊各自映射，Task 8 的測試刻意做成「只改一邊就有一邊紅」）。
   ⚠ 想改名或加值的人要同時動 csp、shell 兩個縫、以及釘住它們的測試；
   **少動一處不會有錯誤訊息，只會有一種狀態安靜地變成「不畫」**。

4. 🔴 **`scripts/mutation-check.mjs` 在這台開發機跑不完 —— 而 `CLAUDE.md` 把它列為
   「宣稱測試過」的前置條件。這個矛盾要擁有者知道。**
   兩次**獨立**失敗：Task 8 的實作者只跑完 48 個裡的 5 個（約 2 分鐘／個，全跑約 96 分鐘）；
   Task 9 的實作者**卡在第 4 個突變**，失敗的樣子**很像使用者按了 Ctrl-C**（所以很容易被誤判成人為中斷）。
   ⚠ 它會**就地改動原始碼**，所以**不能與任何 agent 同時在同一個 worktree 跑**。
   在有人修好它之前，這條規則實質上是**跑不動的**——要嘛修工具、要嘛改規則，不能繼續假裝有跑。

5. **思考摺頁在 `forced` 回合仍會原樣露出 `DISPATCH`（氣泡已經乾淨了）。**
   「使用者看不到派工指令」這句話**只對氣泡成立**；`anila.reasoning` 那條事件仍帶原文。
   ⚠ 而且**短答 fallback 的那句話正好請使用者去展開那個摺頁**——
   兩件事合起來才是問題：系統把使用者導向唯一還看得到內部指令的地方。
   摺頁露出是**既有行為**，不是本包造成的；但這是一體的 UX 議題，**留給擁有者裁**。

6. **多 frame「先答案、後指令」是唯一還會在氣泡尾巴露出指令的形狀。**
   單一 frame 的所有形狀都已經清乾淨（13 種形狀實測 8/8 單 frame 全乾淨）；
   剩下的殘留是模型**先串答案、之後才串指令**的那一種。
   ⚠ **修法要 line-buffer `forced` 的串流**——代價是犧牲串流節奏（字不再一個一個出來）。
   **值不值得是擁有者的取捨**，不是實作能自己決定的。答案本身是完整的，露出的只是尾巴的指令文字。

7. **擁有者自己會看到的三個畫面變化**（不是後台的事，是使用者當場看得到的）：
   - **非管理員**在治理中心的**每一張知識庫卡**上多一行**常駐的停用原因**
     （「為什麼你不能標記這個庫」）——這是「狀態要明講、不能靠缺席」的字面實作，測試釘著。
   - **助理的答案**上會出現**規章徽章**與**原文泡泡**（滑上去看被檢索到的那一段＋信心分數）。
   - **重新產生**選單多一項「**改用院內規章重查**」。

8. **`_find_answer_split` 的既有 bug：`Thought:\n` 剛好 9 個字元，撞上 CJK index-10 門檻，
   答案會掉第一個字。** 這條**在本包之前就存在**，Task 9 的實作者**刻意不修**（不在範圍內），
   並把測試寫成斷言**尾段與長度下限**，所以**未來真的去修那個切分器時，這些測試不會因此轉紅**。

### 合併前還沒有人驗的（不是長期照顧，但掉了就會出事）

- **`docker compose build csp` 必跑**：治理中心的前端**烤在 csp 映像裡**，
  而 Task 7／8 的建置是借主樹的 `node_modules`（含 devDeps）跑的——**本機 build 過不等於映像建得起來**。
- **深色主題要用眼睛看**：停用狀態的卡片、規章警示那一句，兩個都只有渲染出來才知道。
- **治理中心測試有 2 個既有紅燈**（`healthOverview.test.mjs:190`、`testConnectionFacts.test.mjs`
  模組層崩潰），已用乾淨副本確認**與本包無關**；router 家族另有 3 個既有紅燈
  （**名字與 3 vs 4 的對帳缺口見 §七第 10 點**）。
  這幾條**要擁有者分診**，不要在最終審查時被誤當成本包的回歸。

---

## 九、最後一句

⚠ **這個功能分支還沒合併，也還沒重建映像。** 主線上午重建過一次，
但那是**這個功能開始之前**的狀態。`wt/institutional-kb` 的東西要等十個任務做完、
最終全分支審查通過，才會走 `finishing-a-development-branch`。
