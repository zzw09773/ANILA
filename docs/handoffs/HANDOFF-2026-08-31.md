# HANDOFF — 2026-08-31（→ 下一個 session）

> **這一份與過去幾份不同：規範層在今天被擁有者裁定清除，你接手的是一棵沒有 `CLAUDE.md` 的樹。**
> 先讀第 0 節，它會告訴你「你現在缺什麼、去哪裡拿」。

---

## 0. 接手第一件事

```bash
git log --oneline -1                       # 應為 5b088074
git status --porcelain                     # 應有兩筆 staged 刪除（見 §1）
docker ps --format '{{.Names}}\t{{.Status}}' | grep anila   # 應 14 個 healthy
```

🔴 **這棵樹已經沒有 `CLAUDE.md`，也沒有全域規範與 playbooks。** 2026-08-31 擁有者裁定：
> **「跨專案清除（所有 claude 規範），但是教訓要留著。」**

**所以你不會拿到「該怎麼做」的指示，但你拿得到「別人怎麼摔的」**：

| 你要的東西 | 在哪裡 |
|---|---|
| **134 條教訓**（每條帶日期與事故現場） | `~/claude-lessons/LESSONS-team-20260821-0831.md` |
| **ANILA 環境事實／TLS 大坑／運維 footgun／最貴的四條教訓** | `~/claude-lessons/LESSONS-anila-env.md` |
| **68 份逐條記憶** | `~/.claude/projects/…/memory/`（本體）＋ `~/claude-lessons/memory-snapshot/`（快照） |
| **被清除的規範全文**（要復原就從這裡） | `~/claude-rules-backup-20260831-1134/`（1.1M、200 檔） |
| **完整進度快照** | `~/anila-deliverables/PROJECT-STATE-20260831.md`（108 行、十節） |

⚠ **讀教訓的兩個前提**：① 它們是「某次事故換來的」，不是「應該這樣做」——**重新設計制度時看那個現場，不是那句結論**；② 裡面的數字是 2026-08 的快照，**動之前實測**。

🔴 **新規範尚未設定。擁有者要重新設計，等他開口，不要自己補一份回去。**

---

## 1. 樹的狀態

```
本地分支   restart/from-redesign
HEAD       5b088074  fix(ingest): reuse captions only when the generator itself matches
追蹤       origin/restart/from-redesign-20260822    0↓ 0↑（已同步）
⚠ 舊遠端   origin/restart/from-redesign             落後 376、刻意未動
```

**未提交（今天清除規範產生的，已 `git add`，擁有者尚未裁要不要 commit）**：
```
D  CLAUDE.md
D  docs/WHERE-THINGS-LIVE.md
M  docs/README.md          （拔掉指向上面那份的死連結）
```
**未追蹤且刻意不納管**：`.claude/`（本機權限設定與 workflow）、`.worktrees/`

**四棵 worktree 還在，都沒刪**：
`wt-stage5-wheelhouse`（350M 產物，見 §3）、`wt-startup-migration`（碼已併入主線）、
`wt-grok-eb`、`wt-grok-pins`、`wt-asr-auth`、`wt-breakglass-login`、`wt-governance-404`

---

## 2. 活體平台（本機 `-p anila-restart`）

**14 容器全部 healthy。** csp 與 ingestion-worker 於 4 天前隨最後一次重建更新，其餘 7 天。

| 入口 | 現況 |
|---|---|
| `/`、`/anila/`、`/router/health` | 200 |
| `/asr/health` | 啟動初期短暫 503（自檢流程），之後 200 —— **不是故障** |
| `/anilalm/` | **503 是刻意的發行閘**，不是故障 |

資料庫：`alembic_version = r1_0036`、65 表、110 圖、359 片段。

---

## 3. 上線主線：PLAN 九段，停在第 5 段

| 段 | 狀態 |
|---|---|
| 1–4、6 | ✅ 08-05～08-10 完成 |
| **5 wheelhouse** | 🔶 **做到一半，執行席位已廢棄，擁有者裁定重做** |
| 7 重掃重打包 | ⬜ 🔴 **順序硬相依：先 wheelhouse 再掃描**（反過來會掃到一組確定不出貨的套件） |
| 8 部署包＋全新安裝驗證 | ⬜ |
| 9 使用者手冊（HTML） | ⬜ |

### 第 5 段：已完成的別重做，未完成的是這三件

**產物在 `wt-stage5-wheelhouse`，對應 commit `5b088074`，350M，未刪。**

✅ 已完成：
- 六份 freeze 對 `docling/easyocr/torch` **零命中**（08-17 裁定它們不出貨，該砍的是 freeze 那邊）
- `services/anila-core-router/requirements.txt` → `pydantic-settings==2.15.0`（唯一範圍 specifier 已釘死）
- wheel↔freeze 雙向閉包 audit 綠；舊 docling/easyocr/torch wheel 已清

🔴 未完成：
1. **overlay 演練**（用 wheelhouse 當 `-c` 約束裝一個小套件，前後 `pip freeze` diff 只有它自己）
2. **`root:root` 檔案權限處理**（容器寫出來的，主機端要讀得到、清理不卡）
3. **交件報告**

⚠ **量到但沒過的一格**：**映像實裝版本 vs wheelhouse manifest 有 22 筆差異**
（cryptography 50.0.0→50.0.1、onnxruntime 1.28→1.29、uvicorn、idna、lxml… 各 CPU 服務 5 筆、asr-decoder 7 筆）。
**成因＝出貨映像是舊解析、新 dist 是今天的解析，不是 wheelhouse 收集錯。**
👉 **前一任的建議（未經裁定）**：**推遲到第 8 段重建映像後複驗**——第 8 段本來就要對打 tag 的樹全部重建，
屆時兩邊解析幾乎同時發生，再比才有意義；現在硬追平是白做一次。

---

## 4. 🔴 上線前必修（四件）

| 件 | 現況 |
|---|---|
| **啟動 migration 靜默降級**（HIGH） | ✅ **已修並上線**（`14f22a1b`）。啟動會印「即將 r1_0036 → head」，失敗一律致命、可用 `ANILA_SKIP_STARTUP_MIGRATIONS` 拒絕 |
| **OIDC discovery 未驗證**（HIGH） | 🔴 **材料備好、未派工**：`~/anila-private-audits/security-sweep-2026-08-19/REPAIR-OIDC-DISCOVERY-materials.md`。⚠ 開單時第一句要寫「什麼叫假修」——**只給 `jwks_uri` 加 https 檢查會擋住信任錨那一半，而 `token_endpoint`（連同 `client_secret`）一個字都沒動** |
| **稽核匯出結構破壞＋CSV 注入**（MEDIUM） | 🔴 **材料備好、未派工**：同目錄 `REPAIR-AUDIT-CSV-HEADER` ＋ `REPAIR-CSV-XLSX-INJECTION`。⚠ 同樣有假修陷阱：**現成的中性化函式只看首字，而 payload 的逗號引號在中間** |
| **6 條 DSN 紅** | 平常被 skip 看不見。2 條 `downgrade_..._clean` **根因已查明＝要穿過 `r1_0035` 這道刻意的單向門，守衛沒壞**；另 4 條 `test_memory_scope_pg` **成因未查** |

---

## 5. 其他未結（`~/anila-deliverables/queued-*.md` 共 42 份，會咬人的幾張）

- 🔴 **一般同仁 OOBE 第 2–9 步從未走過** —— 當初被登入牆擋死，牆解掉後沒有人回去補走。
  **那是三千人每天要走的路，目前等於未稽核，而第 9 段手冊就是照它寫的。**
- **圖片位置對映 P-1**：服務端跳過缺件時其後的圖全部位移一格。
  ⚠ **觸發條件目前未知**（CMYK 那個推測**已被二次撤回**，docling 頁面圖產生時就轉 RGB）；
  驗收要用**注入式跳過**，**「重匯後位置看起來對」不得用來關它**。
- **CSRF 第二掛載點**：`sse.js:431` 與 `:128` 同形，**只有 `:128` 有守衛**。
- **Router 三份 system prompt 進治理中心設定頁**：擁有者已裁定要做，migration 取 `r1_0037`。
- **檔案權限建置期守衛**：0640 檔 COPY 進映像後容器讀不到，**本週咬兩次**
  （一次讓 alembic 靜默死、一次讓 csp 起不來），**而 git 只追蹤 exec 位元，diff 裡看不見**。
- **重匯時重用既有 caption**（LOW）⚠ 帶一條不變式：**不得靜默取消「重匯以套用修法」那條路**。
- **我們自己截的 caption 沒標記**（MEDIUM）。
- **浮水印**：Q59 已裁定拿掉，樹上仍有。

---

## 6. 擁有者已裁定，不要再提案

- **repo 2026-08-24 起 PRIVATE**；歷史裡的真員編與兩把退役 `server.key` **一律不處置**。
  ⚠ 殘餘照實記著：公開窗口 2026-04-25→08-24 約四個月，**期間的 clone／快取不會因為改 private 而消失**（fork 數 0、無外部貢獻者）。
- **治理中心 51 個 `.vue` 零執行覆蓋** ＝ **具名的已接受風險**（接受人：審查長，2026-08-22；擁有者可隨時覆蓋）。
- **圖說在開發測試期間維持開啟** ——「關掉會把要找的訊號一起關掉」。
- **幕僚長派工＝擁有者意圖**（不解除安全紅線；**commit／push 仍只在擁有者要求時**）。
  ⚠ **但這條是轉述型授權**：2026-08-26 有一席的權限系統**不接受轉述**而停手——**那是正確的**，遇到就請擁有者直接對該席說。

---

## 7. 席位

**全部團隊席位已結束**（審查長／Reviewer／資安官／code-grok／code-deepseek／doc-deepseek／scan-deepseek）。
**deepseek 席 2026-08-31 由擁有者裁定廢棄。**
🔴 **新的編制與派工規矩尚未設定** —— 舊的已隨規範一起清除，**不要照舊記憶重建**。

---

## 8. 給接手的三句話

1. **你沒有規範，這是刻意的。** 需要判準時去 `~/claude-lessons/` 找**現場**，不要找**結論**；
   要立新規矩之前先問擁有者——**他正在重新設計。**
2. **最急的不是技術債，是那條沒有人走過的路**（§5 第一項）。
   八月底上線的手冊要照它寫，而它到現在沒有被稽核過。
3. **這棵樹上每一個「看起來已經好了」的東西，本週都至少有一個是假的**：
   容器與 tag 一致但新碼沒進去、測試全綠但守衛紅不起來、報告寫「已清掉」而清理從沒進主線。
   **驗的時候跨過那個變化去比，不要跟另一個衍生物比。**
