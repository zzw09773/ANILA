# Handoff — 2026-07-03～04 假日改動總結

> 給週一進公司的自己(與 Codex)。這批工作在**家裡的 Windows 機**(D:\ANILA,
> Docker Desktop 本機 stack)完成,全部已 commit + push。看完「待辦」段就能接手。

## TL;DR

假日做了四件事,**全部已推上 `main` + 4 條 downstream**(移植零漂移,已抽查):

1. **維運工具**:新增 `anila-ops.sh`(健檢/備份還原/憑證換發/break-glass)+ 從 0 到上線部署指南
2. **治理 UI**:登入後預設跳 `/anila/app`(?next 回跳不變)+ 全字階 +1px
3. **產出中心**:ANILALM 新增 `/outputs` 跨知識庫產出總覽,Shell 導覽「產出中心」深連結分流(原本跟「我的知識庫」同落點)
4. **互動心智圖**:NotebookLM 式橫向樹,逐層展開到最深層、**點節點直接把問題送進對話**;SVG/DOT 降為下載選項

## 各分支狀態(2026-07-04 推送完畢)

| 分支 | 狀態 |
|---|---|
| `prod-intranet-card` | 原始 4 commit:`05125a5` ops → `beac4a3` governance-ui → `ae66df2` outputs → `c46ae54` mindmap |
| `main` | 移植版 `0823382`/`f06be82`/`313ad19`/`483fbfc`(新分支模型下 card 碼由旗標閘控,非汙染) |
| `dev-public` / `prod-public-passwd` / `dev-military` / `prod-military-passwd` | 同樣 4 個移植 commit,與 card 分支逐檔一致 |
| `trial-military` | **未帶**(維持挑選式 port 政策;展示版要不要這批功能待決) |

## 改動細節(要深讀就看各 commit message,都寫得很完整)

### 1. 維運工具(`05125a5`)
- `infra/deployment/scripts/anila-ops.sh`:`health`(深度健檢,FAIL 即 exit 1 可接告警)/
  `backup [--full]`(DB+.env+JWT+PKI,700/600)/`restore`(改名保留舊庫+灌壞自動回滾)/
  `cert-renew`(nginx -t 被拒自動還原舊憑證)/`model-ca`(先驗鏈)/`gateway-key`/
  `break-glass on|off`/`prune`。**有過一輪 code review,HIGH/MEDIUM 全修**。
- `docs/runbooks/intranet-zero-to-prod-guide.md`:空機到上線乾淨主路徑,runbook 當深入對照。
- `.gitignore` 補 `/backups/`、`*.pfx`、`.venv-*/`。

### 2. 治理 UI(`beac4a3`)
- `LoginView.vue` `resolveNextDestination()` 無 `?next=` 時預設 `/anila/app`(原本回治理 dashboard)。
- `tokens.css` 字級 token 全階 +1px(base 13→14px)。

### 3. 產出中心(`ae66df2`)
- 新 `apps/anilalm/src/routes/OutputsPage.tsx`:聚合 artifact store(localStorage)全部產出,
  類型篩選/搜尋/各格式下載/跳工作區/移除。
- `shellNav.jsx` 產出中心 → `/anilalm/outputs`;shellNav 測試同步改(222 測全綠)。

### 4. 互動心智圖(`c46ae54`)
- 後端:mindmap job 持久化驗證後的 `MindmapSpec` JSON,下載路由開 `fmt=json`;
  舊 job 回 404 → 前端 fallback 舊下載面板。OpenAPI 已重匯、ANILALM 型別已再生。
- 前端:`MindmapTree.tsx`(橫向樹/⊕N 展開/全部展開收合/純 CSS 接線);點節點帶上層
  脈絡組問題 → workspace store 新增 `pendingAsk` 橋接 → `WSChat.send` 支援代發
  (串流中排隊不吞問題)。**這條橋接是通用機制,之後「點了就問」都能重用**。
- studio 心智圖相關 32 測全過(含新增 json 斷言);全套 508 過。

## 本機(家裡 Windows)環境備忘

- 本機 stack 已可跑:`docker compose up -d`(10 服務,無模型/無卡登,admin 帳密在本機 `.env`)。
- 踩過的 Windows 坑:①Docker Desktop 平行拉層 snapshot 競態(重試即過)
  ②Git Bash MSYS 把 `-subj "/CN=..."` 路徑轉換弄壞 → 產憑證要 `MSYS_NO_PATHCONV=1`。
- 四月舊 csp-pgdata volume(4 users/6 conversations)已 dump 到本機 `backups/legacy-pgdata-2026-04-21.sql.gz` 後清除重建。

## 待辦 / 未決(進公司後處理)

1. **心智圖端到端實測**:本機無模型,「生成→點節點→AI 回答」整條還沒真跑過。
   到 dev 機或內網生成一張實測;有問題回頭修 `MindmapTree.tsx` / `mindmaps.py`。
2. **anila-ops.sh 實機演練**:只在 Windows 過語法與 review,沒碰過真 Linux stack。
   建議 dev 機跑一輪 `health` → `backup` → 拋棄式 volume `restore` 排演再進 SOP。
3. **`test_hydrate_images` 3 failures**:FLUX 環境相依(Windows 無 FLUX),與本批改動無關;
   在 dev 機確認是綠的即可安心。
4. **`docs/onyx/` 兩檔在我的工作樹被刪**(非我所為,可能另一視窗/Codex 動的)——
   未 commit。故意的就補 commit,不是就 `git checkout -- docs/onyx/`。
5. **trial-military 要不要帶這四個功能**:待決。
6. **內網部署收尾**(既有待辦,CLAUDE.md §5):`.12` gateway API key 簽發後
   `anila-ops.sh gateway-key` 填入。
7. **OutputsPage 已知限制**:清單存 localStorage(沿用 artifact store MVP 設計),
   跨裝置要做後端 artifacts 持久化 API(store 註解標 Sprint 6+)。
