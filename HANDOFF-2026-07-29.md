# HANDOFF — 2026-07-29 工作交接

> ⚠ 2026-08-17 盤點：本檔為【歷史紀錄】——2026-07-29 當日的 session 交接,不代表現況。現行狀態與執行順序見 `PLAN.md`。

> 給下一個 session 的接手文件。讀完這份就能接著做，不必回頭翻對話。
> 權威文件仍是：規格＝`SYSTEM-MAP.md`、順序＝`PLAN.md`、環境與陷阱＝`CLAUDE.md`、歷史＝`RESTART-FROM-REDESIGN.md`。
> 本檔只記「這一天發生了什麼、為什麼這樣決定、下一步卡在哪」。

---

## 0. 一句話現況

**P0 全關、P1 除了必須進內網的 1.6 之外全關**，本機 13 服務健康運行，所有成果已推上 `restart/from-redesign`（`main` 一字未動）。下一步由擁有者選：P2（上線前安全）或 P3.1（資料庫備份，PLAN 標為「現在就有的風險」）。

---

## 1. 今天完成了什麼（commit 由舊到新）

| Commit | 內容 |
|---|---|
| `faa2ac3` | 三份權威文件入庫（SYSTEM-MAP／PLAN／RESTART），順帶解掉「程式註解引用 PLAN.md 但檔案不在樹裡」的追溯問題 |
| `657ba43` | **P0.2** http 模型端點旗標分域（merge） |
| `f8a2eb9` | **P0.3** codeserver 修復：自建 image（docker CLI＋extension 離線烘焙）＋init chown |
| `93ba157` | **P1.1** 部門三層樹（`r1_0009`，merge） |
| `a2d1f42` | **P1.2** 用量逐層加總（merge） |
| `526783c` | **P1.3** 單位管理員（`r1_0010`，merge） |
| `5062c96` | **P1.4** 批次核准（merge） |
| `1584ac4` | 附件儲存部署缺陷修復（見 §3） |
| `8332303` | **P1.5** 附件處理（`r1_0011`，merge） |
| `9ac179b` | 部門深度上限改可設定＋`page_count` 進 API |
| （本檔） | PLAN 納入 Open WebUI 參照盤點＋本交接檔 |

**測試基線變化**：663 → 749 passed。既有的 37 failed / 1 skipped / 2 errors 是環境相依的 pre-existing，全程未變——**任何一輪若這組數字變了就是回歸**。

**alembic head：`r1_0011`**（三個新 migration 都已在本機真 Postgres 上跑過）。

---

## 2. 每個功能包的設計裁定與理由

⚠ **這一節最重要**：以下多數是「刻意這樣做」而非「還沒做到」。不寫下來，接手的人會把它們當成缺陷去「修」。

### P1.1 部門三層樹
- **深度上限**：預設 3，但已改成設定值 `ANILA_DEPARTMENT_MAX_DEPTH`。擁有者的組織圖顯示「院長室→管理中心→人力資源處」也是三層；若處/組底下還有科，改 `.env` 即可。
- **停用有活子節點的父節點 → 400**（fail-closed），不做級聯停用。
- **名稱全域唯一**未改。組織圖確認處室名稱不重複，暫時安全。
- **並發**：階層變更走 `pg_advisory_xact_lock`，鎖後 `db.refresh` 再讀（審查抓到過鎖前讀取的競態）。

### P1.2 用量逐層加總
- **讀取時聚合**，寫入端歸屬不動。
- **`get_top_departments` 維持直接歸屬**（不 rollup），因為那是排行榜語意。
- **組織圖確認**：六個中心與研究所同為一級單位，所以「系統製造中心用了多少」用逐層加總直接答得出來，**不需要額外維度**。（我一度誤判需要，已更正。）

### P1.3 單位管理員
- **不含額度分配**（擁有者拍板，遞延至計價 epic，docstring 已註記）。
- **綁定表 `unit_admin_assignments`，`users.role` 與 `auth_service.py` 一字未動**（審查以 sha256 驗）。unit_admin 永不通過 `is_admin_tier`／`require_admin`。
- **零對話 metadata**：成員視圖只給用量數字與帳號狀態，連對話標題都不給。
- **不可見帳號回 404 而非 403**，與 list 端點的不可見性一致（避免用狀態碼枚舉帳號）。

### P1.4 批次核准
- **兩道界線**：原始輸入 1000 個 id、實際核准 500 個。後者**算待核准數而非成員總數**，否則超過 500 人的院級節點永遠批不動。
- **稽核雙寫**：每個核准帳號一筆＋批次摘要一筆。
- **稽核寫入失敗即 500 中止整批**，不留半套（`log_audit_event` 是 fail-soft，會吞例外並 rollback，批次端點必須檢查回傳值）。

### P1.5 附件處理（最大的一包，四輪修訂）
- **改動前附件功能等於不存在**：上傳只寫檔，模型收到的只有檔名。
- **准入是推導值不是儲存狀態**：`extract_status` 只記抽取結果（pending/ok/failed/unsupported/too_large），「塞不塞得下」每次依當下模型重算。這消除了「用 A 模型算出的判定拿去 B 模型用」的失真，也讓刪除後的遞補變成天然行為（無需 recheck）。
- **預算 = context window × 0.7**，估算乘 1.15 安全係數。**歷史不動態扣除**——那 30% 就是留給歷史與問答的，讓預算隨對話變長而縮小會讓刻度變成移動標靶，還可能事後踢掉已接受的文件。
- **儲存上限是絕對值**（`ANILA_ATTACHMENT_MAX_STORED_TOKENS`），刻意不綁預算：抽取時還不知道模型，綁預算會讓大 context 模型吃得下的文件被永久丟棄。
- **不做切塊檢索**（擁有者拍板）：超出預算就明白告知並引導去知識庫。
- **容量物件隨每個回應吐出**，讓前端能畫「已用 68%」的刻度——這是參考 claude.ai 的做法：預算要一直看得見，撞牆才拒收體感很差。
- ⚠ **依賴 P1.6**：文件每輪都重新注入，沒有前綴快取的話第 10 輪跟第 1 輪一樣貴。

---

## 3. 順手修掉的兩個既有缺陷

**附件儲存（`1584ac4`）**：`ATTACHMENT_STORAGE_PATH` 預設是容器相對路徑且無掛載，每次 `up -d --build csp` 全體使用者附件消失。已掛 `share/attachments`。
⚠ **刻意不放在 `share/uploads/` 底下**：nginx 的 `location /uploads/` 是**無認證直接服務靜態檔**（只有 `/uploads/ingestion/` 被 404），附件路徑是 `<user_id>/<uuid><ext>`，放進去等於全院可直接下載別人的檔案。已實測 `https://主機/uploads/attachments/` 回 404。

**gitlab 起不來**：omnibus 直接讀 `GITLAB_ROOT_PASSWORD` env，空字串在 Ruby 是 truthy 會炸長度檢查。**部署到 `.15` 的 `.env` 必須帶這個 key。**

---

## 4. 待擁有者決定（依時效性排序）

| # | 事情 | 為什麼有時效 |
|---|---|---|
| 1 | **OW-1 訊息歷史樹**（PLAN 新段落）：`messages` 現在是線性的，沒有 `parent_id`。要不要「編輯重問／重新產生各成分支」？ | **還沒有真實對話資料，現在改最便宜**；三千人開始累積後要處理歷史資料與所有假設線性的程式碼 |
| 2 | 下一條線走 P2（上線前安全）還是 P3.1（資料庫備份）？ | PLAN 自己標 3.1 是「現在就有的風險」——稽核帳與長官月報都在那個沒備份的資料庫裡 |
| — | ~~OW-3 執行機制~~ **已拍板：用 exec**（擁有者親自體驗用例後決定；用例＝點按鈕跳懸浮視窗選 prompt，固定化流程如「第一步載入資料庫數據」，判斷貼 Python 最快且部分需求非 n8n 該做）。實作時必須連帶做三件事，見 PLAN OW-3 | 已決 |
| — | ~~agent 七態核准是否過度工程~~ **已查證：成立**，見 PLAN「過度工程盤點」與 OE-1 | 已決 |
| 4 | 部門樹實際最深幾層？處/組底下還有科嗎？ | 旋鈕已備好，但建樹前確認比較好 |
| 5 | codeserver 的 docker.sock 掛載＝主機 root 等價，密碼即失守。可接受嗎？ | 已上線運行中，取捨已記在 platform.yml 註解 |

---

## 4b. ⚠ 系統性過度工程稽核（下個 session 的重點工作之一）

**擁有者的提醒（2026-07-29）**：這棵樹是從他認為「**被做壞的**」狀態複製過來的，所以**必定殘留大量過去的過度工程**。今天的 agent 七態核准只是被抓到的第一個，不是唯一一個。

### 為什麼會這樣

重啟樹的基底 `a4118a3` 是 **redesign 收斂點**，而那批程式碼是照 `docs/anila-redesign-docs/` 的 **doc 00–10** 建的。SYSTEM-MAP 是**後來**才寫的，而且開頭明文宣告「**這份取代先前所有規劃文件的權威地位**」。所以：**照舊文件蓋出來的東西，沒有人回頭用 SYSTEM-MAP 重新檢驗過。**

### 可 grep 的稽核入口（已量測）

那些構造在原始碼註解裡留著出處，例如 `# doc 05 §3`、`# doc 08 §7`。實測規模：

```
grep -rE "doc ?0[0-9] ?§[0-9]+" services/ packages/ --include="*.py"
→ 83 個檔案
```

引用密度最高者：`app/modules/policy/service.py`(30)、`app/models/artifact.py`(22)、`app/schemas/contracts/agents.py`(21)、`app/schemas/contracts/classification.py`(17)、`app/models/agent.py`(15)、`app/models/task.py`(12)。
migration `r1_0001`–`r1_0008` 全部是照那批文件建的 schema（Slice 2a/3a/5a/6a/7a/8a、R-SEC）。

### ⚠ 稽核判準（重要，不要簡化成「有引用就砍」）

**引用 doc ≠ 過度工程。** SYSTEM-MAP 確實要求的東西也會帶著 doc 引用——例如**分類的單向閂鎖**是硬需求，照樣寫著 `doc 08 §N`。砍錯會拆掉真需求。

⚠ **但「閂鎖是需求」不等於「五級是需求」——我自己在這裡就犯了一次錯，值得當範例看。**
本檔初稿我寫「五級分類單向閂鎖是硬需求」並警告不要動它。擁有者質疑「怎麼還有五類」，一查才發現：**SYSTEM-MAP §8 寫的是四級**（「實際會碰到：**無機密 / 營業秘密 / 密 / 機密**」，門檻表欄位亦同），而程式碼是五級且結構不同——見 §4c。**我當時是照程式碼的自我描述複述，沒去核對規格**，正好違反自己在上一段寫的「用文字找，不要憑印象」。稽核時每一條都要真的去 SYSTEM-MAP 找字，包括那些「看起來明顯是需求」的。

正確判準是三問：
1. **SYSTEM-MAP 有沒有要這個？**（用文字找，不要憑印象）
2. **它有沒有持續成本？**（每次都要走的流程、每次都要維護的欄位、限制設計的約束）
3. **它的唯一理由是不是只剩舊文件？**

三題都是「是」→ 收斂候選。今天 agent 核准就是三題全中（SYSTEM-MAP 只要「註冊→指派→用」、每註冊一個 agent 都要走三關、註解全指向 doc 05）。

### 已確認的兩個實例

| 實例 | 情況 |
|---|---|
| **agent 七態核准 + trace-test 閘門** | 已查證成立，排成 **OE-1**（詳見 PLAN） |
| **P0.1 的四個啟動閘** | 反向案例：PLAN 說要拿掉，但實際上**本基底根本沒有**——那是 07-13 之後才加的，也就是說 PLAN 當時也在對著錯誤的認知寫 |

### 建議優先查的（依引用密度與矛盾程度）

- **`app/models/trace_span.py` / `app/api/traces.py` / `proxy/spans.py`（doc 05 §6）** — SYSTEM-MAP §7 有一句幾乎是直接否定：「一張表加幾個索引。**不需要 span 樹、parent 關係、trace id。**」需確認 trace 基礎設施在稽核章節有沒有其他依據；若沒有，這是最大一塊。
- **`app/modules/policy/`（doc 08，30 處引用）** — 但**要小心**：五級分類是 SYSTEM-MAP 硬需求，這裡多數可能是真的。要逐條分。
- **`app/models/artifact.py` + `modules/artifacts/`（doc 08 §5，22 處）** — SYSTEM-MAP 對 Studio 產出的描述是「暫存伺服器、設時限自動刪」，比 artifact 四表契約簡單很多。
- **`app/schemas/contracts/`（agents 21＋classification 17＋…）** — 契約層是否還對應現行需求。

### 稽核完的產出應該長怎樣

不是一份「可以砍的清單」，而是**每一項標成三類**：`規格有要`（保留並在註解改引 SYSTEM-MAP）／`規格沒要但無害`（留著，標為技術債）／`規格沒要且有持續成本`（排入收斂）。第三類才進 PLAN 排工。

---

## 4c. ⚠⚠ 分類等級與門檻都與規格不符（時效性最高，灌真實資料前必須處理）

擁有者 2026-07-29 質疑「怎麼還有五類」，查證後**兩個問題都成立**。

### 問題一：等級集合不同（不只是「多兩級」）

| | 0 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| **SYSTEM-MAP §8**（權威） | 無機密 | 營業秘密 | **密** | 機密 | — |
| **程式碼** `schemas/contracts/classification.py:39-43` | 無機密 | 營業秘密 | 機密 | 極機密 | 絕對機密 |

- **「密」在現行程式碼完全無法表示** —— 一份實際標為「密」的文件沒有值可存。
- **「機密」兩邊位階不同**：規格是最高級，程式碼是中間級。
- 極機密／絕對機密是規格明說「實際會碰到」清單之外的。

規格原文（§8 開頭）：「密等在**專案啟動時就標好了**，平台主要是**記錄**它。實際會碰到：**無機密 / 營業秘密 / 密 / 機密**。」下方門檻表的欄位也正是這四個。

### 問題二：門檻判準錯了（SYSTEM-MAP 自己已經記著要改）

規格兩條線：**可以做 = 密等 ≤ 營業秘密**、**要落稽核 = 密等 ≥ 營業秘密**。
SYSTEM-MAP §8 第 244 行原文：「（現行程式碼是統一判準 `> 無機密` 全部管制，所以**營業秘密現在不能分享 —— 要改**。）」

也就是說營業秘密目前被當成機密在管，複製／匯出／分享／列印全被擋。

### 為什麼必須在灌真實資料前做

分類是**單向閂鎖，沒有降級路徑**。等級寫錯之後要更正，得走受治理的解密申請流程（申請人 ≠ 核准人）或做資料遷移。現在資料庫還是空的，改成本最低。

→ 已排成 **PLAN 的 OE-3（等級對齊）與 OE-4（門檻對齊）**。

---

## 5. 派工與審查的操作要點（下個 session 直接沿用）

- **grok 實作派工用 fast 變體**：`--model cursor-grok-4.5-high-fast`（2026-07-29 使用者指示，已改寫 playbook 10 §3c 並備份到 `~/.claude/backups/2026-07-29-cursor-fast-variant/`）。
- **審查編組**：opus（Agent 工具）＋ sol（`codex exec resume <session>` 續 session 省 cache）。今天的 session id：`019fac5f-ce1a-7d33-a799-22d4b884f3db`。
- **codex CLI 陷阱**：`-m sol` 短名會 400（要全名 `gpt-5.6-sol` 或裸跑）；`codex exec resume` **不吃 `-s` 旗標**。
- **測試指令**（本樹尚無自己的 venv，借舊樹直譯器）：
  ```
  cd services/csp && PYTHONPATH=<repo>/packages/anila-core/src \
    ~/桌面/ANILA/anila-migration-20260706/ANILA/services/csp/.venv/bin/python \
    -m pytest -q -p no:cacheprovider
  ```
  ⚠ 接 `| tail -2` 時 **exit code 取自 tail 不是 pytest**，別把 exit 0 當成測試全綠。

---

## 6. 這一天學到的方法論（會重複踩，值得記）

1. **派工包給不變式與驗收案例，不要給沒驗算過的公式。** P1.5 連續兩輪的阻擋級缺陷都源自我寫的 `bytes/2`（壓縮 PDF 高估 3.8–1980 倍；ASCII 也高估一倍）。實作者會照抄規格，測試會照著錯誤規格寫成綠的。
2. **驗收要從外面打。** sol 每次走 HTTP 端點，opus 多半直接呼叫服務函式——五輪下來 sol 抓到三件 opus 判乾淨的東西（跨使用者上傳、`text/*` 例外、`?model=` 狀態不一致）。派審查包時明寫「請走真實端點」。
3. **裁決分歧要自己驗算術，不要數票。** opus 判 `text/*` 例外乾淨，但它自己探針印的 `400000 bytes → 粗估 200000 tokens` 就是反證（真值約 100,000），它把矛盾數字歸檔在「預期結果」欄下。
4. **審查者讀不到自己的盲點時，讓它自陳方法。** opus 事後精準診斷了自己為何失效（用 `"a "*n` 這種每隔一字元是空格的退化字串去校準 `bytes/2`，剛好讓公式看起來精準）。
5. **寫的人不驗自己的產出**——今天我自己動的碼被 sol 抓到一次真 bug（`load_only` 漏欄位造成 N+1）。
6. **審查包禁用漏洞挖掘語彙**：我的 P1.3 審查包用了 privilege-escalation／adversarially probe／leak，整則訊息被安全分類器擋下。改中性維護語彙（「回應碼一致性」而非 existence oracle），驗證強度不變。

---

## 7. 沒做的事（明列，不要當成已完成）

- **P1.6 前綴快取查證**：必須進內網確認 `.12` 跑哪個推論引擎、有沒有開 prefix caching。**它的結果會回頭決定 P1.5「整份進 context」在實務上划不划算**。
- 附件的 **3 天保留期與刪對話留孤兒檔案**：P1.5 明確不含，仍待處理。
- 前端：P1.3–P1.5 都只做後端，UI（單位管理員儀表板、批次核准介面、附件容量刻度）尚未動工。
- ANILALM 與 Studio：本日完全未碰。
