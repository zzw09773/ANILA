# 工具能力表（唯一真相來源）：name → read_only | write | admin。
#
# SDK 的 @function_tool 沒有 is_read_only 欄位，工具能力在此宣告。
# P2 的政策（configs/policy.yaml）以此判定 deny-all 下允許哪些工具執行。
# 未登錄的工具 fail-closed 視為 admin（最受限）。

capabilities:
  search_documents: read_only
  read_document: read_only
  search_memory: read_only
