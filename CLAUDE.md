# CLAUDE.md — ANILA 重啟樹(給 Claude Code)

> **這是重啟樹**:2026-07-28 平台擁有者決定回到 redesign 收斂點 `a4118a3`(2026-07-03)重新出發。
> 權威文件就在本樹根目錄:**規格＝`SYSTEM-MAP.md`**(14 章,`## 0`～`## 13`;
> 檔頭的 Q1–Q28 是「依平台擁有者逐題確認重建」的**來歷**,不是章節結構)、
> **順序＝`PLAN.md`**(到 8 月底上線)、
> **歷史＝`RESTART-FROM-REDESIGN.md`**(373 commit 履歷＋attic 取回方式)。
> 🟣 **2026-08-17 擁有者裁定:`PLAN.md` 是專案權威**——其餘文件指向它,不與它競爭。
> **分工要記牢:`SYSTEM-MAP.md` 是規格(系統「應該」長什麼樣);
> `PLAN.md` 是現況與工作順序(現在「是」什麼、下一步做什麼)。兩者不互相取代。**
> 📌 **接手先讀 `docs/handoffs/HANDOFF-2026-08-15.md`（最新）＋ `docs/handoffs/HANDOFF-2026-08-11.md`**
> （後者仍有效，兩道交付閘門、九段順序、驗收心法在那份；`-08-10` 與 `-08-07` 談的是
> 兩道交付閘門的來龍去脈與院內規章檢索的長期照顧事項）；
> ⚠ **這一行標「最新」的檔名每加一份 HANDOFF 就會過期一次**——2026-08-17 實際發現它指著
> `-08-11`，而 `-08-15` 早就存在。**寫「最新」就是寫一個保證會過期的宣稱。**
> 以 `ls docs/handoffs/HANDOFF-*.md | sort | tail -1` 為準，別信這一行的檔名。
> 擁有者要看的是 `docs/TOMORROW.md`。
> 🟢 **設定頁只剩 12 顆**（2026-08-11，Q46）。新增設定前先過這一關：
> **一顆值要留在設定頁，必須答得出：上線之後，誰、在什麼情境、為什麼不能等下一次改版。**
> 那 84 顆被砍掉的，正是兩個 CRITICAL 的根源——**能從網頁動到的安全開關，就是一個等著被動的安全開關。**
> 🔴 **凍結前的硬閘，而且是兩道獨立的關**：① **雜物掃描**（`scan-image-artifacts.sh`）；
> ② **映像要真的載得回來**才算數。**兩道關互相看不見對方漏掉的東西**——
> **每次「重建映像」的收貨都要把這兩步都跑一次**。
> **`docker build` 成功不等於映像出得了門。**
> ⚠ **2026-08-15 修訂（機制已變，事故仍為真）**：舊版寫的是「`docker save` 2/7 張存不出去」。
> `6e68f931`（2026-08-14，已在 HEAD 祖先）之後，**本專案自己 build 的映像不再走 `docker save`**：
> `docker buildx bake --set <target>.output=type=docker,dest=<tar>` **由 builder 直出 tar**，
> 掃 tar，再**真的 `docker load` 驗回來**（`build-and-export-for-intranet.sh`，五段式 `[1/5]`～`[5/5]`）。
> **只有上游映像（pg／redis／nginx／gitlab）與選配 model 映像仍走 `docker save`。**
> 📌 **兩個歷史數字留著，因為它們是真的**：2026-08-10 實測**雜物掃描 65 筆違規／4 張映像**、
> **`docker save` 2/7 張存不出去**（本機 DCS 代理注入，掃描器結構上看不見）。
> **過時的是機制描述，不是那次事故**——那次事故正是現在這道「載得回來才算數」的由來。
> 🔎 **驗收單一定要有這一句**：「去找這一包自己有沒有長出它要消滅的那個形狀」。
> 2026-08-06 六包，**六包全中**，而且全部是驗收抓的——命中率比逐條檢查驗收條件還高。
> 🔴🔴 **2026-08-17 最貴的一條:落檔不是緩解措施。**
> docling 遠端化那包走了**九輪審查、兩票獨立、1634 個測試全綠、每輪都抓到真缺陷**——
> 然後有人第一次**把映像跑起來、POST 一份真的 PDF**:`422 parse failed: ImportError`。
> `docling[easyocr]` 拉進非 headless 的 opencv,而 Dockerfile 的 apt 層沒有 `libxcb1`／`libgl1`／`libglib2.0-0`。
> **九輪裡所有的綠,都是綠在 wire 兩側各自的假物件上。**
> 🔴 **稽核長自己的檢討(原話,比缺陷本身有價值)**:
> > 「我把『沒被執行過』轉換成一條**寫下來的邊界**,再把『寫下來』當成緩解措施。
> > **落檔不會阻止部署,它只證明我知道——它保護的是稽核者,不是平台。**」
> ⚠ **而規則早就在這份檔案裡**(下一段那條「用出貨啟動字串起一次」)。第一輪用它抓到「沒有模組層級 `app`」,
> **然後就把那一格當成做過了,再也沒套用在整個映像上**。**不是不知道規則,是用過一次之後當成已結案。**
> 👉 **兩條可執行的結論**:
> ① **未驗清單不該是合併的附件,該是派工單。** 把清單上的一格關掉,常常比再審一輪值錢
> (九輪 vs 一次 `docker run`)。
> ② **任何新服務,交付前必須真的啟動一次、並執行它的主要功能一次。**
> **`docker build` 成功不算、測試全綠不算、兩票通過也不算。**
>
> 🚪 **2026-08-17 新增（一天內同一形狀出現兩次）：驗證入口必須是使用者的入口。**
> ① **服務類交付,一定要用出貨那個啟動字串起一次**——不能只驗工廠函式叫得起來。
> docling 服務 18 條測試全綠、兩張審查票都說接縫對得上,而 `uvicorn app.main:app` **載不進去**:
> `main.py` 只有 `def create_app(`,沒有模組層級的 `app`。**所有人都用「建構物件」驗,出貨用「字串路徑」啟動。**
> ⚠ 守衛測試要**從 Dockerfile 讀出那個字串**再解析它;手寫一個等價字串守的是「某個字串載得進去」,
> **不是「出貨用的那個載得進去」**——兩者在測試碼裡長得幾乎一樣,而差別正是那條 CRITICAL。
> ② **compose 類驗收要「重現操作者實際打的那一行」,不是照清單跑幾種叫法。**
> 同一天,`dev.yml` 單獨 `config -q` 壞掉而**兩張獨立的票都沒抓到**,因為驗收單只點名了疊加那一種;
> 補成「五種叫法」之後**又漏一格**——`-f infra/compose/platform.yml` 少了 `--project-directory .`
> 會讀不到 repo 根的 `.env`。**同一形狀當天出現四次,每一次的補法都是「把清單加長」,而每一次清單又漏。**
> 🔴 **清單會漏,「重現實際打法」不會。**
> 🔴 **這一條的教訓比缺陷大:獨立的兩張票擋不住「共用的規格本身有盲區」。**
> **驗收單是一張免責令——被它赦免的那一塊,從此沒有人會再看。**
> 👉 寫驗收條件時要問的不是「該驗什麼」,是**「使用者實際上是從哪裡進來的,而那些入口我列全了嗎」**。
>
> 📋 **守衛的涵蓋範圍如果是一份手寫清單,它遲早漏。2026-08-17 一天之內同一結構出現四次:**
> ① 治理中心 `裸 data.detail` ratchet 掃的是**四檔手寫清單**——新違規長在清單外,守衛不會更紅;
> ② 例外類別的**捕捉點清單**——漏了 6 個呼叫點裡的 2 個;
> ③ compose 驗收的**叫法清單**——補到五種還漏 `--project-directory`;
> ④ `severity` 對照表——**五筆手寫**,零反射,**新增第六顆時守衛全綠**。
> 🔴 **而第 ④ 格真正的傷害不在守衛,在它旁邊那句宣稱**:docstring 寫著「新增一分類會被迫更新那張表」,
> **而那句話就寫在它自己保護的那個欄位旁邊——可信度最高的位置。**
> 👉 **守衛少覆蓋一格,代價是「有些事沒被看」;宣稱它覆蓋了,代價是「下一個審的人不會再看」。**
> **後者貴得多。**
> ✅ 處置優先序:**能用反射／窮舉自動維護的,就不要留一份要人記得更新的清單**;
> 真的只能手寫時,**把「這份清單不會自己長大」寫在清單旁邊**,不要寫成「會被迫更新」。
> ⚠ **驗收判準也要跟著換**:不是「測試綠」,是**「加一顆沒進清單的東西進去,它會不會紅」**。
>
> 📥 **窮舉的基準是「這個消費者實際收得到什麼、各自是誰發的」,不是「我想得到的分類」。**
> 2026-08-17 docling 客戶端把 HTTP 狀態碼分成三域,稽核問「有沒有第四類被塞錯」,**答案是四個**。
> 🔴 **而抓到它們靠的不是把邊界列得更長,是換基準**:
> ① **數 `raise HTTPException` 只數到「作者寫出來的」**——**FastAPI 的路由自己會發 404 與 405,一行程式都不用寫**;
> ② **有些碼只有「路上」發得出來**(代理／nginx 的 404／405／429／408／3xx),
> **而客戶端沒有任何辦法分辨這個碼是服務發的還是路上發的**,分類卻假設每個碼都出自服務。
> 📌 **`3xx` 從頭到尾沒有人列**——作者、指揮官、稽核在讀到分支結構之前都沒想到
> (`httpx` 的 `follow_redirects` **預設 False,與 requests 相反**,所以重新導向真的會抵達那段)。
> ⚠ **與 [[盤點三份清單]] 同族**:呼叫點／實際抵達的輸入**可以窮舉**,捕捉點／想像的分類**只能靠回憶**。
> 🔴 **另一半教訓:分類一旦拿來當可觀測性的軸,分錯一格就連告警一起錯。**
> 那一輪把 HTTP 狀態碼與 log 等級都綁在 `severity` 上是對的決定,**但它讓「分錯類」的代價變成兩倍**——
> 404 被歸成 `warning` 之後,**全院每份文件都失敗,而三級 log 與 5xx 告警一個都不會亮。**
> 👉 **分類不完備時,要把殘餘風險寫成一句話落檔**(「代理若發 400 仍會被當文件錯」),
> **這樣它是已知限制,而不是下一個人以為的完備。**
>
> 💊 **派工單裡寫的修法也是假設——處方本身可以就是洞。**
> 2026-08-20:派工單指定「改 `pytest.importorskip("easyocr")`」,作者照做、審查抓到:
> importorskip 對**任何** ImportError 都 skip,**包含「裝了但缺 libxcb/libGL」——
> 正是 08-17 那次 422 事故現場的狀態,也正是這條守衛存在要抓的那一格。**
> 👉 與「審查者給的修法方向是假設」同一條規矩,適用面擴到派工單:**傳不變式,不要傳處方**;
> 非傳處方不可時,標明它是假設。此格的不變式:「沒裝→skip;裝了但起不來→必須紅」。
>
> 🔢 **一個碰巧正確的窮舉器,跟一個正確的窮舉器,在輸出上長得一模一樣。**
> 2026-08-20 資安官自陳:一天內自寫三支窮舉工具全部吐錯數字,病根同一個——
> **窮舉器假設母集合的成員長成自己預期的樣子**(regex 把 session UUID 當 commit hash、
> `**/` glob 要求至少一層目錄所以漏根層檔、import 解析只解絕對匯入而那檔 11 條全是相對匯入)。
> 🔴 其中 glob 那支**answer 剛好對**(csp 恰無根層 .py)——同一寫法在 anila-core-router 就漏 `main.py`。
> 👉 規矩不能寫成「檢查窮舉器對不對」,要寫成:**① 先實看母集合成員長什麼樣(不要假設);
> ② 用一個已知的例外反驗規則(known-exception counter-test)。**
> ⚠ 三次全靠「數字與預期不符所以去查」救回——**這條防線依賴查帳的人剛好有預期,
> 對沒有直覺的母集合它不存在。** 與 [[要數字不要要警覺]] 同族:數字擋不住時,要例外案例來擋。
>
> 🧯 **新增例外類別／錯誤碼,消費者有三種,盤點要三份清單:碼／型別／屬性。**
> 2026-08-17 docling 那包連踩兩次:① 新類別做成 `ParseError` 的 **sibling**,
> 於是 `except ParseError` 的接縫接不到它(稽核只數了「誰比對錯誤碼字串」,漏掉「誰 `except` 哪個型別」);
> ② 補上型別那份之後,**又長出第三種**——`_ingestion_error_status` 讀 `exc.retryable`,
> **屬性的消費者**。它一寫出來,**每一個既有子類都當場被指派了一個 HTTP 狀態碼。**
> 🔴 **而更硬的判準是換基準,不是把清單加長**:
> **盤點的基準要是「拋出它的那個函式有幾個呼叫點」,不是「我想得到的捕捉點」。**
> **呼叫點可以窮舉,捕捉點只能靠回憶。** 實例:`extract_text` 有 6 個呼叫點,兩份清單只涵蓋 4 個。
> ⚠ **加寬 `except` 的型別範圍時,一併問「這個分支的行為是為誰校準的」**:
> 那個分支原本只承接 `severity="warning"` 的 `ParseError`,放寬成 `IngestionError` 之後
> **開始承接含 `severity="critical"` 的成員**,而且對每個未來新子類自動生效。
>
> 🗄 **測試跑 SQLite、生產跑 PostgreSQL——任何依賴「引擎行為」的守衛在這裡結構性失明。**
> 2026-08-17 實測到的具體差異:**PostgreSQL 在交易裡一句出錯之後,後面每一句都被拒絕**
> (`current transaction is aborted, commands ignored until end of transaction block`);
> **SQLite 出錯之後照樣執行下一句。**
> 🔴 **後果**:一個 `finally` 裡的清理 SQL,在生產上**自己也會拋**,而
> **Python 的 finally 拋出的例外會取代正在往上傳的那一個**——**維運看到的是
> 「transaction is aborted」,不是真正的原因**。而測試在 SQLite 上**永遠綠**。
> 📌 這個 codebase 自己寫過這條理由:`services/ingestion-worker/.../handlers.py:591-593`
> 「Best-effort — … (**would mask the original error**)」。**知識在樹上,只是沒有被套用到新寫的地方。**
> 👉 **house pattern:清理／best-effort 的收尾一律 `try/except Exception: pass`**,
> 並**在旁邊註明「測試跑 SQLite,這一格重現不出來」**——否則下一個人會以為有測試蓋著。
> ⚠ 一般化:**凡是「錯誤之後的行為」「並行／鎖」「型別強制」「交易語意」,測試環境的 DB 都不能當證據。**
>
> 🎭 **測試替身替代的若正是修法所依賴的那個機制,它只能確認你對機制的模型,永遠不能確認機制。**
> 2026-08-17 #52(RLS 造成「待重算筆數」恆為 0)的替身 `_RlsAwareSession`,
> 對 `SELECT id FROM ingestion_collections` **無條件回 `[7,9]`**。
> 🔴 **假如那張表自己也受 RLS 保護,真機上那個迴圈一次都不會跑、兩個計數維持 0——
> 原缺陷原封不動,而替身永遠綠。替身把「決定修法成不成立」的那個問題寫成了答案。**
> 👉 稽核的處置是對的:**去跑真的資料庫**(唯讀、`BEGIN … ROLLBACK`、零 DDL),
> 查**活體 `pg_policies`** 而不是查 migration 檔,`SET ROLE csp_app` 實測四格全成立。
> ⚠ 一般化:**替身可以驗「我的碼有沒有照我想的呼叫」,不能驗「我想的對不對」。**
> 後者只有真環境答得出來,而**那正好是缺陷本體所在的那一格**。
>
> 🧪 **測試不准自己塞進它要斷言的前提。** 2026-08-17:離線閘的 online 測試
> **先 `setenv("HF_HUB_OFFLINE","0")`、再斷言它是 `"0"`** → **恆綠**,在映像把它烘死成 `1` 的真實狀態下照樣通過。
> 📌 這是 [[負向斷言要有正向錨點]] 的鏡像變體:**不是缺錨點,是錨點被測試自己放進去的。**
> 👉 兩條合起來記:**前置條件要驗(不能假設),而且不能是你自己造的(不能製造)。**
>
> 🏷 **標了 `[UNVERIFIED]` 之後就不要在旁邊下實質斷言——那比不標更危險。**
> 2026-08-17 稽核長自陳的形狀:它把「EasyOCR 會不會繞過 `HF_HUB_OFFLINE`」標成未驗(沒映像可測),
> **卻在標註旁邊寫下「氣隙保證本身沒破,破的只是操作者的認知」**。實測結果:**保證本身有洞**
> (`artifacts_path=None` ＋ `HF_HUB_OFFLINE=1` 時 `EasyOcrOptions.download_enabled` 仍為 `True`)。
> 🔴 **標註讓讀者以為你知道自己的限制,而斷言讓他們照樣採信——兩件加在一起產生的信任,比單獨一句斷言還高。**
> 👉 未驗的格子只准寫「我不知道」與「要驗它需要什麼」,**不准寫傾向**。
> 📌 反例也記著:同一份裁定被推翻之後**維持 HIGH 沒有升 CRITICAL**,理由是氣隙內下載會失敗或逾時,
> **後果是功能壞掉不是祕密出去**。**被證明錯了之後不要用加碼嚴重度來補償**,那會讓處置優先序失真。
>
> 🚨 **fail-loud 驗的是存在,不是有效。** 2026-08-19:docling 的 `.env.example` 佔位 token
> `replace-with-openssl-rand-hex-32` **以字面值實測拿到 200**——服務端「缺 token 拒絕啟動」的
> fail-loud 只驗非空。⚠ **它的代價比沒檢查更高:「拒絕啟動」這個動作本身,會讓後面每一個人
> 相信這顆值被驗過了。** 與 `nginx -t` 同族但不同格:那條是**驗錯對象**(驗語法不驗行為),
> 這條是**驗錯性質**(驗存在不驗有效)。**兩條都要記,不要合併。**
> 👉 主防線是**角括號佔位慣例** `<openssl rand -hex 32>`——**複製過去就是不能用**,
> `cp && up` 會壞掉而不是安靜地成功;服務端黑名單只是第二層,**黑名單不會收斂**。
>
> 🔬 **宣稱「測試過了」之前先跑突變檢查**：`cd apps/anila-shell && node scripts/mutation-check.mjs`。
> 這個專案六輪獨立驗收，**每一輪都找到「改一行讓功能整個死掉、而套件全綠」的位置**；
> 舊套件只抓得到 11/22。⚠ csp 那邊還沒有同樣的工具。
> ⚠ **執行順序以 `PLAN.md` 開頭那段〈九段〉為準**（擁有者 08-03 裁定，不趕上線、兩週內做完）。
> ⚠ **`AGENTS.md` 是 2026-06-22 版,大幅過時**——讀碼以 SYSTEM-MAP 與現行程式碼為準,別照它辦事。
> 舊資料夾 `~/桌面/ANILA/anila-migration-20260706/ANILA`(main＋attic/2026-07-28/*)只當**參照**,不在上面開發。
> 回覆一律**繁體中文＋台灣用語**(禁簡體、禁大陸用語)。

---

## -1. 跨專案制度(playbooks)

派工／驗收／完成判準在 `~/.claude/playbooks/`(symlink,真身＝`~/claude-harness-kit/`;10 調度、20 判斷、30 派工模板、40 維護、90 信)。路由表在全域 `~/.claude/CLAUDE.md`:**派 subagent 前讀 10、宣稱完成前讀 20**。與本檔衝突時專案檔優先。工作流編組(grok 主力、Claude 審查、sol 二票、kimi-k3 三票)照《10 §3b–3c》,不在此重複。

---

## 0. 一句話定位

ANILA = 中科院(NCSIST)**院內內網(air-gapped)的 AI 工作平台／AI Operating Layer**,PKI 自然人憑證卡登入。
🔴 **2026-08-17 擁有者更正:不要再寫「NotebookLM 式平台」。** NotebookLM 是**文件問答工具**
(你帶文件進去、它回答關於那些文件的問題);ANILA 是**別的東西跑在上面的那一層**。
**這個定位差別會改變設計判斷**——把它當工具會去優化「單次問答的體驗」,
當作業層要優化的是「接得上什麼、管得住什麼、用量算不算得出來」。
📌 **兩個並存的目標,缺一個都不是 ANILA**(擁有者原話):
① **下限像 OpenWebUI**——`docker` 部署好、模型接上去,**最低限度就能用**;
② **上限是治理**——計量、部門、分類、稽核、agent 註冊。**CSP 不是附掛的後台,它是「這東西為什麼是平台而不是工具」的理由。**
(⚠ 用語:擁有者已兩度糾正「不要再寫軍方」——這是中科院內部用的平台。)**開發線仍只有一條:`restart/from-redesign`**(工作 worktree 分支除外);舊 4 分支模型已進 attic,**不要**在 PLAN 排到之前重建部署分支。
⚠ **但遠端不只一條 ref**(2026-08-22 起):`origin/restart/from-redesign` 停在舊點,而**擁有者要求另推的快照分支 `origin/restart/from-redesign-20260822` 帶的是 8/22 當日的工作內容**。
📌 **這裡刻意不釘 SHA**:第一版寫了建立時的 `dca11560`,**而修這一行的那個 commit 一推上去就把它推走了**——**一份文件釘住它自己所在分支的 SHA,在被提交的那一刻就過期,寫的人不可能事先知道自己的 SHA。**與檔頭「寫『最新』就是寫一個保證會過期的宣稱」同病。**要當下的值請 `git ls-remote origin`。**
**在哪一條上開工看的是本地 `restart/from-redesign`,不是遠端最新的那條 ref**——快照分支是給人看的,不是開發線。Repo **2026-08-24 起為 PRIVATE**(擁有者當日改的;`gh repo view --json visibility` 可核)——**但祕密零外洩照舊**:`.env`/`*.pem`/`*.key`/`secrets/` 一律不進追蹤。⚠ **private 停止的是未來的曝光,不收回 2026-04-17～08-24 那段公開期**(期間的 clone／快取不會因為改 private 而消失;實測有利的一格:fork 數 0、無外部貢獻者)。目標:**8 月底全院上線**(PLAN.md),一人維運。

---

## 1. 內網機器拓撲(環境事實,與程式基底無關)

| 主機 | IP / 名稱 | 角色 |
|---|---|---|
| 平台主機 | `.15` = 10.53.100.15 / `anila.ai.ncsist.org.tw` | docker compose 全棧;**現為 redesign 前舊版,資料可刪、砍掉重來**(RESTART 文件 §四) |
| 模型 gateway | `.12` = 10.53.100.12 / `aiagent2.ai.ncsist.org.tw` | My-OpenAI-Frontend;`/v1` 出 gpt-oss-20b / gemma4 / nv-embed,需 Bearer key |
| MLSteam | `aiops.ai.ncsist.org.tw` | anila-agent 跑在這的 Lab(**純 http NodePort**) |
| 本開發機 | `~/桌面/ANILA/anila-restart-20260729/ANILA` | 重啟樹寫碼處;舊 dev stack 已於 07-29 授權下線,埠 80/443/4443/5433 已釋出 |

- **csp 容器沒裝 `curl`** → 測內部端點用 `docker exec <csp> python3 -c "import httpx; ..."`(curl 回空＝假陰性)。

## 2. 內網 TLS / CSPKI(踩過大坑)

- 內網 https 走 **CSPKI**:`CSPKI Root CA G1`(自簽 root)→ 中繼 → `*.ai.ncsist.org.tw` leaf;`.12` 只送 leaf。
- **`services/csp/app/services/cspki_ca_bundle.pem`**(卡登驗章那份)= csp 信任 `.12` 所需完整鏈,可直接當 `ANILA_MODEL_CA_FILE`。
- ⚠ **`SSL_CERT_FILE`(=`ANILA_MODEL_CA_FILE`)是「取代」整個信任庫,非疊加**——指到空/壞檔,csp 所有出向 https 全掛。
- 先 `openssl s_client -connect host:443 -CAfile X` 驗到 `verify return code: 0` 再接進服務。
- **`.12` 一律用 FQDN**(SSRF 私網 guard＋憑證主機名);compose `extra_hosts` 解析;`ANILA_TRUSTED_HOSTS` 放行。**`.12` 本身走 https(CSPKI),不需 http 旗標**;P0.2 的 `ANILA_ALLOW_HTTP_ENDPOINT` 是為其他純 http 模型端點(PLAN 0.2)。
- **aiops agent 是純 http** → endpoint 填 `http://...` 並設 `ANILA_ALLOW_HTTP_AGENT_ENDPOINT=1`;填 https → `WRONG_VERSION_NUMBER`。
- 平台門面:`server.pfx`(空密碼)抽 `server.crt`＋`server.key`;用 IP 連跳憑證警告是正常。

## 3. 運維 footgun

- **`docker restart` ≠ recreate**(不重載 `.env`/compose);套設定一律 `up -d`。
- csp 啟動要求 secrets 非 dev 預設值(`startup_security.py`),缺 JWT keypair → JWKS 500、登入炸;keypair 產到 `./secrets`(compose 掛 `:ro`)。
- 本機起正式 compose 用**獨立 project 名**(如 `-p anila-restart`),避免撞舊 stack 的網路/volume 名。

## 4. 當前快照(2026-08-01 凌晨,會變,動前核對)

**細節看 `PLAN.md` 的〈現在在哪裡〉、教訓看 `docs/handoffs/HANDOFF-2026-08-01.md`**——這裡只留每次 session 開頭必須知道的。

- alembic head = **`r1_0031`**。本機 `-p anila-restart` **15 容器**。入口現況(2026-08-17 實測):
  `/`、`/anila/`、`/asr/health`、`/router/health` **四個通**;
  🔴 **`/anilalm/` 回 503 是刻意的發行閘,不是故障**——本版關閉 ANILA LM(`6554fdd9`),
  nginx `anila.conf:450-467` 註解掉那段 location、回「尚未開放」頁,重開程序見
  `docs/runbooks/anilalm-release-gate.md`。**容器仍在跑,供內部測試。**
  ⚠ **舊版這一行寫「五個入口都通」(08-01 寫的,關閉之前)**——2026-08-17 一次重建驗收
  就因為照抄它而把刻意的閘判成故障。**驗收條件抄自快照時,先確認那一行的日期。**
- 測試——🔴 **判準是規矩,不是數字**:**三個套件都必須被跑**(csp、anila-shell、
  治理中心 `apps/csp-governance-ui`),而且**每一條紅都必須有歸屬**(真缺陷／環境／既有,
  各自指名)。**不要求全綠,要求沒有一條紅是無主的。**
  ⚠ **數字會漂,規矩不會**——下面的數字是用來**偵測漂移**的,不是用來當通過條件的。
  **合併會新增測試的包之後,要回來更新它們。**
  🔴 **報「既有紅 N 條」一定要附名字。沒有名字的,收貨方一律當「未驗」不當「已知」。**
  2026-08-17 作者回報 csp「10 個失敗、均為既有環境問題」——稽核用正確設定重跑:**0 failed**。
  **那 10 條不存在。** 結論(「與我的變更無關」)是對的,**但理由是錯的,而錯的理由會被下一個人繼承**:
  若沒人重跑,「既有紅 10 條」會進入口徑,**然後下次真的紅一條時數字還是 10,沒有人會發現。**
  ⚠ **同一天第三次**「作者回報的紅／掛住,在收貨方環境是綠／秒過」——**三次都不是不誠實,三次都是環境,
  而三次都只能由收貨方重跑才分得出來。** 最可能的原因是 `PYTHONPATH` 沒帶 `packages/*/src`(收集期爆炸)。
  **跑 csp 全套的正確指令(照抄,不要憑印象)**:
  `cd services/csp && PYTHONPATH="../../packages/anila-core/src:." pytest tests/ -q`
  ⚠ **2026-08-20 第三次同形**:用非指定直譯器(anaconda python3)跑會出**同一組 10 條假紅**
  (alembic/opencc/bcrypt 類),作者與收貨方都能「一致重現」——**一致重現的紅仍可能是環境紅**。
  正確口徑不是「10 條既有」,是「指定直譯器 0 failed;那 10 條只在非指定直譯器下出現」。
  🔒 **判準（不變式,不會過期）:指定直譯器跑,0 failed**(csp／anila-shell;治理中心＝除具名紅外 0)。
  📅 **最後實測（標日期讀成歷史,不讀成現況;第四次漂移後 2026-08-20 改成這個形式——
  一個會過期的數字寫成現況,就是一句會自己變假的話,與檔頭「最新」那條同病）**:
  csp **2661/73/0**、anila-shell **59 檔/706**、治理中心 **169/169/0**
  (2026-08-22 凍結窗,Reviewer 量測＋審查長獨立重跑,兩邊逐一相符;csp 兩邊都是 768s)。
  🔴 **數字要帶性質,不只帶數值**(審查長 2026-08-22):**anila-shell 那 706 含 23 支真 render(jsdom);
  治理中心 169 是 27 支測試全部把 `.vue` 當文字讀、`mount/render` 0 支**——
  **並排寫會讓人以為兩個前端的驗證強度相當。沒帶性質的數字會自己長大,跟沒帶範圍的核可一樣。**
  📌 **舊值供對照,標日期讀成歷史**:csp 2627(08-20)／2572(08-17)／2538(08-11)、
  anila-shell 366(08-11)、治理中心 144/143/1(08-17)、139/138/1(08-15)。
  **治理中心三天漂 5 條、shell 從 366 到 706**——原因不是筆誤,是**任何新增測試的包都會讓它過期**,
  這正是判準要放在規矩上的理由。
  ✅ **2026-08-22:既有紅全部歸零。** 那條紅了一個月的 `tests/healthOverview.test.mjs:190` 裸 `data.detail`
  ratchet(指著 `DashboardView.vue`,`25cdcb4f` 2026-07-31 進來)**本輪關掉,而且母集合已從四檔手寫清單
  換成窮舉 `src/**` 107 檔**(換完首跑揪出 22 檔 100 處漏網,全清)。**歸 F-9,結案。**
  ⚠ **它紅了 15 天沒人看見的原因是「治理中心從未進基線」——今天它同時被修好、
  而且是在基線裡被確認修好的。這兩件要一起記,否則下次還是會有一個套件沒被宣告。**
  ⚠ **沒裝 `node_modules` 時會多一條假紅**(`tests/testConnectionFacts.test.mjs` 載不到 `vue`)——那是環境不是缺陷。
  📌 **這一行 2026-08-15 之前只列兩個套件,治理中心從未進基線**——**沒被宣告的套件不會被跑,
  沒被跑的守衛等於不存在**。F-1 能活兩週,這是第二個原因。
  ⚠ 那個「26 個紅燈」的舊基準是**錯的數字**,2026-07-31 已修好並釘住(見 `services/csp/tests/README.md`)。
  🔴 **`packages/anila-core` 的既有紅有名字,不要再寫「同四條」**(2026-08-17 稽核長實跑
  `4 failed / 1634 passed / 6 skipped in 210s` 後釘住):
  `test_chunking_plugins.py::test_hierarchical_pop_resets_deeper_headings`／
  `test_g3_retrieval_path_uniqueness.py::test_g3_design_doc_grep_form`／
  `test_router_runtime_contract.py::test_router_non_stream_includes_anila_meta`／
  `test_router_runtime_contract.py::test_router_stream_emits_trace_and_meta_events`。
  📌 **為什麼一定要列名字**:docling 那包連續四輪的報告都寫「同四條既有」,**沒有一份寫出是哪四條**——
  而**「換掉一條、總數還是四」在報數字上長得一模一樣**。
  🔴 **一個不能被核對的基線,跟沒有基線的差別只在它讓人安心。**
- **7/30–8/01 共合併部署 73 包**。P4 全關、P2 只剩 2.1,P3 除 SMTP 寄送外全關。
- ⚠ **`.15` 尚未部署過任何一項**。本機是唯一驗證環境,**P5.5 整段未開始**。
- **待裁決集中在 `docs/OWNER-QUESTIONS.md`**(**真正未答 2 題**,**不要重問**;⚠ 數字會過期,以檔內標記為準)。
  🔴 **2026-08-22 更正:先前寫 24 是虛胖的**——**21 題的內文寫著「✅ 已答」而標題沒有標記**,
  於是用 `grep "^## Q"` 數就把它們算成未答。**數的是標記,而答案在內文。**
  已補上標記;判讀規則見該檔檔頭。
  規則:遇到需要裁決的事**不停下來等**,記進去、用最保守假設繼續、註明假設。
- **氣隙防護盤點報告在 `~/anila-private-audits/`,刻意不在 repo**(寫了尚未修補的弱點位置)。⚠ **repo 改 private 之後這條仍然成立**——理由從「任何人看得到」變成「弱點清單不該跟著程式碼一起流通」。
- 其他文件:`docs/FAKE-CONTROLS.md`(30 項)、`docs/UX-IDEAS.md`、`docs/designs/`、
  `docs/TOMORROW.md`(給擁有者的清單)。

### 本機新增的環境事實(2026-07-31)

- **語音輸入活體可用**:`cht/` mock 讀卡機 + `asr` profile。
  `up -d` **必須帶 CPU overlay**,否則 nvidia driver 錯誤會中斷整批啟動:
  `docker compose -p anila-restart -f compose.yaml -f infra/compose/asr-cpu.yml --profile asr up -d`
- **卡登本機是開的**(mock 讀卡機 + 執行時生成的測試 CA,`secrets/dev-card-ca/`,gitignored)。
  `.env` 的 CARD_* 鍵照**初裝章 §3 替換清單**辦理(`docs/runbooks/first-install-rehearsal.md`):
  信任測試 CA 類**絕不可帶**;`CARD_INITIAL_OWNERS` **必須帶、換真員編**。
- **GPU 沒接進 Docker**(`nvidia-container-toolkit` 未裝),語音跑 CPU `small`,中文有同音錯字。
- **`.well-known` 經 nginx 是 403**——`location ~ /\.` 的隱藏檔規則誤傷,不是政策。P2.1 的前置。

### 這台機器的操作陷阱(每一條都今晚踩過)

- **`docker compose up -d` 之後要 reload nginx**(已寫進 `deploy-prod.sh`)。upstream 區塊 DNS
  只在載入設定時解析一次,recreate 任何服務都會讓 nginx 打舊 IP → **全站 502 但容器全綠**。
- **治理中心在 csp 映像裡**(`infra/docker/csp.Dockerfile` 多階段 build)。改治理中心要 **rebuild csp**,
  不是 anila-ui。⚠ 查部署路徑要**從跑著的容器往回追**,不要從 repo 裡看起來對的檔案往前推。
- **`npm run build` 過 ≠ 映像建得起來**。本機借用的 node_modules 有 devDeps,映像只裝 production。
  前端要合併前用 `docker compose build <service>` 驗;平行建置的錯誤訊息不會說是哪個服務。
- **新增 Dockerfile 的每個 `RUN` 都要以同一個 shell layer 的尾端清理 DCS 注入**：直接複製
  `rm -rf /var/lib/sdcssagent /run/sisidsdaemon.pid`，規則與原因見 `services/asr-decoder/Dockerfile:8-26`；
  guard 會掃 repo 內所有 Dockerfile，只有明列非交付用途的檔案例外。
- **平行派多包前先分配 migration 編號**,並把檔案集真的列出來對。一晚撞三次(兩次編號、一次同檔)。
- **驗 API 要看 Content-Type**。SPA catch-all 會回 `200 text/html`,看起來像端點沒有保護。
  🔴 **而且它會往哪個方向騙你,取決於你的腳本怎麼消費那個 body**(2026-08-15 稽核長實際踩到):
  拿去 `JSON.parse` 會**當場炸**,你會發現;拿去 `includes('某字串')` 只會**回 false**,
  於是「這條路由回了 200、但裡面沒有我要的資料」——**一個完全反過來的結論,而且沒有任何錯誤訊息**。
  **寫驗證腳本時要先斷言 Content-Type,不要靠「剛好用了會爆的解析方式」。**
- **改 bind-mount 的單一檔案,內容不會進到容器裡**。Docker 用 inode 綁定,而 git 改檔是「建新檔取代」
  (新 inode),容器還抓著舊的那個——**沒有任何錯誤訊息**,只是你的修改沒生效。
  nginx 設定就是這樣掛的(`infra/nginx/anila.conf` → 容器的 `default.conf`),
  改完要 `up -d --force-recreate nginx`,不是 reload。⚠ 也不要把檔案 `docker cp` 進 `conf.d/`,
  那會變成第二份設定檔然後 `resolver` 重複宣告 → 語法檢查失敗(2026-07-31 踩過)。
- ⚠ **nginx 容器的健康檢查原本是 `nginx -t`**——那只驗設定檔語法,所以 nginx 在狂噴 502 時
  照樣顯示 healthy。已於 2026-07-31 改成真的發 HTTP 請求。**這是今晚三次「容器全綠但使用者
  進不來」裡最根本的那一個。**

### 這個專案最貴的四條教訓

1. **不要把系統越搞越嚴。** 上一輪 25 天工作被整包放棄,直接原因就是安全限制越加越複雜。
   擁有者原話:「過於嚴格我也不會想維護,開發者不會想掛 agent,使用者也不想用。」
   加限制前先問:擋掉的是誰／被擋的人怎麼自救／**它會不會在未來擋住我們自己**。
2. **驗證要驗行為。** 只有眼睛能判斷的就渲染出來看(浮水印深色主題的結論與審查猜測相反);
   「有呼叫 close()」不算證明,要量到連線真的回到池子;
   **把 production 改動還原回去還會過的測試,等於不存在**(今晚抓到三條)。
3. **審查者給的修法方向是假設不是解答。** 派修訂輪傳**不變式與驗收情境**;
   同一缺陷兩輪關不掉就問「這兩個目標是不是互斥」。
4. **靜默成功比報錯危險。** 26 個「按了、沒報錯、什麼也沒發生」的控制項,
   其中兩個是使用者以為鎖住了存取但沒有。**任何讓使用者以為發生了什麼的控制項,
   都要確認後端真的收得到那個意圖。**

## 5. 鐵則

- **祕密零外洩**(repo 2026-08-24 起 private,**這條不因此放寬**):`.env`/`*.pem`/`*.key`/`secrets/` 不進追蹤;容器掛載不看 gitignore,新增 RW 掛載要想清楚。**private 只是少一層讀者,不是一層防護。**
- **端到端驗證用對方法**:別只看 status code(SPA catch-all 回 200 text/html → 驗 Content-Type);取資料走正式 API＋auth,不直連 DB。
- **別腦補成 bug**:功能按 spec ≠ bug;先客觀呈現,讓 user 判斷。
- 前端驗 `npm run build`(非只 tsc);後端 pytest(本樹尚無 venv,暫借舊樹 `services/csp/.venv` 的直譯器＋`PYTHONPATH=packages/*/src`,建好自己的 venv 後改用)。
- SSRF guard、卡登驗章、JWT 信任錨不可弱化(P0.2 的 http 旗標分域是擁有者拍板的例外,紀錄在 PLAN.md);改 schema 必加 alembic migration;runtime DB 用 `csp_app` role(非 superuser,否則繞過 RLS)。
- commit/push 只在 user 要求時。
