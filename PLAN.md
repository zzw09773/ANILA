# 到 8 月底上線的計畫

> 依 [SYSTEM-MAP.md](SYSTEM-MAP.md) 與現行程式碼的實際差異排出。
> **目標:8 月底全院上線。** 一個人維運。
>
> **這份文件是計畫,不是紀錄。** 表格只寫「要做什麼」與「狀態」;
> 已完成項目的細節、踩過的坑與教訓,一律在文末〈已關板紀錄〉,不佔計畫表的版面。
>
> ⚠ **執行順序(擁有者 2026-07-30 裁定)**:Open WebUI 區塊 → **P4 → P3 → P2**。
> P2 移到最後不代表可略過——上線前仍必過。3.1 備份是「現在就有的風險」,性質不變。
>
> ⚠ **加限制之前先問三件事**(擁有者 2026-07-30:上一輪 25 天的工作被整包放棄,
> 直接原因就是安全限制越加越複雜):①擋掉的是誰 ②被擋的人怎麼自救
> ③**它會不會在未來擋住我們自己**。第三個最容易漏——端點位址遮蔽花五輪封了七個出口,
> 最後擋住的是自己要撿回的功能,2026-07-30 已放寬。
> 同一個屬性冒出第三個外洩出口時,該問的是「這個保護值不值得」而不是「怎麼封第三個」。
>
> ⚠ **每一包收貨要交一句「留下什麼需要長期照顧的東西」**(擁有者 2026-08-05 補充:
> 「要新增也可以,但重點是系統不要越來越複雜,導致後續難以維護」)。
> **軸線是維護成本,不是加或減。** 新增只要有人願意照顧就沒問題;
> 真正會出事的是**沒有人知道自己接手了什麼**。
> 具體要點名的形狀(這一天全部踩到過):釘在字面字串上的錨點(會靜默失準)、
> 沒設環境變數就整批跳過的測試(看起來有覆蓋率、實際上沒有)、
> 一次編輯就能無聲打破的效能不變式、必須跟著文案改的正規表示式。
> **這句話寫進交接,不是寫進 commit message。**

---

## ⚠ 執行順序(擁有者 2026-08-03 裁定,取代下方 07-30 那條)

不趕上線,兩週內做完為準。**九段,依序:**

| # | 段 | 內容 | 狀態 |
|---|---|---|---|
| 1 | **把手上的做完** | 測試地基、CSP Triton、插話、專案入口磁貼、知識庫密等、ASR | ✅ 08-05 |
| 2 | **合併** | 八條分支 | ✅ 08-05 |
| 3 | **開工修復** | ~~治理中心 11 個靜默控制項~~(分診證實**早已全修**)、健康總覽假訊號、PII 誠實化→**拆掉遮罩**(Q32)、anilalm RAG 靜默退化、索引錯配靜默全空、**憑證偵測＋四條規則精度**(Q36) | ✅ 08-06 |
| 4 | **安全** | CSRF 繞過＋starlette 1.3.1 ✅ 08-06;csp／ingestion-worker／pptx-renderer 非 root 化＋所有權對齊腳本＋`.dockerignore` 整類修正 ✅ 08-06 晚(三輪實作、三輪驗收) | ✅ 08-06 |
| 6 | **檢視套件是否為穩定版** | 不只看有沒有 CVE,要看版本本身穩不穩(**移到 5 之前**,見下) | ⬜ |
| 5 | **wheelhouse** | 進氣隙前必須。沒有它,內網之後任何套件問題都動不了 | ⬜ |
| 7 | **重掃、重打包** | P2.6 全項重掃(Q22 已裁決不縮範圍)＋ trivy 掃 OS 層 | ⬜ |
| 8 | **部署包＋部署文件** | 重建映像、重跑一次全新安裝驗證 | ⬜ |
| 9 | **使用者操作手冊(HTML)** | P5.1,三千人用。擁有者指定 HTML | ⬜ |

> 💡 **第 6 段已移到第 5 段之前**:wheelhouse 是把所有 wheel 抓下來凍住;
> 若之後才檢視版本並決定更換,整包要重抓。先定版本、再凍 wheelhouse。
> (08-06 的 starlette 升版就是這個順序的實例——**趕在凍結前把已知有洞的版本換掉**。)

### ⚠ 第 8 段之前必須關掉的（08-06 盤點,都不在原本九段裡）

這些是第 3、4 段做的時候**量出來**的,不是猜的。**沒關掉就重建映像等於把它們封進交付包。**

| | 事情 | 為什麼卡在第 8 段之前 |
|---|---|---|
| ✅ **匯入** | ~~embedding 請求零批次切分~~ **08-07 關板**:每批 32 段(0.64 秒 vs 35 秒預算,約 50 倍餘裕),CSP 一行未改。⚠ **計費語意變了**:大文件失敗以前計零,現在已成功的批次會計費、重試再計——實測 1.80×／2.60×,上限嚴格 <3×,數字已印進 `.env.example`、runbook、程式 | 驗收 2 輪＋微修 |
| ✅ **匯入** | ~~掃描 PDF 的 OCR 永遠不觸發~~ **08-07 關板**:佔位符先剝除;門檻改成「重複出現的裝飾行不算文字」(舊的字數下限會被每頁 39 字的密級標示打穿);旗標**原本根本沒接進 compose**。OCR 的四種損失(超頁截斷、分頁標記、圖片佔位符、參照斷根)全部量進 `metadata['ocr_losses']` | 驗收 2 輪＋微修(首輪 REJECT:測試把有損語意鎖成正確);⚠ 預設仍 OFF |
| ✅ **資料** | ~~`embedding_model` 大小寫碰撞~~ **08-07 關板**:寫入端存 registry 拼法;`r1_0032` 就地改寫四張表(**含 `document_chunks.embedding_source_model`,那才是檢索過濾的欄位**)。migration **實測跑在非 superuser＋FORCE RLS 下**(否則會靜靜改 0 列,#52 的形狀);歧義／未註冊列跳過並**逐表回報幾列沒修** | 驗收 2 輪＋微修;⚠ 大小寫不敏感比較**必須留著**(第 8 條未關) |
| ✅ **檢索** | 🆕 ~~引用頁碼是錯的~~ **08-07 關板**(驗收挖出、獨立成包):圖片佔位符被當成「頁」,10 頁×3 圖 → 報成 40 頁,**十筆引用九筆錯**;另一個獨立偏移是空白頁被過濾掉。⚠ **已匯入的文件頁碼仍是錯的**,而且 chunk 邊界與向量也錯 → 只能重新匯入,不能改號 | 驗收 2 輪＋微修 |
| ✅ **匯入** | ~~DOCX 的表格全部被搬到文件最後~~ **08-06 深夜已修**(wt/docx-tables):body 順序單迴圈,預設 chunker 實測表格與引導句同 chunk 0/3→3/3;`w:sdt`/`w:ins` 靜默丟棄已釘測試＋每份文件一則彙總警告 | 驗收 2 輪 ACCEPT;⚠ 映像未重建前不到達執行環境 |
| ✅ **設定** | ~~`ALLOWED_HOSTS` 等於關著~~ **08-06 深夜關板**:allowlist 移到最外層、compose 單一旋鈕、**開關那一行有守衛測試看著(刪掉即紅)**、格式錯誤開機即拒(不再 healthy-but-500)、runbook 的「確認有開」實測雙向可用 | 驗收 2 輪(首輪 REJECT:開關無守衛);⚠ nginx `$is_anila_host` map 是最後一份手抄本,會先腐 |
| ✅ **設定** | ~~ingestion-worker 要 recreate 不是 restart~~ **08-06 深夜關板**:追查證實 deploy-prod 四條路徑**本來就全是 recreate**(沒發明改動);規則落 `docs/runbooks/restart-vs-recreate.md`,含實測(restart 後 env 仍舊值、容器 ID 不變) | 純文件包 |
| ✅ **映像** | ~~build context 吃進產物、黑名單補不完~~ **08-06 深夜關板**:`scan-image-artifacts.sh` 建後掃描已接進匯出路徑(build 後、save 前、無跳過旗標);檔名家族＋**內容規則凌駕白名單**(私鑰藏在白名單路徑也抓)＋自我測試(掏空規則/白名單長歪/抽檔沒做完都大聲死) | 驗收 3 輪 ACCEPT;掃不到 ENV/history 層,檔頭誠實列明 |
| ✅ **映像** | ~~匯出閘門是紅的~~ **08-07 凌晨關板**:三張映像清乾淨(修在源頭,掃描器一個字沒動)。⚠ 真根因不是 COPY——**本機 Symantec DCS 在 RUN 執行當下把自己的目錄注入容器可寫層**(用零 COPY 的映像重現);它同時讓 `docker save` 失敗,**約六分之一的建置會產出根本匯不出去的映像**。清理只能寫在會被注入的那一層(後補 `rm` 修不掉已 commit 的上層),所以匯出閘門**加了 `docker save` 檢查**——掃描器結構上看不見這一類 | 驗收 2 輪(首輪 REJECT:驗收自己重建的 codeserver 存不出來);⚠ **儀式沒有強制力**:新 Dockerfile 漏抄那幾行,只有匯出當下才會發現 |
| ✅ **映像** | ~~上游 base 層的東西只能遮不能刪~~ **08-07 裁決:兩者都豁免**(Q38)。codeserver 那把是**公開 npm 套件的測試夾具**;asr 的 `bootstrap.log` 逐行查過是 **Canonical 2024-10-16 建 base 時的 debootstrap 紀錄**(零筆我方資料、零筆祕密、零筆內網位址)。⚠ 別跟 asr 的 **IDS 日誌**搞混——那個嚇人的已經修掉了,不屬於 Q38 | 不換 base、不壓平 |
| ✅ **交付包** | ~~08-03 交付包的 csp tar 內含測試 RSA 私鑰＋25MB 日誌~~ **08-07 裁決:銷毀,已執行**(Q37)。該 tar 已刪,同目錄留 `00-這包不能用了.md` 說明其餘 tar 也落後 95 個 commit。⚠ **`02-weights/` 7.7 GB 權重沒動**;`CHECKSUMS.sha256` 刻意不改,讓校驗失敗指出「這包已作廢」 | 第 8 段重建全部映像出新包 |

### ⚠ 一條不在段落裡、但每次動到它都要當紅線審的

**`apps/anila-shell/src/data.jsx` 的 `BLOCKING_FAMILIES`。**
偵測規則會從兩端衰退——「負空間」判斷會隨表單改版一次一個樣本地重新喊狼，
廠商前綴會安靜地對新金鑰格式失明。**兩種衰退可以承受的唯一理由，是憑證不能擋人。**
所以那一行決定了衰退的代價是「一條白跳的橫幅」還是「一則送不出去的訊息」。
**任何把它放寬的改動，用改送出路徑同等的規格審。**

### 📋 部署／內網才能做的量測（第 8 段之後,P5.5）

- **對真 `nv-embed` 重跑稀疏表單量測**(腳本已寫好,切一個環境變數即可)——現有數字來自代理模型,**排序方向可信、絕對值與 0.3 地板必須重校**。
- **拿一份 ≥200 頁的真實文件跑完整 embedding 鏈路**——那是全管線**唯一沒有活體證據**的環節。
- **上傳三到五份真規範**,看附件標題行有沒有套 Word 標題樣式(決定附件表單找不找得到)。
- **五個入口 smoke**,在 starlette 1.3.1 的新映像上。
- 🆕 **對真 `nv-embed` 校準 ANILA 規章檢索的分數門檻**。⚠ **這件事今天只能用 API 做,沒有畫面**
  ——設計 §7 寫的那個校準視圖**沒有被任何一個 task 承載**(Task 4 出的是後端:設定存
  `platform_settings`、三個端點、migration `r1_0034`,零前端)。已出貨的是三個 **admin 限定**端點:
  - `GET /api/institutional-kb/threshold` — 目前生效值 ＋ `calibrated` 旗標(`false` ＝ 還沒有人量過)
  - `PUT /api/institutional-kb/threshold` — 改值,**下一個請求就生效**(讀取每次真的查 DB,刻意不做快取),留稽核
  - `POST /api/institutional-kb/preview` — 拿一句測試問句跑**正式那條檢索路徑**,回命中的內容與**實際分數**

  ⚠ **preview 會套用目前門檻**(它要誠實反映使用者看到的東西),所以校準是三步:
  **先 `PUT` 成 0 → 跑幾句 preview 看分數分布(相關的落在哪、不相關的落在哪) → `PUT` 成中間那個值**。
  ⚠ **預設 0.3 是用替代模型量的**(同上一條),內網換上真 `nv-embed` 之後那個數字沒有意義,要重量。
  ⚠ **換嵌入模型＝門檻已知失效事件**——這是本平台唯一「換模型就必須重新校準」的數字,每換一次都要重來。
- 🆕 **校準時 trace 要看得見「每個庫檢索回幾段」**——門檻調對了沒有,只看最終答案看不出來,
  要看到**被門檻擋掉幾段、留下幾段、來自哪個庫**。
  ⚠ 目前**只有非串流回應的 trace 帶得出 kb 段落**;串流路徑的 kb trace 是空的(`"trace": []`),
  **校準要走非串流那條看**。

**與 07-30 那條的關係**:07-30 的「OW → P4 → P3 → P2」講的是功能區塊的先後,
本段講的是**收尾階段**的先後,不衝突;衝突時以本段為準。

---

## 現在在哪裡(2026-08-01)

**alembic head `r1_0031`;本機 15 容器全綠;五個入口都通;csp 測試 1431 過/13 略/0 失敗
(而且**站哪個目錄跑都是這個數字**,08-01 修掉 cwd 相依後基準才可信);
anila-shell 387 過。08-01 再合併並部署 6 包,**全部只在本機,`.15` 仍一項未部署,尚未 push**。**

### 2026-08-01:六包(擁有者四個裁決 ＋ 核對時挖出的兩件)

**四個裁決全數結案並上線**:
①編輯沒改字**可以重送**(原本靜默沒反應;順帶修掉「重送後同一則回覆畫兩次」的既有顯示 bug——
7/30 就在,重送打開才現形;存檔失敗現在會浮紅色橫幅,不再靜默);
②`.well-known` nginx 例外**已上**(只放行 `jwks.json` 一條,其餘隱藏檔照擋);
③Q17 **留憑證、去識別**(憑證位元組不動、斷言改從憑證自取值;順帶全樹清掃——
那位真人持卡者的姓名/工號/信箱/卡號在整個 repo 現在是**零**);
④**P2.1 方向拍板:拿掉 `csk-`,agent 身分全面短效簽章**(見 P2 表)。

**核對時挖出的兩件**:
- **測試套件「站哪個目錄跑結果不一樣」**:交接寫 1431/0、核對跑出 3 紅,**兩個都是真的**——
  `Settings` 用相對路徑吃 `.env`,負向測試又沒釘自己的前提。已修(旗標成對釘死＋
  測試 DB 改 per-session 臨時檔),`tests/README.md` 基準重釘。
  順帶拆掉一顆未爆彈:`.env` 哪天翻成卡登唯一模式,443 支測試會整批起不來。
- **P2.1 內網環境讀出**(擁有者在 Lab 實測):Lab 打得到 `.15`;`.15` 端的是**正確的 CSPKI
  萬用字元憑證**;Lab **缺信任錨**(佈 `cspki_ca_bundle.pem` 即解);**FQDN 尚未向網管申請**
  (要送,P5.5 前置;下來前 agent 用 `extra_hosts`,同 `.12` 前例)。**不需要釘公鑰備案。**

### 完成度

| | 狀態 |
|---|---|
| **P0** | ✅ 全關 |
| **P1** | ✅ 全關(1.6 需進內網) |
| **P2** | 2.2 隨 G9、2.3、2.4、2.5、**2.7 稽核帳防竄改 ✅**;**2.1 後端已全合(08-01)**,剩前端接線與拆舊發行面;**2.6 待重跑掃描** |
| **P3** | 3.1 備份(含還原演練＋還原後的擁有權驗收)、3.2 偵測半、3.3 健康總覽、3.4 回饋頁(**可匯出 CSV**)、3.5 日誌上限 ✅;**3.2 寄送等 SMTP** |
| **P4** | ✅ 全關——**4.4 升密即刪、4.5 記憶收斂到同一個對話框已於 07-31 補上** |
| **OE-1～OE-4、OW-1、OW-3** | ✅ 全關 |
| **G9 對話密等標記介面** | ❌ **未做**——全庫無任何 API 可把對話設為營業秘密或機密(`/classify` 硬寫「密」),**待擁有者定義誰能設哪些級**。本表原記「✅ 全關」與同檔 OE 表自己寫的「待擁有者定義」互相矛盾,08-01 查證後更正 |
| **D1** span/trace 後端 | ✅ 退場 |
| **撿回** | 心智圖、ASR(**語音輸入活體可用**)、FLUX ✅;**origin 邊界施工中**(2026-07-31 擁有者實測:CSP 建的知識庫在 ANILALM 看得到) |

### 2026-07-31 白天:15 包(擁有者的 11 件 ＋ 順路挖到的五件)

**擁有者交辦(11 件中 10 件已關,Q12–Q16 待裁決)**:部門三層(問題在 UI 沒有上層欄位,不是後端)、
回饋 CSV 匯出、第一次點擊被畫面擦掉、假分支、移除產出中心、麥克風(早就寫好,是服務沒開＋認證壞)、
P2.7 稽核鏈、Q7 派工評估與廉價修正、anila-core 兩條分支、同事名單＋真的能接手的交接、備份路徑。

**順路挖到、比原題目更嚴重的五件**:

| | 原本會怎樣 |
|---|---|
| **權杖撤銷永久鎖死** | 改一次密碼 → 那個帳號**再也不能用 Studio 與語音**。csp 自己的登入還是好的,所以看起來只是「登入了但半個平台不認我」 |
| **官方 CLI 註冊 agent 一定 422** | 文件教的指令從來沒有可能成功 → **零個 agent** → router 從沒派過工,卻每則訊息都在付那次判斷 |
| **還原備份會弄壞平台** | 擁有權被壓平 → 開機 `/health` 200、容器 healthy、**沒有人登得進來** |
| **入口層 502 的真正病因** | 前一次的修正治的是另一個病因;四個 upstream 仍在載入時釘死 IP |
| **測試套件本身不可信** | 「26 個紅燈」是錯的數字;卡片登入整條流程**沒有任何綠燈守著**,還有一支空綠測試 |

### 今天新查出的缺陷(P4+ 區)

**X.1 連線池、X.2 串流沉默、X.3 時區、X.4 API key 500、X.5 span 後端、X.6 密等降級、
X.7 假控制項(13/26)、OE-2 的 B1/B3** —— **全部已修並部署**。
X.8 Router 記憶體洩漏已修,**07-31 已合併**(wt/leak,見 OWNER-QUESTIONS Q8)。

### 壓力測試(PLAN 原本完全空白)

連線池修正**在真實並發下站得住**:舊的「31 連線懸崖 → health 全滅 → >8 分鐘恢復」沒有再現;
VU=32 時連線峰值 **19**、`idle_in_xact=1`、零失敗;負載停止後 **3–6 秒**恢復。
**新破口在 VU≈64 起的 502 洪水,而 `/health` 仍 10ms、容器仍 healthy**——
報告明講**不要再調 pool**,根因是入口/單行程。腳本在 `infra/loadtest/`,runbook 已寫。

### 等你裁決

全部集中在 [`docs/OWNER-QUESTIONS.md`](OWNER-QUESTIONS.md),共 **18 題,12 題已答**(Q17 於 08-01 結案)。
未答的六題**都不擋開發**:**Q12** GPU 沒接進 Docker(要 sudo,會斷全部容器)、
**Q13** 登入頁公告拿不到(公告 API 要登入,而登入頁的人沒有權杖)、
**Q14** 記憶來源歸屬會被覆寫(升密即刪有一個漏網情境)、
**Q15** 本機卡登入是關的、**Q16** 範本命名不一致、
**Q18** 編輯重送/改字重問會把附件掉在原訊息上(修法要動後端契約,08-01 審查時發現)。

### anila-core 兩條分支:已合併(2026-07-31)

`wt/leak`(記憶體無界成長)與 `wt/core-opt`(效能)都合併了。
core-opt 是照擁有者條件先修掉兩個壞預設,再過一輪跨家審查(kimi:有條件合格,條件已補)。

### 其他文件

[`docs/FAKE-CONTROLS.md`](FAKE-CONTROLS.md) 26 項假控制項(13 已處理)、
[`docs/UX-IDEAS.md`](UX-IDEAS.md) 15 條(4 已做)、
[`docs/designs/`](designs/) 稽核帳兩家設計＋anila-core 三家對照。

---
## P0 — 讓它跑得起來 ✅ 全部關板

| # | 事情 | 狀態 |
|---|---|---|
| 0.1 | 拿掉會拒絕開機的啟動閘 | ✅ 消解——四個閘不存在於本基底 |
| 0.2 | http 模型端點改成正常情況,不是安全違規 | ✅ `657ba43` |
| 0.3 | 本機用正式 compose 完整起一次 | ✅ 13/13 healthy |
| 0.4 | 資料庫砍掉重建 | ✅ alembic 從零跑到 head 無錯 |

---

## P1 — 主線正確(三千人走的那條路)

| # | 事情 | 狀態 |
|---|---|---|
| 1.1 | **部門三層樹**:`departments` 加 `parent_id`,建 院/所/組 | ✅ `93ba157` |
| 1.2 | **用量逐層加總**:某節點的用量 = 自己 + 所有子孫 | ✅ `a2d1f42` |
| 1.3 | **單位管理員角色**:綁節點、看該節點以下、核准/停用、**看不到對話明文** | ✅ `526783c` |
| 1.4 | **批次核准**:一次選整個單位核准 | ✅ `5062c96` |
| 1.5 | **附件處理**:塞得下就整份進 context,塞不下才切塊並告知 | ✅ 已具備 |
| 1.6 | 確認 `.12` 的推論引擎與**前綴快取**設定 | ⏸ **需進內網現場** |

---

## P2 — 上線前必過(源碼掃描 + 弱點測試)

**這一段不是選配。** 上線前要過源碼掃描與弱點測試,不得有 OWASP Top 10 高風險項。

| # | 事情 | OWASP | 狀態 |
|---|---|---|---|
| 2.1 | **對 agent 的身分改用短效簽章 JWT**,agent 用 CSP 的 JWKS 驗簽 | A07 | ✅ **後端全數合併(08-01,本機未部署)**——W1 簽發/W1b/W2 驗證回程/W3/W4/W5 出向＋CA 與驗簽器下載路由。⚠ 缺口是 W2 補上的:平台原本**只簽不驗**,agent 拿新憑條回頭搜知識庫會 401。剩:①前端「測試連線」接線(進行中)②拆掉舊 `csk-` 發行面(未排)。環境已驗:JWKS 探索可行,水電三件=信任錨佈進 Lab/`extra_hosts` 等 FQDN/`.well-known` 例外(本機已修) |
| 2.2 | ~~「加密模式」改名為「列管」~~ | A02 | ✅ 隨 G9 連帶關掉 |
| 2.3 | 關掉 `/router/docs`、`/redoc`、`/openapi.json` 等匿名可讀面 | A05 | ✅ 2026-07-30,對外實測 404 |
| 2.4 | **存取控制逐條複驗**:每個 by-id 端點都確認「這個人有沒有權限碰這一筆」 | A01 | ✅ 2026-07-31 `1a8633c`,166 個可定址面逐條複驗,關掉 4 個授權破口＋3 群列舉洩漏 |
| 2.5 | 相依套件掃一次,把有已知漏洞的版本升掉 | A06 | ✅ 2026-07-30 `cf288cf` |
| 2.6 | 跑一次源碼掃描,把高風險項清掉 | 全部 | ⚠ **仍未關**。08-01 已重跑並重建清單(報告在 repo 外的 `~/anila-private-audits/p26-source-scan-2026-08-01.md`,因為本 repo 是 PUBLIC):**Python 面 HIGH 0／MEDIUM 2**,兩條都是「程式碼沒做到自己宣稱的事」,已排包修。<br>**不能就此宣告關閉,理由是涵蓋率不是 findings**:本機無 bandit/semgrep/pip-audit 且不能裝,只用 ruff 的 `S` 規則掃 Python;**JS/Vue 230 檔、shell 31 個、Dockerfile 21 個、nginx 設定、全部相依套件 CVE 通通沒掃**。把這樣登記成「掃描完成」正是本專案最在意的假完成。<br>關閉條件寫在報告末節。舊報告 `p26-source-scan-2026-07-31.md` 確認從未落地,不再尋找 |
| 2.7 | **稽核帳防竄改**(OE-2 缺口 G2) | A09 | ✅ 2026-07-31 `r1_0027`,日級雜湊鏈＋擁有權收回,維運者每月 0 分鐘 |

⚠ **2.7 比原本想的難。** `0014` 把 public schema 所有表的**擁有權**轉給執行帳號 `csp_app`,
而在 PostgreSQL,擁有者可以刪掉自己表上的 trigger、再把 UPDATE 權限授給自己。
規格 §8 的威脅模型是「防止 admin 權限人員偷偷做假」,那個人手上拿的就是這組帳號。
→ 真正要做的是**把稽核表的擁有權從執行帳號拿走**;PLAN 0.4 說資料庫可重建,現在是最好的時機。
雜湊鏈是另一層,但單獨用擋不住(能改列的人也能重算),除非鏈頭存在他碰不到的地方。

**2.1 同時是計費正確的前提**:身分能偽造,用量歸屬就能偽造,月報表就不可信。

---

## P3 — 一個人撐得住(營運)

| # | 事情 | 狀態 | 驗證 |
|---|---|---|---|
| 3.1 | **資料庫備份** + 排程 + **實際還原演練一次** | ✅ 2026-07-30,還原後列數對得上 | 從備份還原到一台乾淨的機器,資料對得上 |
| 3.2 | **告警**:平台無回應、DB 連不上、gateway 連續失敗、agent 派工失敗、磁碟快滿 → 寄 Outlook,分級 | **偵測✅／寄送待做** | 偵測與分級已做並寫進 `alerts` 表、治理後台看得到;**寄送等 SMTP 位址**(Q3)。「平台無回應」需要**外部**看門狗,內部結構上測不到 |
| 3.3 | **健康總覽頁**:一眼看出哪個服務不對 | ✅ 2026-07-30 | `DashboardView.vue`;⚠ 2026-07-31 掃描發現健康探測把 **401 當 healthy**,修正中 |
| 3.4 | **回饋看得到**:治理後台加一頁看評分與文字回饋 | ✅ 2026-07-30,**可匯出 CSV** 2026-07-31 | `FeedbackView.vue`;匯出走同一個白名單,**不含對話正文**。⚠ 2026-07-31 擁有者要改評分粒度:**讚=6–10 分、爛=1–5 分**,施工中 |
| 3.5 | **容器日誌輪替** | ✅ 50MB×7/服務 | compose 全檔零個 `logging:` 區塊,日誌無上限成長會寫爆磁碟。3.2 的「磁碟快滿告警」是治標 |

⚠ **3.1 是現在就有的風險**,不是新功能:要留半年的稽核帳、給長官的月報表,全在那個資料庫裡,而它沒有備份。

---

## P4 — 主線體驗與分級

| # | 事情 | 狀態 |
|---|---|---|
| 4.1 | **外流面門檻改成兩條線**:可做 = ≤ 營業秘密;落稽核 = ≥ 營業秘密 | ✅ `ef5ef7c`(=OE-4) |
| 4.2 | **浮水印**:受控內容全頁印「密等 · 讀取者 · 時間」 | ⚠ **只做在 anila-shell**(08-01 查證:anilalm 與治理中心零命中),規格要的是全頁。主聊天介面已達成,另兩個介面待補 |
| 4.3 | **分享改成指定的人/單位**,拿掉匿名連結 | ✅ `r1_0019`,2026-07-30 |
| 4.4 | 記憶:使用者可看可刪的管理頁;**升密時撤回已萃取的記憶** | ✅ 2026-07-31(管理頁 07-30、升密即刪 07-31) |
| 4.5 | ANILALM 的記憶改成只在同一 session 內 | ✅ 2026-07-31,收斂到**同一個對話框**(含另一個 ANILALM 對話框也算外部) |
| 4.6 | 模型**整批帶入** | ✅ `2c0fdb3` |
| 4.7 | agent **多知識庫綁定** | ✅ `r1_0017`,2026-07-30 |
| 4.8 | **主 embedding 模型設定＋自適應維度** | ✅ `r1_0018` |
| 4.9 | **綁定密等閘門**:agent 等級 ≥ 知識庫等級 | ✅ 2026-07-30 |

### 4.4 / 4.5 記憶規則(2026-07-30 擁有者裁定)

- 對話**升密之後,先前萃取的記憶直接刪除**(不是標記不可用)。
- 使用者在管理頁刪除記憶 = **真刪**。
- ANILALM 的「同一 session」= **同一個對話框**;不是關分頁,也不是登出。
- **既有跨 session 存著的記憶直接清空**(新平台,無包袱)。
- 設計原則:**明示且可檢視,勝過自動且隱形**。使用者要看得到自己有哪些東西被記住、刪得掉——
  在四級密等的軍網場域,這是治理問題不只是體驗問題。管理頁與「升密即刪」是同一個設計的兩半。

### 4.8 主 embedding 模型設定＋自適應維度(2026-07-30 擁有者裁定)

**起因**:`memory_service` 用**寫死的模型名精確比對** registry(`nvidia/NV-embed-V2` vs 實際註冊的
小寫 `nvidia/nv-embed-v2`),**記憶檢索每次都失敗**、只在日誌噴 error,使用者完全無感。
現況是**三套獨立來源**各自選 embedding 模型:`memory_service`(環境變數＋寫死名)／
每個知識庫自己存的 `embedding_model`／`ingestion-worker` 自己的設定。

- 模型頁一個「設為平台主 embedding」,admin+ 可設,取代寫死名字比對。
- **向量記來源模型**:三張向量表各加來源模型與原生維度;檢索**只取符合當前主模型的列**,
  不符的留著不用,管理頁顯示「N 筆待重算」。理由:換模型後舊向量不是壞掉而是**語意空間不同**,
  維度相同也不能比——A 模型的第 1 軸跟 B 模型的第 1 軸不是同一件事。
- **自適應維度**:設定時實際打一次 embeddings 探測真實維度並存下來。
  - **實測**:`halfvec(4096)` + HNSW → `ERROR: column cannot have more than 4000 dimensions`;
    `halfvec(4000)` 可建。現行 4000 **就是 pgvector 建 HNSW 的上限**,不是隨便挑的。
  - 原生 ≤ 4000 → **補零**到 4000。用餘弦距離,補零不改內積也不改長度,
    相似度**數學上完全等價**,排序不變、索引照用 → 換模型不必改 schema。
  - 原生 > 4000(NV-embed-V2 = 4096)→ 只能截斷,設定時明白告知。這是 pgvector 的限制。
  - ⚠ **補零把「大聲的錯誤」變成「安靜的錯誤」**:沒補零時換維度當場報錯,補零後新舊向量都是
    4000 維、會安靜地一起被檢索。所以來源模型標記是**必要條件**,兩者必須同一包做完。
- 新建知識庫的預設模型跟著主設定;**既有知識庫的 `embedding_model` 維持不准改**(現行約束是對的)。
- `ingestion-worker` 一併接到同一個設定。
- ⚠ attic 的 `1001664`(補零)**被作者自己 revert**,理由與上面同一條;正確版本是
  `301aaa7` 的 `pad_from=` 形式(只在維度等於指定值時才補),要撿撿那個。

### 4.9 綁定密等閘門(2026-07-30 擁有者裁定)

P4.7 雙票審查查出:**綁定知識庫時完全不比對分類等級**——「無機密」agent 可以綁「機密」知識庫,
使用者問它就把機密內容答進一個 latch 在無機密的對話裡,**繞過 G9 建立的密等閂鎖**。
缺口在 P4.7 之前就存在(單綁時代一樣沒比),P4.7 只是放大。

- 規則 = **agent 等級 ≥ 知識庫等級**(軍規慣例:處理某密等資料的系統須核定在該密等以上)。
  多庫綁定時,agent 等級須 ≥ 所有已綁庫的最高等級。
- 違反時 **拒絕綁定並說明原因**,**不**自動抬升 agent 等級——擁有者要可預測、不偷改已設定的值。
- ⚠ 補救路徑必須是使用者自己走得完的。若「提高 agent 等級」只有 admin 能做,dev 就會卡死在
  一個自己解不開的 403——這正是 P4.7 修訂輪在收的同一種坑。

---

## P4+ — 今天新查出的缺陷(不屬於原編號,但擋在上線路上)

| # | 事情 | 狀態 |
|---|---|---|
| X.1 | ✅ **已修** 連線池耗盡:RAG 搜尋抓著 DB 連線去打 embedding(池 10+20,逾時 30 秒);且 `expire_on_commit=True` 讓每次 commit 後的屬性讀取重新借連線 | 進行中,修訂輪二 |
| X.2 | ✅ **已修** 串流中途失敗對前端不可見:SSE 已回 200 之後才收到下游錯誤,FastAPI 無法再回錯誤 → 畫面永遠停住。**這是「hi 沒回覆」的真正病因** | 待做 |
| X.3 | **時區**:158 個 datetime 欄位只有 7 個帶時區,回應零個 `field_serializer` → 瀏覽器當本地時間解,**每個時間戳差 8 小時**(含稽核帳) | 待做 |
| X.4 | ✅ **已修** API key 到期比對 naive vs aware → 任何設了到期日的 key **第一次請求就 500**(不是 401) | 待做,一行 |
| X.5 | ✅ **已完成** D1 span/trace 後端退場:畫面已於 2026-07-30 移除;後端每則訊息 2 列 span＋2 commit 仍在熱路徑 | 待做 |
| X.6 | ⚠ 非 admin 可把 agent 密等降低(紅線) | ✅ **已修**——條件式 UPDATE,不加鎖 |
| X.7 | ⚠ 假控制項 | 分享 ✅ 隨 P4.3;交接 ✅ **已移除按鈕**(要接回需先答 Q9) |
| X.8 | ⚠ Router 記憶體無界成長:`RemoteAgentRegistry` 以 `sha256(token)` 為鍵、零淘汰,JWT 每 60 分鐘輪替 → 每小時新增約 3000 個永不釋放的條目 | **已修於 `wt/leak`,未合併**——見 Q8 |

### X.8 Router 記憶體無界成長(2026-07-30 kimi 於 anila-core 三家討論查出,已驗證)

`packages/anila-core/src/anila_core/registry/remote_agent_manifest.py:47-48`:
`_agents_by_key` 與 `_last_refresh_by_key` 是兩個純 dict,鍵是 `sha256(api_key)`(`:63`)。
**全檔零個淘汰機制**——沒有 `pop`、沒有 `del`、沒有 maxsize、沒有 LRU(實地 grep 確認 0 命中)。

JWT 每 60 分鐘輪替一次,每次輪替就是一個新的雜湊鍵 → **三千人每小時新增約三千個條目**,
每個還掛著一份 agent manifest 清單,而且永遠不釋放。

**為什麼這條特別惡劣**:它**隨 uptime 惡化**。本機測不出來、壓測跑一小時也測不出來,
上線之後平穩跑幾天甚至幾週才會 OOM,而症狀是「router 莫名其妙死掉」,
對一人維運是最難查的那種。三家討論中只有 kimi 抓到,它的原話是
「整個區域唯一隨 uptime 惡化且目前無人認領的缺陷」。

**修法**:給兩個 dict 加 TTL 淘汰或有上限的 LRU。屬小包,但**不要跟 `wt/core-opt` 混在一起**——
那條分支擁有者還沒體驗過,混進去會讓「這條到底是誰造成的」變得說不清。

### X.7 假控制項:畫面宣稱做得到,後端沒接(2026-07-30 UX 走查＋實地驗證)

**這是一個類別,不是單一 bug。** 目前確認兩例,兩例都**不會報錯**,使用者以為做到了:

| 控制項 | 畫面說什麼 | 實際送出什麼 |
|---|---|---|
| 分享的「組織內／同部門／指定人」 | 可以限定誰能看 | `createShare` 只送 `mode`/`allowFork`/`expiresAt`;`ShareCreate` **無 scope 欄**;`GET /api/public/share/{token}` 註明 **No auth required** → 任何拿到連結的人都能讀 |
| 「交給同事」交接 | 輸入同事帳號 → 送出交接請求 | `app.jsx:2355` 送 `{conversationId, note: "交接給 <帳號>"}`,**完全沒有 `to_user_id`**(後端 `handoffs.py:23` 收得到但前端沒給)→ 建立一筆交給**沒有人**的交接,收件人名字只存在一個自由文字欄位裡 |

**為什麼比一般 bug 嚴重**:兩者都**靜默成功**。使用者按下去、沒有錯誤、以為完成了。
分享那個更糟——使用者以為只給了特定同事,實際產出的是**任何人拿到連結就能讀**的匿名連結。
在四級密等的軍網場域,那是「以為鎖了其實沒鎖」。

**處置**:
- 分享 → **P4.3 做完即消失**(P4.3 正在做,兩者都要:人和單位)
- 交接 → 待做。最小修法是前端把帳號解析成 `to_user_id` 再送;或在接好之前**先把「給同事」隱藏**,
  不要留一個會騙人的按鈕
- ⚠ **標準檢查項(新增)**:任何「使用者按了會以為發生了什麼」的控制項,
  要確認後端真的收得到那個意圖。**靜默成功比報錯危險。**

### X.6 agent 密等可被降級(2026-07-30 對抗式驗收查出,實測)

`api/agents/registration.py:449` 用不上鎖的 SELECT 讀出 agent,`:505` 拿**那個可能已過期的物件**去問
`refuse_classification_downgrade`,`_common.py:166` 寫回。兩個並行請求交錯時,舊值算出來的結果會蓋掉新值。

**實測**(真 PostgreSQL、兩個作業系統執行緒、強制在 SELECT 與 UPDATE 之間卡住):
**10 次有 4 次**非 admin 成功把「機密」寫成「密」。對照組(舊的 `expire_on_commit=True`)是 5/10
→ **既有缺陷,不是連線池那包造成的**。`credentials.py:71` 同形狀。

這違反 G9 的鐵則:「**任何寫入不得降低 agent 的有效等級,除非 admin**」。
而 agent 的等級決定了它應答的對話會被 latch 到哪一級——降級成功等於後續對話全部被標低。

⚠ **修法不要只加鎖就算了**:`apply_classification` 剛示範過「加了 `FOR UPDATE` 卻忘了在
no-op 分支釋放交易」會把鎖握到請求結束。要嘛用只能往上升的條件式 UPDATE(`WHERE level < :new`,
不需要鎖),要嘛鎖了就確保每條分支都釋放。

⚠ **測試要在 PostgreSQL 上跑並強制交錯**:`with_for_update()` 在 SQLite 上是**靜默無效**的,
而 conftest 用的正是 SQLite 且所有 session 共用一條連線。驗收實測發現,不強制卡住交錯點的話,
**壞掉的程式碼也會回報 0 次失敗**。

⚠ X.1～X.4 都是實地查證的(有 file:line),不是推測。X.2 與 X.4 各自都會讓使用者看到
「系統壞了但不知道為什麼」,在三千人上線時是客服災難。

---

## P5 — 上線準備

| # | 事情 |
|---|---|
| 5.1 | **操作手冊**(我寫) |
| 5.2 | 影片腳本 + 分鏡(我寫,你錄);或逐步截圖圖解 |
| 5.3 | 分批放行的名單與節奏(小組長規劃) |
| 5.4 | 支援信箱(建議用群組信箱,不要個人信箱) |
| 5.5 | 打包 tar → 搬進 `.15` → 部署 → 驗證 |

---
## 從 attic 撿回(2026-07-30 擁有者裁定)

**前提更正**:`RESTART-FROM-REDESIGN.md` 記的 25 天工作被砍,原因是**當時長出來的把關機制鎖太死,功能是連坐**——
不是功能本身有問題,只是它們剛好落在收斂點之後。所以撿的原則是:**撿功能,不撿當時綁在功能上的閘門**
(Gate 0–6 體系、capability freeze、部署姿態契約、CRL 硬門檻——P0.1 已在做同方向的事)。

⚠ **硬規則:凡碰分類的撿回動作,一律「改寫」不是「照抄」。** 那批碼寫在 OE-3／OE-4／G9 之前,
當時複製/匯出/分享/列印/稽核五個閘門全掛在 legacy 的 `classified` boolean。照抄會把四級模型打回布林。
實例:`d4c0fab`(上傳時宣告密等)寫死**五級**且「機密」在兩邊等級不同,只能參考設計。

### 已裁定撿回

| 項目 | 成本 | 備註 |
|---|---|---|
| **心智圖**(`c46ae54`) | 11 檔 / 無 migration / 無新路由 / **零相依** | 觸及的檔案全部與基底一致,可直接套。後端僅約 46 行(既有 API 多支援一種格式)。⚠ 點節點自動提問是**繞過輸入框的第二條送訊息路徑**,日後訊息級治理要記得涵蓋 |
| **FLUX OpenAI 相容**(`ba7bd92` 等 3 個 changeset) | 44 檔 / 1 migration / 0 新容器 / **零相依** | `services/{flux2-dev,flux2-dev-agent,anila-studio}` 下 13 檔與基底完全一致,原樣可套。⚠ 三件待處理見下 |
| **ASR 語音輸入**(`fab3dd5` 等 5 個) | 50 檔 / 2 新服務 / 0 migration / 3.1 GiB 模型檔 | 擁有者定位:**給院內資深同仁用講的輸入**。氣隙可行且是設計上的預設路徑(模型不烘映像、唯讀掛載、四個離線旗標、缺檔 fail-loud)。⚠ 兩件待處理見下 |
| **知識庫歸屬 origin 邊界**(`a76ccb5`) | 40 檔 / 1 migration(1 欄 + CHECK) / **零相依** | 擁有者裁定:**ANILALM 與 CSP 本就該各自獨立**。它修的是現在還活著的 bug——CSP 建的知識庫出現在 ANILALM「你的知識庫」還附刪除鈕(`api/ingestion/collections.py` 只濾 `created_by`/`status`)。migration 只肯在**零知識庫**的環境跑,擁有者確認目前無既有知識庫→阻礙解除 |

### 撿回前必須處理(不是選配)

- **FLUX**:①migration 編號 `r1_0009` 已被部門樹佔用,要改號 ②`GET /api/models/image-primary` **明文回傳端點位址**,
  直接推翻 P4.6b 花五輪封掉的遮蔽——它不會產生衝突、會安靜貼進去然後把洞打開,**必須改寫或由擁有者明示開例外**
  ③註冊 image 型模型現在需要 owner 指定的端點位址權限,但介面仍以 admin 判斷→會撞 403,要對齊
- **ASR**:①它要改的 `infra/nginx/anila-gate2-pilot.conf` 與三個氣隙映像清冊檔**本樹不存在**,那部分工作沒有落腳點
  ②它自帶一份憑證撤銷快取的複本,而該複本**比本樹的版本新**(626 行 vs 543 行,帶 subscribe-then-replay 修正)。
  照抄會造成「同一張被撤銷的票在一個服務被擋、在另一個服務仍有效」——**該檔案自己的檔頭就寫明這不能發生**
- **origin 邊界**:它把七組 ingestion router **改成掛兩次**(`/api/ingestion` 給 CSP、`/api/personal` 給 ANILALM),
  所有知識庫 API 的網址都會動。與 P4.7 多庫綁定**互補不衝突**(origin 不進檢索/RLS/授權,綁定仍以整數 id),
  但 P4.7 先落地的話,新的綁定呼叫點也要補 `origin=` 參數(缺參數是 `TypeError`,編譯期就會抓到,不會靜默漏)

### 已裁定不撿 / 延後

| 項目 | 理由 |
|---|---|
| `/outputs` 產出總覽(`ae66df2`) | 資料來源是**瀏覽器本機儲存**、527 行內**零密等處理**、伺服器從未被詢問。一個專門跨知識庫看產出的畫面列出機密產出還附下載鈕。且該分支自己十天後(`56911d2`)已改寫為伺服器權威版——撿舊版=撿他們淘汰的版本 |
| artifact 版本歷史(`444f222`) | 標「純前端」是假象:它讀的後端欄位與整條 ANILALM↔CSP artifact 橋接本樹都沒有,要先撿一大包 blob/保存期限機制。且與 **G3「artifact 三天自刪」直接打架**(三天就刪則版本歷史只有三天)。**併入 D3 一起處理** |
| §三「把關機制」除三項外全部 | 該節文件自承「2026-07-28 當天本 agent 仍在新增機制,直到擁有者指出方向錯誤為止」。僅撿:`check_orm_pg_drift.py`(本專案 migration 多、對症)、WCAG 對比 gate(2026-07-30 浮水印深色主題失衡實證有用)、zh-TW lint(專案硬要求) |

---
## 明確不在這一輪(可延後)

| 項目 | 理由 |
|---|---|
| **Studio 產出** | 使用者已同意可延後;而且範圍縮小後(下載 pptx、不用網頁編輯器)之後做很快 |
| ISO 42001 儀表板 | 使用者說後做 |
| 真正的加解密 | 等資安中心規範 |
| 人資介接 | 單位管理員核准就足夠,介接是加值 |
| 額度硬停 | 現階段只計量不設限 |
| ~~心智圖~~ | **2026-07-30 翻案**:擁有者裁定撿回(見「從 attic 撿回」)。原理由「是瀏覽方式不是產出物」在**要從零做**的前提下成立;實地盤查後 attic 有現成實作、零相依、11 檔不含 migration,成本前提已不同 |
| Open WebUI 參照中的 Functions/Channels/Notes 等 | 見上方「參照盤點 · 刻意不做」,附理由 |

---
## 已關板紀錄(細節與教訓;計畫表只留狀態)

> 這一節是**紀錄**,不是待辦。放在這裡是為了讓上面的計畫表能一眼掃完。
> 每一條保留的是**日後會再用到的教訓**,不是流水帳。

### 分類四級與兩條門檻(OE-3 `c4bee70` / OE-4 `ef5ef7c` / G9 `1b05ea9`)

四級 = 無機密 < 營業秘密 < 密 < 機密。`r1_0003` **原地改寫**(擁有者裁決,全庫可拋),
舊識別字 CONFIDENTIAL/TOP_SECRET/ABSOLUTE_SECRET 已刪無 alias。
外流兩條線(可做 ≤ 營業秘密、落稽核 ≥ 營業秘密)全面改讀等級,boolean 只剩顯示旗標;28 條真值表測試。

**G9** 補上缺口:agent 註冊時選四級密等(取代「加密模式」開關,**連帶關掉 P2.2**),
dispatch 把該 agent 應答的對話 latch 到該等級,營業秘密層終於可達。
⚠ 鐵則:**任何寫入不得降低 agent 的有效等級**(含 legacy boolean 隱含的 floor),除非 admin;
稽核記的是有效等級的轉移,不是欄位轉移。

⚠ **時效性**:分類是單向閂鎖、沒有降級路徑。真實資料一旦用錯等級寫進去,更正要走受治理流程。
**必須在灌真實資料前處理完** —— 已完成。

⚠ **仍缺活體格**(需進內網):agent 實際 latch 一次對話到營業秘密(本機解析不到 `aiagent2`/`aiops`,
agent 進不了 approved);legacy floor 那一案無法以合法 API 構造。

### 端點位址:先收緊,再放寬(`53313af`+`16e4135`,2026-07-30 放寬)

設定權收為 **owner ＋ owner 逐人指定的人**(`r1_0015`/`r1_0016`)——這是規格變更,SYSTEM-MAP §6 已同步。
⚠ `r1_0016` 不做 backfill,**既有列沒有建立者,位址只有 owner 改得動**(含 `.12` gateway 與 router primary)。

**可見性同日放寬**:owner ＋ 同一批指定的人(可以是 admin,如小組長或管模型的人)看得到真位址;
服務 token 呼叫拿真位址;**分組金鑰整套刪除**。

⚠ **這裡有一個貴的教訓。** 位址遮蔽花了五輪才封住,已知出口有七個:
回應欄位／稽核 detail／稽核 metadata／錯誤訊息(含守衛拒絕)／網址衍生值(分組鍵)／
proxy 追蹤與失敗訊息／告警的 message 與 metadata。前四輪都在守門,窗戶一直開著。
代價還包括一個難用的介面(非 owner 的 admin 匯入選單一列一個選項),
最後**擋住了自己要撿回的功能**。
→ 放寬後全部收斂為**一個判斷函式**(`can_see_endpoint_address`,失敗時關閉)。
**日後任何碰端點的功能,不必再對七項清單,只要確認有呼叫那個函式。**

### 模型整批帶入(4.6,`2c0fdb3`)

匯入列繼承端點層事實且**預設停用待複核**(因密等上限常為空,停用才是真正在擋的那一半);
每模型事實不猜,未提供即留空並回報 `guessed_fields`;批次上限以**已建立列數**計算故重跑會前進。
**已接受殘餘**:匯入報告仍會讓 admin 得知某名稱是否屬於來源端點。
**未驗**:活體抓取、TLS 信任鏈、真實 gateway 酬載形狀——需進內網。

### 訊息樹與訊息級自訂按鈕(OW-1 `dbcdd3d`+`0bf0c49` / OW-3 `591a6fc`…`45de1b7`)

OW-1:parent_id + active_leaf 指標、branch/active-leaf/子樹 DELETE、破壞性 `/edit` 移除、
前端改以伺服器為真相、`r1_0012`。活體 e2e 10/10 含硬重載後分支保持。
留檔:切回舊分支會 canonicalize 到最新變體(規格行為)、migration 降升循環會壓平樹形。

OW-3:**宣告式**(prompt 模板＋這則訊息 → 送模型),懸浮視窗選 prompt,結果以 OW-1 手足分支回填。
⚠ **exec 面整包移除**——擁有者 07-30 與同事討論後撤回「貼 Python」需求(`r1_0014`)。
**因此 P2 掃描不再有此高風險項,不需簽風險接受書。**
撰寫權:建立=dev+;修改/刪除/指派誰能按=dev 限自己建的、admin+ 任何一筆;稽核匯出=admin+。
⚠ 寫死的三顆舊按鈕已移除,建立受治理動作前訊息列上沒有按鈕——範本見
`docs/runbooks/ow3-default-actions.md`。
透明化(`45de1b7`):能按就能讀模板;每種形狀都會開懸浮視窗(快速路徑已刪,避免揭露被靜默繞過);
說明明列三個佔位符會被替換成什麼、以誰的身分送出、平台未審核。

### agent 多知識庫綁定(4.7,`r1_0017`,2026-07-30)

junction 表取代單數 FK;`bound_collection_id` 降為**由 trigger 維護的鏡像**——
因為刪除 collection 時 junction 走 cascade、而欄位走 SET NULL,兩者會不一致到「還綁著卻回報未綁定」。
downgrade 先把集合收斂進純量再 drop 表(第一版直接 drop,會讓鏡像漂移過的 agent 靜默變成未綁定)。
寫入面改為**只驗差集**:新增要有權限、移除不用、已綁的不重驗——否則使用者多勾一個就被 403 鎖死。

⚠ **驗收查出兩個測試不是承重的**:把 production 改動還原掉它們照樣綠。
→ 此後派驗收固定要求:**說出你把哪一行改回去、確認測試會變紅**。

### URL 拼接統一(2026-07-30)

註冊位址帶不帶 `/v1` 兩種慣例並存,四個呼叫點三種假設。串流聊天把 `/v1/chat/completions`
接在已經以 `/v1` 結尾的位址後面 → `/v1/v1/...` → 下游 404 → 而且錯誤在串流開始後才到,
傳不回瀏覽器,使用者看到的是**永遠停住的畫面**。

⚠ **測試連線與健康檢查都給綠燈**:那台 gateway 先驗金鑰再看路由,連 `/v1/v1/models` 都回 401;
健康檢查打的 `/health` 永遠 200。**兩道檢查都證明不了路徑是對的。**

→ 收斂為一支共用函式(`anila_core.security.upstream_urls`),規則:**路徑帶的版本說了算,
位址上的版本段一律剝掉**,`api_version` 欄位是版本的唯一真相。
⚠ 第一版只認「路徑要的那個版本」,把 `/v1` 修好卻**把 `/v2` 弄壞**——同一個病換個版本號,審查抓到。

### 其他

- **P1.1–P1.4**:部門三層樹(深度上限 3、停用 fail-closed、name 全域唯一是保守預設,可翻案)、
  usage 讀取時聚合子樹、unit_admin 綁定表(`users.role` 與 `auth_service.py` 一字未動,以 sha256 驗)、
  批次核准(兩道界線=原始輸入 1000／實際核准 500)。
- **codeserver**:擁有者裁定必要(掛 repo＋docker extension 內網維運)。
  ⚠ docker.sock = 主機 root 等價,密碼即失守——取捨記錄在 `platform.yml` 註解。
- **n8n/gitlab 裁定保留**;gitlab 的 `.env` 必帶 `GITLAB_ROOT_PASSWORD`(omnibus 直讀 env,
  空字串在 Ruby 是 truthy,長度檢查會炸)。
- ⚠ **`ANILA_ENV=production` 與 `ANILA_ALLOW_DEV_SECRET` 無交叉檢查**——內網部署照抄 dev 值
  不會被擋,上線前自查。
- ⚠ 觀察(未處理):`UserResponse.updated_at` 非選擇性,若有資料列該欄為 NULL,
  `GET /api/users` 會 500 而非降級。

---
## 參照盤點:Open WebUI(2026-07-29 納入)

來源:`~/.cursor/projects/.../openwebui-feature-analysis.canvas.tsx`(2026-07-29)。

⚠ **先講框架落差,免得誤讀**:那份報告是寫給「**要從零蓋一套對話平台**的人」的參照清單(它自稱讀者目標是「做出自己的對話平台」),**通篇未提 ANILA、內網、氣隙、卡登**。它的 Phase 1–4 路線圖假設讀者手上什麼都沒有,對已經在跑的 ANILA 不適用,**照抄會製造假工作**。下面只留經過判斷後對 ANILA 真正成立的部分。

### 應補(真的要做)

| # | 事情 | 型態 | 驗證 | 與既有項目的關係 |
|---|---|---|---|---|
| OW-1 | ⚠ **訊息歷史樹**:`messages` 加 `parent_id`,讓「編輯重問」「重新產生」各自成為分支而非覆寫/線性追加 | 新建 | ✅ **已關板 2026-07-30**(WP-A `dbcdd3d`+WP-B `0bf0c49`;規格依據 SYSTEM-MAP:46,擁有者拍板完整功能):parent_id+active_leaf 指標、/branch+/active-leaf+子樹 DELETE、破壞性 /edit 移除、前端 revisions[] 退場改伺服器真相、r1_0012。活體 e2e **10/10 PASS**(含硬重載分支保持、sole-root/上限/ANILALM 409、附件存活、migration 降升循環)。藍圖=docs/plans/ow1-message-tree-blueprint.md。留檔觀察:切回舊分支 canonicalize 到最新變體(規格行為,UX 待擁有者過目)、降升循環壓平樹形(運維手冊)、串流結束至 persist 落定間 ~1 RTT 切換窗口(後續票) |
| OW-2 | 訊息操作列權限切細:編輯/重新產生/評分/刪除各自獨立權限位,而非只看 role | 修改 | 關掉某一項,該按鈕對該角色消失且 API 也擋 | 併入 **2.4 存取控制逐條複驗** 一起做 |
| OW-3 | **訊息級自訂按鈕(Action)**:每則 AI 回覆下方、與「重新產生」同一列的自訂 icon;可指定只給特定使用者/專案 | 新建 | ✅ **已關板 2026-07-30**(WP-A `591a6fc`／WP-B `de32c11`／WP-C `c7ed9a2`／拒絕稽核 `3fc039c`／exec 移除見下):宣告式動作(prompt 模板＋這則訊息 → 送模型)、懸浮視窗選 prompt、結果以 OW-1 手足分支回填、可見性 fail-closed(角色／部門子樹／指定人,admin 不繞過)、每次撰寫寫完整快照稽核＋匯出、呼叫與拒絕皆落稽核且先節流。活體 e2e **8/8 PASS**(含真實瀏覽器驗出處標示可見性)。⚠ 原先寫死在 SPA 的三顆按鈕(翻譯/摘要/公文)已移除,**擁有者建立受治理的動作前,訊息列上不會有自訂按鈕**;三份範本見 `docs/runbooks/ow3-default-actions.md` |

**⚠ OW-3 exec 決定已於 2026-07-30 由擁有者撤回。** 原 07-29 拍板「用 exec」,實作完成並通過三輪審查與活體驗收後,擁有者與同事討論確認:**按鈕不需要執行程式,只要點擊出現懸浮視窗載入預設 prompt**。宣告式路徑正是這個形狀且已交付,因此 exec 面**整包移除**(執行模組、kind/result_mode 判別欄、`ANILA_ENABLE_ACTION_EXEC` 旗標與三個調校設定、執行專屬稽核動作、shell 的直出處理/截斷提示/產出標示、控台的類型切換與能力警告),風險接受文件一併刪除(內容留在 git 歷史)。**因此 P2 掃描不再有這一項高風險,擁有者也不需要簽署風險接受書。**

下方原「三件連帶」中的稽核與權限要求**仍然有效且已實作**(每次建立/更新寫完整快照稽核＋owner 可匯出複查),只有「書面風險接受說明」隨 exec 一起取消。

**撰寫權限(擁有者 2026-07-30 拍板,取代原 owner-only)**:

| 動作 | dev | admin+ | owner |
|---|---|---|---|
| 建立 | ✅ | ✅ | ✅ |
| 修改／刪除／指派誰能按 | ✅ **限自己建的**(依 `created_by_user_id`) | ✅ 任何一筆 | ✅ |
| 稽核匯出 | ❌ | ✅ IP 與 metadata 遮蔽 | ✅ 完整 |

匯出改為 admin 可用並沿用既有稽核頁的遮蔽規則(`api/audit_logs.py` 的 `SENSITIVE_REDACTED`),理由:body 不再是程式碼、只是 prompt 文字,原本 owner-only 的依據消失;且規格 §8 要求「admin 查、要能匯出給稽核單位」,只有 owner 能匯出在一人維運下是瓶頸。

**OW-3 的關鍵設計問題:按下去之後跑的是什麼。** 按鈕本身沒有安全疑慮,有疑慮的只是 Open WebUI 交付它的那個做法(管理員貼 Python,後端同進程 `exec`,擁有讀 `.env`/讀檔/發網路請求的完整權限 —— 等於讓「平台管理員」與「主機 root」透過一個網頁表單變成同一件事,而且貼在表單裡的程式碼比放在 git 裡難審)。ANILA 有三條不新增執行面的路:

| 機制 | 適合的按鈕 | 代價 |
|---|---|---|
| **宣告式**(prompt 模板＋這則訊息 → 送模型) | 翻譯、摘要、改寫語氣、抽待辦 —— 推測涵蓋多數需求 | 幾乎沒有;不能做「去撈資料」 |
| **接既有 agent** | 需要外部資料或多步驟處理 | 沿用 agent registry 的七態核准與 trace-test 閘門;agent 跑在 MLSteam,天然隔離 |
| **接 n8n**(已保留部署) | 觸發工作流程、跨系統動作 | Code Node 白名單已鎖;需設計 webhook 與回填路徑 |

⚠ **選哪條取決於同仁實際要的按鈕是什麼**,這題還沒問到。若都是翻譯/摘要類,宣告式就夠,整個執行面的討論用不到。

**OW-1 的時效性(這是本次盤點唯一急的事)**:ANILA 現在的 `messages` 是線性的(已查證,無 `parent_id`)。那份報告對這條的原話是「**務必第一天就選對**」——因為編輯、重新產生、多模型並列全都是同一棵樹上的分支。ANILA 已過第一天,但**還沒有真實對話資料**;等三千人開始累積,再加 `parent_id` 就要處理歷史資料 + 所有假設線性的程式碼。要做就趁現在,不做就明講。

### 已具備(不必再做,列出以免重複投入)

向量庫 adapter(pgvector)、storage provider 抽象、OpenAI-compatible 模型代理、知識庫與文件載入管線、**對話附件整份進 context**(P1.5 已做)、RBAC 與群組(P1.3 unit_admin 已做)、稽核 log、引用來源呈現。

### 刻意不做(理由寫下來,免得日後被當成漏掉)

| 項目 | 理由 |
|---|---|
| Open WebUI 那種 **`exec` 式 Functions**(貼 Python 進網頁表單、後端同進程執行) | ⚠ 注意:**要做的是「訊息級自訂按鈕」本身(OW-3,已排入)**,不做的只是這個特定的執行機制。理由見 OW-3 的表格:它讓平台管理員等同主機 root,且程式碼繞過 git 無從審查。同樣的按鈕用宣告式/agent/n8n 交付,不必付這個代價 |
| Channels(類 Slack)、Notes(Yjs CRDT 共編) | 報告自評「社群型產品才需要;CRDT 共編成本高,可晚做」;SYSTEM-MAP 也沒有這需求 |
| 30+ 網頁搜尋 provider | 氣隙內網沒有外網 |
| Arena 盲測 + Elo、圖像生成多引擎、TTS/STT 多引擎 | 不在 SYSTEM-MAP;內網也沒有這些服務 |
### 過度工程盤點:agent 核准流程(2026-07-29 查證)

擁有者質疑 agent registry 的七態核准是過度工程。**查證結果:成立。**

| 項目 | SYSTEM-MAP 怎麼說 | 實際做了什麼 | 判定 |
|---|---|---|---|
| agent 上線流程 | §「知識庫怎麼運作」八步表:**3 註冊拿 key → 4 admin 指派誰能用 → 5 使用者選它**。無任何核准關卡 | `approval_status` **七值**(draft/pending_connection_test/pending_trace_test/pending_security_review/approved/rejected/disabled),`app/models/agent.py:83-89` | ❌ **無規格依據**,來自 doc 05 |
| trace-test 閘門 | §7 明文:「一張表加幾個索引。**不需要 span 樹、parent 關係、trace id**」 | `trace_test_passed_at` 非空才可核准(硬閘),`audit_level` 預設 `full_trace` 標為 approval blocker,`agent.py:90-100` | ❌ **無規格依據**,且與 §7 相斥 |
| 密級 ceiling | §「稽核」第 267 行:「collection 與 **agent 上有一個列管標記**」;五級分類是硬需求 | `classification_ceiling`,派工時檢查 | ✅ **有依據,建議保留** |

**根因**:這些欄位的註解逐條寫著來源是 `doc 05`(`docs/anila-redesign-docs/05-*`),而 SYSTEM-MAP 開頭寫明「**這份取代先前所有規劃文件的權威地位**」。是舊文件的殘留。

**實務代價**:現況 dev 註冊 agent 後卡在 `pending_connection_test`,要走三關才能核准。一人維運下,這是每次都要自己走完的憑空流程。

| # | 事情 | 型態 | 驗證 |
|---|---|---|---|
| OE-1 | agent 核准流程收斂為 SYSTEM-MAP 的形狀:註冊 → admin 指派 → 可用 | ✅ **已關板 2026-07-30**(`r1_0020`):七態→三態,trace-test 硬閘與 full_trace blocker 移除,`classification_ceiling` 與 G9 降級規則不動。原本的閘門**降級成工具**——連線測試還在,開發者想診斷隨時可跑,只是不再擋人。端到端證明(註冊→指派→200)而非讀狀態機。連帶解鎖 D1 | 註冊一個 agent 後,admin 指派使用者即可使用,中間無其他關卡;既有 agent 資料經 migration 對應到新狀態不遺失 |
| OE-2 | ⚠ **系統性稽核:全樹對照 SYSTEM-MAP 找過度工程**。擁有者提醒本樹是從「被做壞的」狀態複製而來,agent 只是第一個。入口:原始碼註解留著 `doc 0N §M` 出處(帶引用檔案 70 個,`grep -rEc "doc ?0[0-9] ?§[0-9]+"` 可重測;先前 83 為不同量法)。`r1_0001`–`r1_0008` 全部是照那批舊文件建的 schema | 查證＋修改 | ✅ **已完成 2026-07-29**,產出與收斂排程見下方「OE-2 稽核結果」 |

⚠ **OE-2 的判準(不要簡化成「有引用就砍」)**:SYSTEM-MAP 真正要的東西也帶 doc 引用——例如**分類的單向閂鎖是硬需求**,照樣寫著 `doc 08 §N`,砍錯會拆掉真需求。三問全中才是候選:①SYSTEM-MAP 有沒有要 ②有沒有持續成本(每次要走的流程/要維護的欄位/限制設計的約束) ③唯一理由是不是只剩舊文件。
最可疑的一塊:`trace_span` / `api/traces.py` / `proxy/spans.py`(doc 05 §6)——SYSTEM-MAP §7 幾乎是直接否定:「一張表加幾個索引。**不需要 span 樹、parent 關係、trace id**」。

⚠ **注意「閂鎖是需求」不等於「五級是需求」** —— 見下方 OE-3,分類等級本身就是一個已查證的實例。

| # | 事情 | 型態 | 驗證 |
|---|---|---|---|
| OE-3 | ⚠ **分類等級對齊 SYSTEM-MAP**。規格 §8:「實際會碰到:**無機密 / 營業秘密 / 密 / 機密**」(四級,門檻表欄位亦同);程式碼 `schemas/contracts/classification.py:39-43` 是**無機密 < 營業秘密 < 機密 < 極機密 < 絕對機密**(五級)。**「密」在現行程式碼無法表示**,而「機密」在兩邊位階不同(規格為最高、程式碼為中間),多出的極機密/絕對機密是規格明說不會碰到的 | 修改 | ✅ **已關板 2026-07-29**(merge `c4bee70`):r1_0003 原地改寫(擁有者裁決,庫可拋)、舊識別字全刪、rank-2 逐點重推導、前端四級化含浮水印;grok 六輪、opus+kimi 雙票(sol 額度罄)、活體驗收五項全過(砍庫從零→r1_0011、e2e 存「密」、422 擋極機密、anila-ui 重建後 read-back)。門檻逐格=OE-4;未活測:requires_encryption backfill 分支(零列) |
| OE-4 | 分類門檻改為規格的兩條線:**可以做 = 密等 ≤ 營業秘密**、**要落稽核 = 密等 ≥ 營業秘密**。SYSTEM-MAP §8 第 244 行自己記著:「現行程式碼是統一判準 `> 無機密` 全部管制,所以營業秘密現在不能分享 —— **要改**」 | 修改 | ✅ **程式碼面已關板 2026-07-30**(merge `ef5ef7c`,=P4.1):閘門/稽核全改讀等級、G4 task-less 落列、artifact export 判定軸退役、搜尋摘要稽核(批次 commit);28 條真值表測試;kimi AWN+opus APPROVE(SQL-trace 覆核);活體:可達格全過、export 面四格全中(同組謂詞)。⚠ **營業秘密活體格 UNVERIFIABLE→見 G9**;遞延債:雙稽核帳整併/share-deny 不落痕(活體已證)/policy_decisions 保留策略/public-share與search面的鏡射突變測試 |
| G9 | ⚠ **對話密等標記介面缺口**(OE-4 活體驗收發現):全庫無任何 API 可把對話設為**營業秘密**或**機密**——classify 端點硬寫「密」、`agents.default_classification_level` 唯一 writer 是 migration、降級生效需三人。規格 §8 說密等「專案啟動時就標好了,平台主要是記錄它」,但平台沒有記錄介面;OE-4 唯一改變行為的營業秘密層因此在生產中惰性 | 新建 | **待擁有者定義**誰能設哪些級(使用者自標?admin?建立時?);落地後回補 OE-4 營業秘密活體格(分享允許+稽核/讀取稽核/搜尋摘要稽核) |

⚠ **OE-3/OE-4 的時效性最高**:分類是**單向閂鎖、沒有降級路徑**。真實資料一旦用錯的等級寫進去,更正要走受治理的解密申請流程或資料遷移。**必須在灌真實資料前處理。**

### OE-2 稽核結果與收斂排程(2026-07-29 關板)

**產出**:`docs/audits/oe2-2026-07-29/`(a–f 六域逐構造表＋`sol-verify.md` 跨家覆核 10 CONFIRMED/9 PARTIAL/0 REFUTED＋`00-consolidated.md` 彙整)。總量 338 構造:**CAT-A 101/CAT-B 80/CAT-C 157**。除「砍」之外另查出 8 條反向缺口(規格有要、碼沒做)與 4 個實作缺陷,見彙整 §2–§3。

**擁有者裁決(2026-07-29)**:
- **R1** 降級申請雙人流程退場,改 admin 單人改密等＋強制落稽核(G1 升密稽核缺口一併補)。
- **R2** 服務註冊表叢收斂,但**保留「未來服務入口掛 CSP 首頁」的能力**——收斂目標是單一入口連結目錄表(名稱/URL/圖示/可見性/啟用),不是 35 欄治理註冊表;launches/callbacks/雙事實來源退場。
- **R3** D4 併入 OE-1 同一工作包。

**收斂包排程**(逐構造細節見彙整 §1,守衛遷移條款見彙整 §4):

| 包 | 內容 | 排程 |
|---|---|---|
| OE-1+D4(+B2) | 七態→三態＋manifest 收斂為最小自我描述＋runtime_config/shadow-draft 退場＋**補 classification_ceiling 寫入路徑**(B2:現況全樹無寫入,防線恆 no-op) | OE-3/OE-4 之後第一包 |
| B1+B3 快修 | health_checker 補 `endpoint_kind="agent"`(現況 http agent 恆判離線)＋auto_seed 過 SSRF 閘/不覆寫 admin 編輯/不自動核准 | 小包快審,與 OE-1 包同窗或緊接 |
| D1 | span/trace/task 子系統退場(規格 §7 逐字否定;熱路徑每訊息 2 span 列＋2 commit＋Task/Snapshot/audit 三列) | OE-1 包之後 |
| D5 | 降級流程退場(R1)＋審批權責名冊＋PolicyDecision 第二本帳＋盤點端點;**boolean 鏡射與 task-less allow 判定式先併入 OE-4 包**(G4) | OE-4 之後 |
| D2(+B4) | 服務表叢→入口連結目錄單表(R2);`launch/token.py` RS256＋JWKS 機械**不刪,改造給 P2.1**(TTL 10 分改回規格 5 分) | P2.1 前或同窗 |
| D6 | model_registry 五個死欄位＋deprecated 別名＋孤兒 resume 端點 | 機械小包,可交弱級批次 |
| D3 | artifact 契約收斂(6 表 92 欄→規格形狀) | 隨 Studio 延後 |

**反向缺口路由**:G2 稽核帳防竄改→**新 2.7**;G4 →併 OE-4;G5 模型整批帶入→**新 4.6**;G6 agent 多庫綁定→**新 4.7**;G7 gateway 連續失敗告警→併 3.2 驗收;G3 artifact 3 天自刪→隨 D3;G8 訊息樹=OW-1(已排)。

⚠ **測試面警示**(執行各包時):域 D 25 測試中 18 鎖規格外構造,`test_export_allow` 斷言「機密可匯出」與規格門檻表相反;域 C `test_contract_classification.py:96` 把規格合法值「密」鎖成非法。**收斂時測試照規格重寫,不是照舊測試修碼**;驗收基線=既有 37 failed/1 skipped/2 errors(環境相依 pre-existing)。

### 兩條確認(不是工作項,但影響既有規劃)

- **用量分析 ≠ 計費**:報告明講 Open WebUI 的 analytics 是「分析導向,**非計費錢包**」。所以計價 epic(credit ledger、pool、個人上限)**沒有現成的可抄**,仍須自建——這與 07-24 的組內決議一致。
- **「會換的東西第一天就 adapter 化」**:ANILA 在向量庫、storage、模型代理三處已經是這個形狀,值得在之後新增外部依賴時繼續套用。

---
## ⚠ 誠實的風險評估

**一個月、一個人、上面這些,是緊的。** 我不打算假裝它寬鬆。

**最可能來不及的是 P1**(部門樹 + 單位管理員 + 批次核准),因為那是**新建**,不是修改,而且牽動權限模型。

**如果要砍,我建議這個順序:**

1. 先砍 **P4 的浮水印**(4.2)——它是嚇阻,不是阻擋,晚一版上不影響安全底線
2. 再砍 **ANILALM 整個** —— 它不是三千人的主線,ANILA 才是。ANILALM 可以在第二批上
3. **P1 與 P2 不能砍**:P1 錯了報表就是假的,P2 沒過就上不了線

**絕對不能砍的三件事:**

- **3.1 備份** —— 現在就有風險,不是上線才有
- **2.1 身分簽章** —— 它同時是安全與計費的前提
- **1.1/1.2 部門樹與加總** —— 沒有它,長官要的月報表根本產不出來

---

## 進行方式

**每一項做完就驗證**,不要累積到最後一起測。驗證欄不是形式,是「怎麼知道它真的好了」。

**驗證要驗行為,不要驗程式碼讀起來對不對。** 今天的三個實例:
- 浮水印的深淺主題只能用眼睛判斷 → 用真實背景色渲染兩張圖來看,結果與審查的猜測相反
- 連線池只證明「有呼叫 close()」不算數 → 要在呼叫外部服務的當下量到連線真的回到池子裡
- 測試若把 production 改動還原回去仍然通過,那條測試等於不存在 → 派驗收固定要求說出「改了哪一行、確認變紅」

**審查閘門分三級**(擁有者 2026-07-30 為提速拍板):
紅線級(分類/認證/權限/migration/不可逆)= 雙票跨家＋fresh 驗收;
一般功能 = 單票(報 HIGH 才叫第二票);文字/機械改動 = 指揮官自己 read-back。
**判級看「改到什麼」不是行數。檔案集不相交的包可以平行跑**(各自 worktree)。

⚠ **審查者給的「修法方向」是假設,不是解答。** 派修訂輪要傳**不變式與驗收情境**,
不要傳「照審查者說的改」;同一缺陷兩輪關不掉就問「這兩個目標是不是互斥」。
